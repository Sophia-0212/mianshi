
我们项目最初是单进程解析入库，没有分批，在做批量解析入库的时候，发现内存占用过高，于是修改成单进程分批入库，这样解决了内存占用过高的问题，但是仍未解决解析入库流程非常缓慢的问题，后面我们把入库拆成两个进程：一个负责 Markdown 解析、切分和20个一组；另一个负责 Embedding 并写入 Milvus。两个进程通过有界队列传递批次，解析和写入可以并行，提升入库率；队列满时暂停解析，防止内存持续增长。



Python 支持多进程，使用 Python 的 multiprocessing.Process 创建两个子进程，通过multiprocessing.Queue 传递批次数据。