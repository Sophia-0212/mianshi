




--------
# 两条Agentic工作流：graph1 与 graph2 —— 面试问题清单

## 1. 整体设计

- 你这项目里怎么有两个RAG实现？一个简单一个复杂，是分开用在不同场景的吗？
- RAG 和 Self-RAG 的关系是什么？

## 2. graph1：简单工具调用型RAG

- graph1的图结构是怎样的？
- `agent`节点为什么画成"决策+可直接生成回答"？
- `tools_condition`是什么，怎么工作的？
- `retrieve`/`grade_documents`/`rewrite`/`generate`这几个节点分别做什么？

## 3. graph1 vs graph2 设计对比

- graph1用的是`tools_condition`，graph2为什么要自己手写`route_question`？
- `rewrite`回的是`agent`，`transform_query`回的是`retrieve`，为什么不一样？
- graph1没有生成质量校验、没有收敛计数，是故意这么设计的吗？

## 4. graph2：Self-RAG自纠错流程

- graph2整体流程是怎样的？（30秒话术）
- State字段（`graph_state2.py`）都有哪些，分别是干什么用的？

### 问题路由

- 问题路由这一步具体怎么实现的？
- 为什么要加路由这一步？
- 路由判断错了会怎样？

### 文档相关性打分

- 文档相关性打分外层怎么调用的？是整批判断还是逐篇判断？
- LLM内部具体怎么判断一篇文档相关不相关？prompt是怎么设计的？
- 如果检索回来的所有文档都被打成不相关，接下来怎么处理？
- 打分会不会增加延迟？

### 生成结果的双重评估

- "幻觉检测"和"答案匹配度评估"这两层具体分别检查什么？为什么要分两层不合并成一层？
- 为什么必须是这个先后顺序，不能颠倒或者合并成一步？
- 幻觉检测判定为"有幻觉"之后走哪个分支？
- 答案匹配度没通过之后走哪个分支？跟文档不相关走的分支是同一个吗？
- 幻觉检测这一步如果反复判定失败会怎样？有没有重试上限？

### 查询重写与升级web搜索兜底

- 查询重写具体是怎么做的？
- `transform_count`达到多少次会升级web搜索，为什么定这个数字？
- 升级到web搜索之后，还会再走一遍文档相关性打分吗？

### 整体设计权衡

- 这套流程比简单RAG多绕这么多层，会不会导致响应时间明显变长？怎么权衡准确率和延迟？
- 会不会死循环？

## 5. 两者核心区别对比

- graph1和graph2在打分粒度、结果承载方式、循环保护、LLM调用次数等方面具体有什么区别？

## 6. LangGraph背景知识

- 独立node和条件边函数有什么区别？
- 项目里`grade_documents`这个名字在graph1和graph2里分别是node还是条件边函数？为什么容易搞混？
- `ToolNode`是什么，内部怎么工作的？
- `add_messages`这个reducer是干什么的？
- `MemorySaver`是什么，两条pipeline有没有用到？
- `HumanMessage`/`AIMessage`/`ToolMessage`三种消息类型分别代表什么？
- `tool_calls`是独立的消息类型吗？

## 7. 两阶段过滤

- Milvus检索阶段和LLM打分阶段这两次过滤分别做了什么，有什么区别？
- Milvus检索时用了哪些过滤条件？
- 为什么Milvus已经过滤完了，还要LLM再判断一次？

## 8. 召回率与准确率实测数据

- 你说的召回率提升多少，这个数字怎么来的？
- 测试集是怎么构建的？
- 检索层、过滤层、生成层分别测了什么指标，结果如何？
- 过滤层Precision提升了，为什么生成层的质量没有明显提升？
- 具体是什么机制导致"过滤越精细，生成质量不一定越好"？
- 这次评测的局限性是什么？

## 9. 技术方案迭代动机

- 你们为什么要做从graph1到graph2这次迭代？
- 有哪些可能的线上case推动了这次迭代？
- graph2是不是彻底解决了graph1的问题？
