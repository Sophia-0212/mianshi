# 116. 填充每个节点的下一个右侧节点指针

**题目描述**

给定一棵**完美二叉树**（每一层的节点都被完全填充，且所有叶子节点都在同一层），树的每个节点除了 `left`、`right` 指针外，还多了一个 `next` 指针。请填充每个节点的 `next` 指针，让它指向其同一层的右侧下一个节点。如果找不到下一个右侧节点，则将 `next` 设为 `null`。初始状态下所有 `next` 指针都为 `null`。

```
示例：
输入: root = [1,2,3,4,5,6,7]
输出: [1,#,2,3,#,4,5,6,7,#]
解释: 给定的二叉树是完美二叉树，
      填充后：第 1 层的 1 的 next 指向 null（因为它是本层唯一节点）
      第 2 层：2 -> 3 -> null
      第 3 层：4 -> 5 -> 6 -> 7 -> null
      （# 表示每层的结尾，即该层最后一个节点的 next 是 null）
```

## 思路

- 利用 `next`：沿当前层连接下一层。`left.next=right`；若当前节点有后邻，令 `right.next=node.next.left`。逐层下降。时间 O(n)，空间 O(1)。
- BFS：逐层连接相邻节点，层尾指向空。时间 O(n)，空间 O(w)，w 为最大层宽。



## Python

### LeetCode 写法与原有示例

标准库没有针对这种"树内指针原地连接"问题的一行解法，本题只写手写实现：

```python
class Node:
    # 方法：__init__；按题目要求处理输入并返回结果，核心算法见方法体。
    def __init__(self, val=0, left=None, right=None, next=None):
        self.val = val
        self.left = left
        self.right = right
        self.next = next


class Solution:
    # 方法：connect；按题目要求处理输入并返回结果，核心算法见方法体。
    def connect(self, root: 'Node') -> 'Node':
        if root is None:
            return root

        level_start = root  # 当前层的最左节点，作为每层的起点
        while level_start.left is not None:  # 只要还有下一层（有左孩子）就继续
            cur = level_start  # cur 沿当前层的 next 链条向右移动
            while cur is not None:
                cur.left.next = cur.right  # 同一父节点下，左孩子 next 指向右孩子
                if cur.next is not None:
                    # 跨父节点连接：cur 的右孩子 next 指向 cur.next 的左孩子
                    cur.right.next = cur.next.left
                cur = cur.next  # 沿当前层链条前进，处理下一个父节点

            level_start = level_start.left  # 下移到下一层的最左节点

        return root
```


面试考察点：能不能想清楚"用上一层已经建好的 next 链条去推出下一层"这个归纳思路，尤其是"跨父节点连接"（也就是 `cur.right.next = cur.next.left`）这一步，这是这道题能从 O(n) 空间优化到 O(1) 空间的关键难点所在。

以下四种语言均采用**思路二（O(1) 空间，利用已连接的 next 层间递进）**实现。

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；max：Python 内置函数：返回最大值。；abs：Python 内置函数：返回数值的绝对值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。

class TreeNode:
    # 节点由我们自行定义，脱离平台也可以构造二叉树。
    def __init__(self, val=0, left=None, right=None, next=None):
        self.val, self.left, self.right, self.next = val, left, right, next

Node = TreeNode

def build_tree(values):
    """按层序构造树，None 表示空节点。"""
    if not values or values[0] is None:
        return None
    root = TreeNode(values[0])
    queue = deque([root])
    i = 1
    while queue and i < len(values):
        parent = queue.popleft()
        for side in ('left', 'right'):
            if i < len(values) and values[i] is not None:
                child = TreeNode(values[i])
                setattr(parent, side, child)
                queue.append(child)
            i += 1
    return root

def tree_values(root):
    # 层序输出并去掉末尾空节点，方便和题目示例比较。
    result, queue = [], deque([root])
    while queue:
        node = queue.popleft()
        result.append(node.val if node else None)
        if node:
            queue.extend([node.left, node.right])
    while result and result[-1] is None:
        result.pop()
    return result

def find_node(root, value):
    if root is None or root.val == value:
        return root
    return find_node(root.left, value) or find_node(root.right, value)

def inorder_values(root):
    return inorder_values(root.left) + [root.val] + inorder_values(root.right) if root else []

def checked_height(root):
    if root is None:
        return 0
    left, right = checked_height(root.left), checked_height(root.right)
    assert abs(left - right) <= 1, "生成的树不平衡"
    return 1 + max(left, right)

def next_levels(root):
    # 沿 next 指针读取每层，实际检查连接结果。
    result = []
    while root:
        row, node = [], root
        while node:
            row.append(node.val)
            node = node.next
        result.append(row)
        root = root.left
    return result

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：connect；按题目要求处理输入并返回结果，核心算法见方法体。
def connect(root: 'Node') -> 'Node':
    if root is None:
        return root

    level_start = root  # 当前层的最左节点，作为每层的起点
    while level_start.left is not None:  # 只要还有下一层（有左孩子）就继续
        cur = level_start  # cur 沿当前层的 next 链条向右移动
        while cur is not None:
            cur.left.next = cur.right  # 同一父节点下，左孩子 next 指向右孩子
            if cur.next is not None:
                # 跨父节点连接：cur 的右孩子 next 指向 cur.next 的左孩子
                cur.right.next = cur.next.left
            cur = cur.next  # 沿当前层链条前进，处理下一个父节点

        level_start = level_start.left  # 下移到下一层的最左节点

    return root


if __name__ == "__main__":
    # 用例 1：输入 ({'tree': [1, 2, 3, 4, 5, 6, 7]},)；期望 [[1], [2, 3], [4, 5, 6, 7]]。
    arg0 = build_tree([1, 2, 3, 4, 5, 6, 7])
    result = connect(arg0)
    result = next_levels(result)
    expected = [[1], [2, 3], [4, 5, 6, 7]]
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ({'tree': [1]},)；期望 [[1]]。
    arg0 = build_tree([1])
    result = connect(arg0)
    result = next_levels(result)
    expected = [[1]]
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ({'tree': []},)；期望 []。
    arg0 = build_tree([])
    result = connect(arg0)
    result = next_levels(result)
    expected = []
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
type Node struct {
    Val   int
    Left  *Node
    Right *Node
    Next  *Node
}

// 方法：connect；按题目要求处理输入并返回结果，核心算法见方法体。
func connect(root *Node) *Node {
    if root == nil {
        return root
    }

    levelStart := root // 当前层的最左节点，作为每层的起点
    for levelStart.Left != nil {  // 只要还有下一层就继续
        cur := levelStart // cur 沿当前层的 next 链条向右移动
        for cur != nil {
            cur.Left.Next = cur.Right // 同一父节点下，左孩子 next 指向右孩子
            if cur.Next != nil {
                // 跨父节点连接
                cur.Right.Next = cur.Next.Left
            }
            cur = cur.Next // 沿当前层链条前进
        }
        levelStart = levelStart.Left // 下移到下一层最左节点
    }
    return root
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 自定义节点和层序建树函数，nil 表示空节点。
type TreeNode struct { Val int; Left, Right, Next *TreeNode }
type Node = TreeNode
func buildTreeInput(values []interface{}) *TreeNode {
    if len(values) == 0 || values[0] == nil { return nil }
    root := &TreeNode{Val: values[0].(int)}
    q := []*TreeNode{root}; i := 1
    for len(q)>0 && i<len(values) {
        n := q[0]; q = q[1:]
        if i<len(values) && values[i]!=nil { n.Left=&TreeNode{Val:values[i].(int)}; q=append(q,n.Left) }; i++
        if i<len(values) && values[i]!=nil { n.Right=&TreeNode{Val:values[i].(int)}; q=append(q,n.Right) }; i++
    }
    return root
}
func treeValues(root *TreeNode) []interface{} {
    result := []interface{}{}; q := []*TreeNode{root}
    for len(q)>0 { n:=q[0]; q=q[1:]; if n==nil { result=append(result,nil) } else { result=append(result,n.Val); q=append(q,n.Left,n.Right) } }
    for len(result)>0 && result[len(result)-1]==nil { result=result[:len(result)-1] }
    return result
}
func findNode(root *TreeNode, val int) *TreeNode {
    if root==nil || root.Val==val { return root }
    if n:=findNode(root.Left,val); n!=nil { return n }; return findNode(root.Right,val)
}
func inorderValues(root *TreeNode) []int {
    if root==nil { return []int{} }; a:=inorderValues(root.Left); a=append(a,root.Val); return append(a,inorderValues(root.Right)...)
}
func checkedHeight(root *TreeNode) int {
    if root==nil { return 0 }; l,r:=checkedHeight(root.Left),checkedHeight(root.Right)
    if l-r>1 || r-l>1 { panic("tree is not balanced") }; if l>r {return l+1}; return r+1
}
func nextLevels(root *TreeNode) [][]int {
    result:=[][]int{}; for ;root!=nil;root=root.Left { row:=[]int{}; for n:=root;n!=nil;n=n.Next { row=append(row,n.Val) }; result=append(result,row) }; return result
}

// 普通函数和题目中的核心算法。
// 方法：connect；按题目要求处理输入并返回结果，核心算法见方法体。
func connect(root *Node) *Node {
    if root == nil {
        return root
    }

    levelStart := root // 当前层的最左节点，作为每层的起点
    for levelStart.Left != nil {  // 只要还有下一层就继续
        cur := levelStart // cur 沿当前层的 next 链条向右移动
        for cur != nil {
            cur.Left.Next = cur.Right // 同一父节点下，左孩子 next 指向右孩子
            if cur.Next != nil {
                // 跨父节点连接
                cur.Right.Next = cur.Next.Left
            }
            cur = cur.Next // 沿当前层链条前进
        }
        levelStart = levelStart.Left // 下移到下一层最左节点
    }
    return root
}

func main() {
    // 用例 1：输入 ({'tree': [1, 2, 3, 4, 5, 6, 7]},)；期望 [[1], [2, 3], [4, 5, 6, 7]]。
    {
        arg0 := buildTreeInput([]interface{}{1, 2, 3, 4, 5, 6, 7})
        result := connect(arg0)
        data, err := json.Marshal(nextLevels(result))
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[[1], [2, 3], [4, 5, 6, 7]] 实际=", string(data))
    }
    // 用例 2：输入 ({'tree': [1]},)；期望 [[1]]。
    {
        arg0 := buildTreeInput([]interface{}{1})
        result := connect(arg0)
        data, err := json.Marshal(nextLevels(result))
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[[1]] 实际=", string(data))
    }
    // 用例 3：输入 ({'tree': []},)；期望 []。
    {
        arg0 := buildTreeInput([]interface{}{})
        result := connect(arg0)
        data, err := json.Marshal(nextLevels(result))
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[] 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Node {
public:
    int val;
    Node* left;
    Node* right;
    Node* next;

    Node() : val(0), left(nullptr), right(nullptr), next(nullptr) {}
    Node(int _val) : val(_val), left(nullptr), right(nullptr), next(nullptr) {}
};

class Solution {
public:
    Node* connect(Node* root) {
        if (root == nullptr) return root;

        Node* levelStart = root;  // 当前层的最左节点，作为每层的起点
        while (levelStart->left != nullptr) {  // 只要还有下一层（有左孩子）就继续
            Node* cur = levelStart;  // cur 沿当前层的 next 链条向右移动
            while (cur != nullptr) {
                cur->left->next = cur->right;  // 同一父节点下，左孩子的 next 指向右孩子
                if (cur->next != nullptr) {
                    // 跨父节点连接：cur 的右孩子 next 指向 cur.next 的左孩子
                    cur->right->next = cur->next->left;
                }
                cur = cur->next;  // 沿当前层链条前进，处理下一个父节点
            }
            levelStart = levelStart->left;  // 下移到下一层的最左节点
        }
        return root;
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

struct TreeNode {
    int val;
    TreeNode *left, *right, *next;
    TreeNode(int value = 0, TreeNode* l = nullptr, TreeNode* r = nullptr)
        : val(value), left(l), right(r), next(nullptr) {}
};
using Node = TreeNode;

// 层序建树；nullopt 表示空节点。
TreeNode* buildTreeInput(const vector<optional<int>>& values) {
    if (values.empty() || !values[0]) return nullptr;
    auto* root = new TreeNode(*values[0]);
    queue<TreeNode*> q; q.push(root);
    size_t i = 1;
    while (!q.empty() && i < values.size()) {
        auto* node = q.front(); q.pop();
        if (i < values.size() && values[i]) { node->left = new TreeNode(*values[i]); q.push(node->left); }
        ++i;
        if (i < values.size() && values[i]) { node->right = new TreeNode(*values[i]); q.push(node->right); }
        ++i;
    }
    return root;
}
vector<optional<int>> treeValues(TreeNode* root) {
    vector<optional<int>> result;
    queue<TreeNode*> q; q.push(root);
    while (!q.empty()) {
        auto* node = q.front(); q.pop();
        if (node) { result.push_back(node->val); q.push(node->left); q.push(node->right); }
        else result.push_back(nullopt);
    }
    while (!result.empty() && !result.back()) result.pop_back();
    return result;
}
TreeNode* findNode(TreeNode* root, int val) {
    if (!root || root->val == val) return root;
    auto* left = findNode(root->left, val);
    return left ? left : findNode(root->right, val);
}
vector<int> inorderValues(TreeNode* root) {
    if (!root) return {};
    auto a = inorderValues(root->left), b = inorderValues(root->right);
    a.push_back(root->val); a.insert(a.end(), b.begin(), b.end()); return a;
}
int checkedHeight(TreeNode* root) {
    if (!root) return 0;
    int l = checkedHeight(root->left), r = checkedHeight(root->right);
    if (abs(l - r) > 1) throw runtime_error("tree is not balanced");
    return 1 + max(l, r);
}
vector<vector<int>> nextLevels(TreeNode* root) {
    vector<vector<int>> result;
    for (; root; root = root->left) {
        vector<int> row;
        for (auto* n = root; n; n = n->next) row.push_back(n->val);
        result.push_back(row);
    }
    return result;
}
void show(TreeNode* root) { show(treeValues(root)); }

namespace interview {

// 普通函数与辅助函数声明。
Node* connect(Node* root);

Node* connect(Node* root) {
        if (root == nullptr) return root;

        Node* levelStart = root;  // 当前层的最左节点，作为每层的起点
        while (levelStart->left != nullptr) {  // 只要还有下一层（有左孩子）就继续
            Node* cur = levelStart;  // cur 沿当前层的 next 链条向右移动
            while (cur != nullptr) {
                cur->left->next = cur->right;  // 同一父节点下，左孩子的 next 指向右孩子
                if (cur->next != nullptr) {
                    // 跨父节点连接：cur 的右孩子 next 指向 cur.next 的左孩子
                    cur->right->next = cur->next->left;
                }
                cur = cur->next;  // 沿当前层链条前进，处理下一个父节点
            }
            levelStart = levelStart->left;  // 下移到下一层的最左节点
        }
        return root;
    }

}

int main() {
    // 用例 1：输入 ({'tree': [1, 2, 3, 4, 5, 6, 7]},)；期望 [[1], [2, 3], [4, 5, 6, 7]]。
    {
        auto* arg0 = buildTreeInput({1, 2, 3, 4, 5, 6, 7});
        auto result = interview::connect(arg0);
        cout << "用例 1: 期望=[[1], [2, 3], [4, 5, 6, 7]] 实际=";
        show(nextLevels(result));
        cout << "\n";
    }
    // 用例 2：输入 ({'tree': [1]},)；期望 [[1]]。
    {
        auto* arg0 = buildTreeInput({1});
        auto result = interview::connect(arg0);
        cout << "用例 2: 期望=[[1]] 实际=";
        show(nextLevels(result));
        cout << "\n";
    }
    // 用例 3：输入 ({'tree': []},)；期望 []。
    {
        auto* arg0 = buildTreeInput({});
        auto result = interview::connect(arg0);
        cout << "用例 3: 期望=[] 实际=";
        show(nextLevels(result));
        cout << "\n";
    }
    return 0;
}```

## 总结

- BFS 层序遍历是比较通用的解法，思路简单，但代价是要用 O(n) 的队列空间；而利用已经连好的 next 链条逐层往下推的方法呢，能做到 O(1) 额外空间，不过它的代价是只能用在完美二叉树这种同层节点数量固定、左右孩子必定成对出现的特殊结构上。
- O(1) 解法说白了本质就是一个归纳的过程：上一层的 next 链条连好了，才有条件去连下一层，根节点自己就构成了归纳的起点，因为它就一个节点，next 天然是 null。
- 这里最容易出错的地方就是"跨父节点连接"这一步（也就是 `cur.right.next = cur.next.left`），很多人容易只记得去连同一个父节点下的左右孩子，却忘了不同父节点之间在下一层其实也是相邻的，这一步必须单独处理才行。
- 整个循环可以这么理解：外层循环控制的是"还有没有下一层"（通过 `levelStart.left != nil` 来判断，完美二叉树能保证不会出现只有一侧孩子的怪情况），内层循环控制的是"在当前层内从左往右移动，顺便把下一层连好"。这两层循环分别对应着"层间往下推进"和"层内从左到右遍历"，把这两层循环各自的职责想清楚，是写对这道题代码的关键。
