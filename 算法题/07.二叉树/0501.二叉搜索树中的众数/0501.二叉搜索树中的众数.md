# 0501. 二叉搜索树中的众数

**题目描述**

给定一个含重复值的二叉搜索树（BST）的根节点 `root`，找出并返回 BST 中的所有众数（出现频率最高的元素）。如果树中有多个众数，则返回它们的数组（顺序任意）。

```
示例：
输入: root = [1,null,2,2]
输出: [2]
解释: 节点值 2 出现了 2 次，1 出现了 1 次，2 是众数。
```

## 思路

- 中序计数：BST 中序非递减，相同值连续出现。记录前值、连续次数和最大次数；超过最大值清空答案，相等则加入。时间 O(n)，辅助空间 O(h)，不计结果。
- 哈希计数：遍历统计频次，输出最大频次对应值。平均时间 O(n)，空间 O(n)。

h 为树高。



## Python

### LeetCode 写法与原有示例

```python
class TreeNode:
    # 方法：__init__；按题目要求处理输入并返回结果，核心算法见方法体。
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


class Solution:
    # 方法：findMode；按题目要求处理输入并返回结果，核心算法见方法体。
    def findMode(self, root: TreeNode) -> list[int]:
        self.pre = None       # 上一个访问的节点
        self.count = 0          # 当前值连续出现次数
        self.max_count = 0      # 最大出现次数
        self.result = []        # 众数列表

        # 方法：inorder；按题目要求处理输入并返回结果，核心算法见方法体。
        def inorder(node: TreeNode) -> None:
            if node is None:
                return  # 终止条件
            inorder(node.left)  # 先递归左子树

            if self.pre is not None and self.pre.val == node.val:
                self.count += 1  # 与上一个值相同，计数加 1
            else:
                self.count = 1     # 新的值区间，重置计数

            if self.count > self.max_count:
                self.max_count = self.count  # 发现更高频率的众数
                self.result = [node.val]        # 清空之前的记录
            elif self.count == self.max_count:
                self.result.append(node.val)     # 频率相同，加入众数列表

            self.pre = node       # 更新 pre
            inorder(node.right)  # 最后递归右子树

        inorder(root)
        return self.result
```


二叉树节点定义：

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；isinstance：Python 内置函数：判断对象是否属于指定类型。；len：Python 内置函数：返回容器中元素的数量。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；max：Python 内置函数：返回最大值。；abs：Python 内置函数：返回数值的绝对值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；isinstance：Python 内置函数：判断对象是否属于指定类型。；len：Python 内置函数：返回容器中元素的数量。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。

class TreeNode:
    # 节点由我们自行定义，脱离平台也可以构造二叉树。
    def __init__(self, val=0, left=None, right=None, next=None):
        self.val, self.left, self.right, self.next = val, left, right, next

Node = TreeNode

def build_tree(values):
    """按层序构造树，None 表示空节点。"""
    if not values or values[0] is None:
        return None
    root = TreeNode(values[0])
    queue = deque([root])
    i = 1
    while queue and i < len(values):
        parent = queue.popleft()
        for side in ('left', 'right'):
            if i < len(values) and values[i] is not None:
                child = TreeNode(values[i])
                setattr(parent, side, child)
                queue.append(child)
            i += 1
    return root

def tree_values(root):
    # 层序输出并去掉末尾空节点，方便和题目示例比较。
    result, queue = [], deque([root])
    while queue:
        node = queue.popleft()
        result.append(node.val if node else None)
        if node:
            queue.extend([node.left, node.right])
    while result and result[-1] is None:
        result.pop()
    return result

def find_node(root, value):
    if root is None or root.val == value:
        return root
    return find_node(root.left, value) or find_node(root.right, value)

def inorder_values(root):
    return inorder_values(root.left) + [root.val] + inorder_values(root.right) if root else []

def checked_height(root):
    if root is None:
        return 0
    left, right = checked_height(root.left), checked_height(root.right)
    assert abs(left - right) <= 1, "生成的树不平衡"
    return 1 + max(left, right)

def next_levels(root):
    # 沿 next 指针读取每层，实际检查连接结果。
    result = []
    while root:
        row, node = [], root
        while node:
            row.append(node.val)
            node = node.next
        result.append(row)
        root = root.left
    return result

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
def findMode(root):
    # 每次调用独立保存递归状态，连续运行多组用例互不影响。
    state = {}
    # 方法：findMode；按题目要求处理输入并返回结果，核心算法见方法体。
    def findMode(root: TreeNode) -> list[int]:
        state["pre"] = None       # 上一个访问的节点
        state["count"] = 0          # 当前值连续出现次数
        state["max_count"] = 0      # 最大出现次数
        state["result"] = []        # 众数列表

        # 方法：inorder；按题目要求处理输入并返回结果，核心算法见方法体。
        def inorder(node: TreeNode) -> None:
            if node is None:
                return  # 终止条件
            inorder(node.left)  # 先递归左子树

            if state["pre"] is not None and state["pre"].val == node.val:
                state["count"] += 1  # 与上一个值相同，计数加 1
            else:
                state["count"] = 1     # 新的值区间，重置计数

            if state["count"] > state["max_count"]:
                state["max_count"] = state["count"]  # 发现更高频率的众数
                state["result"] = [node.val]        # 清空之前的记录
            elif state["count"] == state["max_count"]:
                state["result"].append(node.val)     # 频率相同，加入众数列表

            state["pre"] = node       # 更新 pre
            inorder(node.right)  # 最后递归右子树

        inorder(root)
        return state["result"]
    return findMode(root)


if __name__ == "__main__":
    # 用例 1：输入 ({'tree': [1, None, 2, 2]},)；期望 [2]。
    arg0 = build_tree([1, None, 2, 2])
    result = findMode(arg0)
    if isinstance(result, TreeNode) or result is None:
        result = tree_values(result)
    expected = [2]
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 2：输入 ({'tree': [0]},)；期望 [0]。
    arg0 = build_tree([0])
    result = findMode(arg0)
    if isinstance(result, TreeNode) or result is None:
        result = tree_values(result)
    expected = [0]
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 3：输入 ({'tree': [2, 1, 3]},)；期望 [1, 2, 3]。
    arg0 = build_tree([2, 1, 3])
    result = findMode(arg0)
    if isinstance(result, TreeNode) or result is None:
        result = tree_values(result)
    expected = [1, 2, 3]
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)
```

## Go

### LeetCode 写法与原有示例

```go
type TreeNode struct {
    Val   int
    Left  *TreeNode
    Right *TreeNode
}

// 方法：findMode；按题目要求处理输入并返回结果，核心算法见方法体。
func findMode(root *TreeNode) []int {
    var pre *TreeNode  // 上一个访问的节点
    count := 0           // 当前值连续出现次数
    maxCount := 0         // 最大出现次数
    result := []int{}      // 众数列表

    var inorder func(node *TreeNode)
    inorder = func(node *TreeNode) {
        if node == nil {
            return // 终止条件
        }
        inorder(node.Left) // 先递归左子树

        if pre != nil && pre.Val == node.Val {
            count++ // 与上一个值相同，计数加 1
        } else {
            count = 1 // 新的值区间，重置计数
        }

        if count > maxCount {
            maxCount = count           // 发现更高频率的众数
            result = []int{node.Val}     // 清空之前的记录
        } else if count == maxCount {
            result = append(result, node.Val) // 频率相同，加入众数列表
        }

        pre = node          // 更新 pre
        inorder(node.Right) // 最后递归右子树
    }

    inorder(root)
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 自定义节点和层序建树函数，nil 表示空节点。
type TreeNode struct { Val int; Left, Right, Next *TreeNode }
type Node = TreeNode
func buildTreeInput(values []interface{}) *TreeNode {
    if len(values) == 0 || values[0] == nil { return nil }
    root := &TreeNode{Val: values[0].(int)}
    q := []*TreeNode{root}; i := 1
    for len(q)>0 && i<len(values) {
        n := q[0]; q = q[1:]
        if i<len(values) && values[i]!=nil { n.Left=&TreeNode{Val:values[i].(int)}; q=append(q,n.Left) }; i++
        if i<len(values) && values[i]!=nil { n.Right=&TreeNode{Val:values[i].(int)}; q=append(q,n.Right) }; i++
    }
    return root
}
func treeValues(root *TreeNode) []interface{} {
    result := []interface{}{}; q := []*TreeNode{root}
    for len(q)>0 { n:=q[0]; q=q[1:]; if n==nil { result=append(result,nil) } else { result=append(result,n.Val); q=append(q,n.Left,n.Right) } }
    for len(result)>0 && result[len(result)-1]==nil { result=result[:len(result)-1] }
    return result
}
func findNode(root *TreeNode, val int) *TreeNode {
    if root==nil || root.Val==val { return root }
    if n:=findNode(root.Left,val); n!=nil { return n }; return findNode(root.Right,val)
}
func inorderValues(root *TreeNode) []int {
    if root==nil { return []int{} }; a:=inorderValues(root.Left); a=append(a,root.Val); return append(a,inorderValues(root.Right)...)
}
func checkedHeight(root *TreeNode) int {
    if root==nil { return 0 }; l,r:=checkedHeight(root.Left),checkedHeight(root.Right)
    if l-r>1 || r-l>1 { panic("tree is not balanced") }; if l>r {return l+1}; return r+1
}
func nextLevels(root *TreeNode) [][]int {
    result:=[][]int{}; for ;root!=nil;root=root.Left { row:=[]int{}; for n:=root;n!=nil;n=n.Next { row=append(row,n.Val) }; result=append(result,row) }; return result
}

// 普通函数和题目中的核心算法。
// 方法：findMode；按题目要求处理输入并返回结果，核心算法见方法体。
func findMode(root *TreeNode) []int {
    var pre *TreeNode  // 上一个访问的节点
    count := 0           // 当前值连续出现次数
    maxCount := 0         // 最大出现次数
    result := []int{}      // 众数列表

    var inorder func(node *TreeNode)
    inorder = func(node *TreeNode) {
        if node == nil {
            return // 终止条件
        }
        inorder(node.Left) // 先递归左子树

        if pre != nil && pre.Val == node.Val {
            count++ // 与上一个值相同，计数加 1
        } else {
            count = 1 // 新的值区间，重置计数
        }

        if count > maxCount {
            maxCount = count           // 发现更高频率的众数
            result = []int{node.Val}     // 清空之前的记录
        } else if count == maxCount {
            result = append(result, node.Val) // 频率相同，加入众数列表
        }

        pre = node          // 更新 pre
        inorder(node.Right) // 最后递归右子树
    }

    inorder(root)
    return result
}

func main() {
    // 用例 1：输入 ({'tree': [1, None, 2, 2]},)；期望 [2]。
    {
        arg0 := buildTreeInput([]interface{}{1, nil, 2, 2})
        result := findMode(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[2] 实际=", string(data))
    }
    // 用例 2：输入 ({'tree': [0]},)；期望 [0]。
    {
        arg0 := buildTreeInput([]interface{}{0})
        result := findMode(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[0] 实际=", string(data))
    }
    // 用例 3：输入 ({'tree': [2, 1, 3]},)；期望 [1, 2, 3]。
    {
        arg0 := buildTreeInput([]interface{}{2, 1, 3})
        result := findMode(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[1, 2, 3] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
    TreeNode(int x) : val(x), left(NULL), right(NULL) {}
};
```

```cpp
// 依赖说明：vector：C++ STL 动态数组。

class Solution {
public:
    // 方法：findMode；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> findMode(TreeNode* root) {
        inorder(root);
        return result;
    }

private:
    TreeNode* pre = nullptr;   // 中序遍历中上一个访问的节点
    int count = 0;              // 当前值的连续出现次数
    int maxCount = 0;           // 目前为止的最大出现次数
    vector<int> result;         // 众数列表

    // 方法：inorder；按题目要求处理输入并返回结果，核心算法见方法体。
    void inorder(TreeNode* root) {
        if (root == nullptr) return; // 终止条件
        inorder(root->left);           // 先递归左子树

        // 处理当前节点：更新连续计数
        if (pre != nullptr && pre->val == root->val) {
            count++;         // 与上一个值相同，连续计数加 1
        } else {
            count = 1;         // 进入新的值区间，重置计数
        }

        // 根据 count 与 maxCount 的关系更新众数列表
        if (count > maxCount) {
            maxCount = count;    // 发现频率更高的众数
            result.clear();       // 之前记录的都不再是众数
            result.push_back(root->val);
        } else if (count == maxCount) {
            result.push_back(root->val); // 频率相同，也是众数之一
        }

        pre = root;             // 更新 pre
        inorder(root->right);   // 最后递归右子树
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

struct TreeNode {
    int val;
    TreeNode *left, *right, *next;
    TreeNode(int value = 0, TreeNode* l = nullptr, TreeNode* r = nullptr)
        : val(value), left(l), right(r), next(nullptr) {}
};
using Node = TreeNode;

// 层序建树；nullopt 表示空节点。
TreeNode* buildTreeInput(const vector<optional<int>>& values) {
    if (values.empty() || !values[0]) return nullptr;
    auto* root = new TreeNode(*values[0]);
    queue<TreeNode*> q; q.push(root);
    size_t i = 1;
    while (!q.empty() && i < values.size()) {
        auto* node = q.front(); q.pop();
        if (i < values.size() && values[i]) { node->left = new TreeNode(*values[i]); q.push(node->left); }
        ++i;
        if (i < values.size() && values[i]) { node->right = new TreeNode(*values[i]); q.push(node->right); }
        ++i;
    }
    return root;
}
vector<optional<int>> treeValues(TreeNode* root) {
    vector<optional<int>> result;
    queue<TreeNode*> q; q.push(root);
    while (!q.empty()) {
        auto* node = q.front(); q.pop();
        if (node) { result.push_back(node->val); q.push(node->left); q.push(node->right); }
        else result.push_back(nullopt);
    }
    while (!result.empty() && !result.back()) result.pop_back();
    return result;
}
TreeNode* findNode(TreeNode* root, int val) {
    if (!root || root->val == val) return root;
    auto* left = findNode(root->left, val);
    return left ? left : findNode(root->right, val);
}
vector<int> inorderValues(TreeNode* root) {
    if (!root) return {};
    auto a = inorderValues(root->left), b = inorderValues(root->right);
    a.push_back(root->val); a.insert(a.end(), b.begin(), b.end()); return a;
}
int checkedHeight(TreeNode* root) {
    if (!root) return 0;
    int l = checkedHeight(root->left), r = checkedHeight(root->right);
    if (abs(l - r) > 1) throw runtime_error("tree is not balanced");
    return 1 + max(l, r);
}
vector<vector<int>> nextLevels(TreeNode* root) {
    vector<vector<int>> result;
    for (; root; root = root->left) {
        vector<int> row;
        for (auto* n = root; n; n = n->next) row.push_back(n->val);
        result.push_back(row);
    }
    return result;
}
void show(TreeNode* root) { show(treeValues(root)); }

namespace interview {
TreeNode* pre = nullptr;   
    int count = 0;              
    int maxCount = 0;           
    vector<int> result;
// 普通函数与辅助函数声明。
vector<int> findModeImpl(TreeNode* root);
void inorder(TreeNode* root);

// 方法：findMode；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> findModeImpl(TreeNode* root) {
        inorder(root);
        return result;
    }

// 方法：inorder；按题目要求处理输入并返回结果，核心算法见方法体。
    void inorder(TreeNode* root) {
        if (root == nullptr) return; // 终止条件
        inorder(root->left);           // 先递归左子树

        // 处理当前节点：更新连续计数
        if (pre != nullptr && pre->val == root->val) {
            count++;         // 与上一个值相同，连续计数加 1
        } else {
            count = 1;         // 进入新的值区间，重置计数
        }

        // 根据 count 与 maxCount 的关系更新众数列表
        if (count > maxCount) {
            maxCount = count;    // 发现频率更高的众数
            result.clear();       // 之前记录的都不再是众数
            result.push_back(root->val);
        } else if (count == maxCount) {
            result.push_back(root->val); // 频率相同，也是众数之一
        }

        pre = root;             // 更新 pre
        inorder(root->right);   // 最后递归右子树
    }
vector<int> findMode(TreeNode* root) {
    // 清空上次调用的辅助状态，三组输入可以连续独立运行。
    pre = nullptr;
    count = 0;
    maxCount = 0;
    result = {};
    return findModeImpl(root);
}
}

int main() {
    // 用例 1：输入 ({'tree': [1, None, 2, 2]},)；期望 [2]。
    {
        auto* arg0 = buildTreeInput({1, nullopt, 2, 2});
        auto result = interview::findMode(arg0);
        cout << "用例 1: 期望=[2] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ({'tree': [0]},)；期望 [0]。
    {
        auto* arg0 = buildTreeInput({0});
        auto result = interview::findMode(arg0);
        cout << "用例 2: 期望=[0] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ({'tree': [2, 1, 3]},)；期望 [1, 2, 3]。
    {
        auto* arg0 = buildTreeInput({2, 1, 3});
        auto result = interview::findMode(arg0);
        cout << "用例 3: 期望=[1, 2, 3] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 这道题的核心就是利用 BST 中序遍历的有序性：相同的值在中序遍历结果里必然是连续排列的，所以可以用一个 `pre` 指针比较相邻节点是否相同，来统计连续出现的次数，完全不需要额外的哈希表。
- 需要维护三个状态变量：`count`（当前值的连续计数）、`maxCount`（目前的最大计数）、`result`（众数列表），然后根据 `count` 跟 `maxCount` 之间大于、等于、小于这三种关系，来决定要不要更新众数列表。
- 有一点要特别注意：`count > maxCount` 的时候一定要记得清空之前的 `result`，为什么呢？因为之前记录的众数已经被更频繁出现的新值给取代了。
- 说白了，这道题就是 0530 那个技巧的自然延伸，都是"中序遍历 + pre 指针"这个模式在不同统计需求下的应用，把这个模式吃透了，就能应对更多"BST 相邻关系"这一类的题目。
