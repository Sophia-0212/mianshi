# 20201003 二叉树周末总结

- 自底向上：后序返回子树结果。高度取左右最大值加一；平衡检查用 -1 传播失衡，避免重复求高。
- 自顶向下：通过参数传路径、累计值、剩余目标，进入孩子前更新；共享路径返回时撤销。
- 最小深度：只有非空子树可形成叶子路径，不能直接对含空子树的深度取最小值。

单次 DFS 通常时间 O(n)，辅助空间 O(h)；逐节点重新求高度最坏 O(n²)，输出路径另计。
