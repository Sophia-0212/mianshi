# 0111. 二叉树的最小深度

**题目描述**

给定一个二叉树的根节点 `root`，返回其最小深度。最小深度是从根节点到**最近叶子节点**的最短路径上的节点数。

```
示例：
输入: root = [3,9,20,null,null,15,7]
      3
     / \
    9  20
      /  \
     15   7
输出: 2  （根到叶子节点9的路径长度为2）

容易踩坑的示例：
输入: root = [2,null,3,null,4,null,5,null,6]
      2
       \
        3
         \
          4
           \
            5
             \
              6
输出: 5  （因为根节点只有右子树，左子树为空但不是叶子，必须走到最深的6才是叶子节点）
```

## 思路

- DFS：空树为 0；只有一侧子树时取非空侧深度加一；两侧均有时取较小值加一。时间 O(n)，空间 O(h)。
- BFS：首次遇到叶子节点即返回当前层数。最坏时间 O(n)，空间 O(w)。

h 为树高，w 为最大层宽。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；min：Python 内置函数：返回最小值。

# 依赖说明：Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：minDepth；按题目要求处理输入并返回结果，核心算法见方法体。
    def minDepth(self, root):
        # 递归法
        if root is None:
            return 0                                # 终止条件
        if root.left is None and root.right is None:
            return 1                                # 叶子节点
        if root.left is None:
            return self.minDepth(root.right) + 1    # 只有右子树，必须走右边
        if root.right is None:
            return self.minDepth(root.left) + 1     # 只有左子树，必须走左边
        return min(self.minDepth(root.left), self.minDepth(root.right)) + 1  # 都存在，取较小值

    # 方法：minDepthBFS；按题目要求处理输入并返回结果，核心算法见方法体。
    def minDepthBFS(self, root):
        # BFS法
        from collections import deque
        if root is None:
            return 0
        queue = deque([root])
        depth = 0
        while queue:
            size = len(queue)
            depth += 1                              # 进入新的一层
            for _ in range(size):
                node = queue.popleft()
                if node.left is None and node.right is None:
                    return depth                    # 第一次遇到叶子节点，直接返回
                if node.left:
                    queue.append(node.left)
                if node.right:
                    queue.append(node.right)
        return depth```


二叉树节点定义：



```python
class TreeNode:
    # 方法：__init__；按题目要求处理输入并返回结果，核心算法见方法体。
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；isinstance：Python 内置函数：判断对象是否属于指定类型。；len：Python 内置函数：返回容器中元素的数量。；min：Python 内置函数：返回最小值。；max：Python 内置函数：返回最大值。；abs：Python 内置函数：返回数值的绝对值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；isinstance：Python 内置函数：判断对象是否属于指定类型。；len：Python 内置函数：返回容器中元素的数量。

class TreeNode:
    # 节点由我们自行定义，脱离平台也可以构造二叉树。
    def __init__(self, val=0, left=None, right=None, next=None):
        self.val, self.left, self.right, self.next = val, left, right, next

Node = TreeNode

def build_tree(values):
    """按层序构造树，None 表示空节点。"""
    if not values or values[0] is None:
        return None
    root = TreeNode(values[0])
    queue = deque([root])
    i = 1
    while queue and i < len(values):
        parent = queue.popleft()
        for side in ('left', 'right'):
            if i < len(values) and values[i] is not None:
                child = TreeNode(values[i])
                setattr(parent, side, child)
                queue.append(child)
            i += 1
    return root

def tree_values(root):
    # 层序输出并去掉末尾空节点，方便和题目示例比较。
    result, queue = [], deque([root])
    while queue:
        node = queue.popleft()
        result.append(node.val if node else None)
        if node:
            queue.extend([node.left, node.right])
    while result and result[-1] is None:
        result.pop()
    return result

def find_node(root, value):
    if root is None or root.val == value:
        return root
    return find_node(root.left, value) or find_node(root.right, value)

def inorder_values(root):
    return inorder_values(root.left) + [root.val] + inorder_values(root.right) if root else []

def checked_height(root):
    if root is None:
        return 0
    left, right = checked_height(root.left), checked_height(root.right)
    assert abs(left - right) <= 1, "生成的树不平衡"
    return 1 + max(left, right)

def next_levels(root):
    # 沿 next 指针读取每层，实际检查连接结果。
    result = []
    while root:
        row, node = [], root
        while node:
            row.append(node.val)
            node = node.next
        result.append(row)
        root = root.left
    return result

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：minDepth；按题目要求处理输入并返回结果，核心算法见方法体。
def minDepth(root):
    # 递归法
    if root is None:
        return 0                                # 终止条件
    if root.left is None and root.right is None:
        return 1                                # 叶子节点
    if root.left is None:
        return minDepth(root.right) + 1    # 只有右子树，必须走右边
    if root.right is None:
        return minDepth(root.left) + 1     # 只有左子树，必须走左边
    return min(minDepth(root.left), minDepth(root.right)) + 1  # 都存在，取较小值


if __name__ == "__main__":
    # 用例 1：输入 ({'tree': [3, 9, 20, None, None, 15, 7]},)；期望 2。
    arg0 = build_tree([3, 9, 20, None, None, 15, 7])
    result = minDepth(arg0)
    if isinstance(result, TreeNode) or result is None:
        result = tree_values(result)
    expected = 2
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ({'tree': [2, None, 3, None, 4]},)；期望 3。
    arg0 = build_tree([2, None, 3, None, 4])
    result = minDepth(arg0)
    if isinstance(result, TreeNode) or result is None:
        result = tree_values(result)
    expected = 3
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ({'tree': []},)；期望 0。
    arg0 = build_tree([])
    result = minDepth(arg0)
    if isinstance(result, TreeNode) or result is None:
        result = tree_values(result)
    expected = 0
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
// 递归法
// 方法：minDepth；按题目要求处理输入并返回结果，核心算法见方法体。
func minDepth(root *TreeNode) int {
    if root == nil {
        return 0 // 终止条件
    }
    if root.Left == nil && root.Right == nil {
        return 1 // 叶子节点
    }
    if root.Left == nil {
        return minDepth(root.Right) + 1 // 只有右子树
    }
    if root.Right == nil {
        return minDepth(root.Left) + 1 // 只有左子树
    }
    leftDepth := minDepth(root.Left)
    rightDepth := minDepth(root.Right)
    if leftDepth < rightDepth {
        return leftDepth + 1 // 都存在，取较小值
    }
    return rightDepth + 1
}

// BFS法
// 方法：minDepthBFS；按题目要求处理输入并返回结果，核心算法见方法体。
func minDepthBFS(root *TreeNode) int {
    if root == nil {
        return 0
    }
    queue := []*TreeNode{root}
    depth := 0
    for len(queue) > 0 {
        size := len(queue)
        depth++ // 进入新的一层
        for i := 0; i < size; i++ {
            node := queue[0]
            queue = queue[1:]
            if node.Left == nil && node.Right == nil {
                return depth // 第一次遇到叶子节点，直接返回
            }
            if node.Left != nil {
                queue = append(queue, node.Left)
            }
            if node.Right != nil {
                queue = append(queue, node.Right)
            }
        }
    }
    return depth
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 自定义节点和层序建树函数，nil 表示空节点。
type TreeNode struct { Val int; Left, Right, Next *TreeNode }
type Node = TreeNode
func buildTreeInput(values []interface{}) *TreeNode {
    if len(values) == 0 || values[0] == nil { return nil }
    root := &TreeNode{Val: values[0].(int)}
    q := []*TreeNode{root}; i := 1
    for len(q)>0 && i<len(values) {
        n := q[0]; q = q[1:]
        if i<len(values) && values[i]!=nil { n.Left=&TreeNode{Val:values[i].(int)}; q=append(q,n.Left) }; i++
        if i<len(values) && values[i]!=nil { n.Right=&TreeNode{Val:values[i].(int)}; q=append(q,n.Right) }; i++
    }
    return root
}
func treeValues(root *TreeNode) []interface{} {
    result := []interface{}{}; q := []*TreeNode{root}
    for len(q)>0 { n:=q[0]; q=q[1:]; if n==nil { result=append(result,nil) } else { result=append(result,n.Val); q=append(q,n.Left,n.Right) } }
    for len(result)>0 && result[len(result)-1]==nil { result=result[:len(result)-1] }
    return result
}
func findNode(root *TreeNode, val int) *TreeNode {
    if root==nil || root.Val==val { return root }
    if n:=findNode(root.Left,val); n!=nil { return n }; return findNode(root.Right,val)
}
func inorderValues(root *TreeNode) []int {
    if root==nil { return []int{} }; a:=inorderValues(root.Left); a=append(a,root.Val); return append(a,inorderValues(root.Right)...)
}
func checkedHeight(root *TreeNode) int {
    if root==nil { return 0 }; l,r:=checkedHeight(root.Left),checkedHeight(root.Right)
    if l-r>1 || r-l>1 { panic("tree is not balanced") }; if l>r {return l+1}; return r+1
}
func nextLevels(root *TreeNode) [][]int {
    result:=[][]int{}; for ;root!=nil;root=root.Left { row:=[]int{}; for n:=root;n!=nil;n=n.Next { row=append(row,n.Val) }; result=append(result,row) }; return result
}

// 普通函数和题目中的核心算法。
// 递归法
// 方法：minDepth；按题目要求处理输入并返回结果，核心算法见方法体。
func minDepth(root *TreeNode) int {
    if root == nil {
        return 0 // 终止条件
    }
    if root.Left == nil && root.Right == nil {
        return 1 // 叶子节点
    }
    if root.Left == nil {
        return minDepth(root.Right) + 1 // 只有右子树
    }
    if root.Right == nil {
        return minDepth(root.Left) + 1 // 只有左子树
    }
    leftDepth := minDepth(root.Left)
    rightDepth := minDepth(root.Right)
    if leftDepth < rightDepth {
        return leftDepth + 1 // 都存在，取较小值
    }
    return rightDepth + 1
}

// BFS法
// 方法：minDepthBFS；按题目要求处理输入并返回结果，核心算法见方法体。
func minDepthBFS(root *TreeNode) int {
    if root == nil {
        return 0
    }
    queue := []*TreeNode{root}
    depth := 0
    for len(queue) > 0 {
        size := len(queue)
        depth++ // 进入新的一层
        for i := 0; i < size; i++ {
            node := queue[0]
            queue = queue[1:]
            if node.Left == nil && node.Right == nil {
                return depth // 第一次遇到叶子节点，直接返回
            }
            if node.Left != nil {
                queue = append(queue, node.Left)
            }
            if node.Right != nil {
                queue = append(queue, node.Right)
            }
        }
    }
    return depth
}

func main() {
    // 用例 1：输入 ({'tree': [3, 9, 20, None, None, 15, 7]},)；期望 2。
    {
        arg0 := buildTreeInput([]interface{}{3, 9, 20, nil, nil, 15, 7})
        result := minDepth(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=2 实际=", string(data))
    }
    // 用例 2：输入 ({'tree': [2, None, 3, None, 4]},)；期望 3。
    {
        arg0 := buildTreeInput([]interface{}{2, nil, 3, nil, 4})
        result := minDepth(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=3 实际=", string(data))
    }
    // 用例 3：输入 ({'tree': []},)；期望 0。
    {
        arg0 := buildTreeInput([]interface{}{})
        result := minDepth(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=0 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
    TreeNode(int x) : val(x), left(NULL), right(NULL) {}
};
```

```cpp
class Solution {
public:
    // 递归法：注意处理单侧子树为空的特殊情况
    // 方法：minDepth；按题目要求处理输入并返回结果，核心算法见方法体。
    int minDepth(TreeNode* root) {
        if (root == nullptr) return 0;                 // 终止条件：空树深度为0
        if (root->left == nullptr && root->right == nullptr) return 1; // 叶子节点，深度为1
        if (root->left == nullptr) return minDepth(root->right) + 1;   // 只有右子树，必须走右边
        if (root->right == nullptr) return minDepth(root->left) + 1;   // 只有左子树，必须走左边
        return min(minDepth(root->left), minDepth(root->right)) + 1;   // 左右都存在，取较小值
    }

    // BFS法：第一次遇到叶子节点就是最小深度
    // 方法：minDepthBFS；按题目要求处理输入并返回结果，核心算法见方法体。
    int minDepthBFS(TreeNode* root) {
        if (root == nullptr) return 0;
        queue<TreeNode*> que;
        que.push(root);
        int depth = 0;
        while (!que.empty()) {
            int size = que.size();
            depth++;                             // 进入新的一层，深度加一
            for (int i = 0; i < size; i++) {
                TreeNode* node = que.front();
                que.pop();
                if (node->left == nullptr && node->right == nullptr) {
                    return depth;                 // 第一次遇到叶子节点，直接返回
                }
                if (node->left) que.push(node->left);
                if (node->right) que.push(node->right);
            }
        }
        return depth;
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

struct TreeNode {
    int val;
    TreeNode *left, *right, *next;
    TreeNode(int value = 0, TreeNode* l = nullptr, TreeNode* r = nullptr)
        : val(value), left(l), right(r), next(nullptr) {}
};
using Node = TreeNode;

// 层序建树；nullopt 表示空节点。
TreeNode* buildTreeInput(const vector<optional<int>>& values) {
    if (values.empty() || !values[0]) return nullptr;
    auto* root = new TreeNode(*values[0]);
    queue<TreeNode*> q; q.push(root);
    size_t i = 1;
    while (!q.empty() && i < values.size()) {
        auto* node = q.front(); q.pop();
        if (i < values.size() && values[i]) { node->left = new TreeNode(*values[i]); q.push(node->left); }
        ++i;
        if (i < values.size() && values[i]) { node->right = new TreeNode(*values[i]); q.push(node->right); }
        ++i;
    }
    return root;
}
vector<optional<int>> treeValues(TreeNode* root) {
    vector<optional<int>> result;
    queue<TreeNode*> q; q.push(root);
    while (!q.empty()) {
        auto* node = q.front(); q.pop();
        if (node) { result.push_back(node->val); q.push(node->left); q.push(node->right); }
        else result.push_back(nullopt);
    }
    while (!result.empty() && !result.back()) result.pop_back();
    return result;
}
TreeNode* findNode(TreeNode* root, int val) {
    if (!root || root->val == val) return root;
    auto* left = findNode(root->left, val);
    return left ? left : findNode(root->right, val);
}
vector<int> inorderValues(TreeNode* root) {
    if (!root) return {};
    auto a = inorderValues(root->left), b = inorderValues(root->right);
    a.push_back(root->val); a.insert(a.end(), b.begin(), b.end()); return a;
}
int checkedHeight(TreeNode* root) {
    if (!root) return 0;
    int l = checkedHeight(root->left), r = checkedHeight(root->right);
    if (abs(l - r) > 1) throw runtime_error("tree is not balanced");
    return 1 + max(l, r);
}
vector<vector<int>> nextLevels(TreeNode* root) {
    vector<vector<int>> result;
    for (; root; root = root->left) {
        vector<int> row;
        for (auto* n = root; n; n = n->next) row.push_back(n->val);
        result.push_back(row);
    }
    return result;
}
void show(TreeNode* root) { show(treeValues(root)); }

namespace interview {

// 普通函数与辅助函数声明。
int minDepth(TreeNode* root);
int minDepthBFS(TreeNode* root);

// 递归法：注意处理单侧子树为空的特殊情况
    // 方法：minDepth；按题目要求处理输入并返回结果，核心算法见方法体。
    int minDepth(TreeNode* root) {
        if (root == nullptr) return 0;                 // 终止条件：空树深度为0
        if (root->left == nullptr && root->right == nullptr) return 1; // 叶子节点，深度为1
        if (root->left == nullptr) return minDepth(root->right) + 1;   // 只有右子树，必须走右边
        if (root->right == nullptr) return minDepth(root->left) + 1;   // 只有左子树，必须走左边
        return min(minDepth(root->left), minDepth(root->right)) + 1;   // 左右都存在，取较小值
    }

// BFS法：第一次遇到叶子节点就是最小深度
    // 方法：minDepthBFS；按题目要求处理输入并返回结果，核心算法见方法体。
    int minDepthBFS(TreeNode* root) {
        if (root == nullptr) return 0;
        queue<TreeNode*> que;
        que.push(root);
        int depth = 0;
        while (!que.empty()) {
            int size = que.size();
            depth++;                             // 进入新的一层，深度加一
            for (int i = 0; i < size; i++) {
                TreeNode* node = que.front();
                que.pop();
                if (node->left == nullptr && node->right == nullptr) {
                    return depth;                 // 第一次遇到叶子节点，直接返回
                }
                if (node->left) que.push(node->left);
                if (node->right) que.push(node->right);
            }
        }
        return depth;
    }

}

int main() {
    // 用例 1：输入 ({'tree': [3, 9, 20, None, None, 15, 7]},)；期望 2。
    {
        auto* arg0 = buildTreeInput({3, 9, 20, nullopt, nullopt, 15, 7});
        auto result = interview::minDepth(arg0);
        cout << "用例 1: 期望=2 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ({'tree': [2, None, 3, None, 4]},)；期望 3。
    {
        auto* arg0 = buildTreeInput({2, nullopt, 3, nullopt, 4});
        auto result = interview::minDepth(arg0);
        cout << "用例 2: 期望=3 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ({'tree': []},)；期望 0。
    {
        auto* arg0 = buildTreeInput({});
        auto result = interview::minDepth(arg0);
        cout << "用例 3: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 最小深度不能简单地把最大深度代码里的 `max` 换成 `min`，因为"叶子节点"要求左右孩子都为空，单侧子树为空的时候不能把那一侧的 0 也拿去比较。
- 递归解法需要对"左空右不空""右空左不空""左右都不空""左右都空"这四种情况分别处理，漏掉中间那两种情况是最容易犯的错误。
- BFS 解法更省心：按层扩展，第一次遇到左右孩子都为空的节点，直接返回当前层数就是答案，完全不需要处理那些特判逻辑。
- 这道题算是理解"叶子节点定义"和"边界条件完整性"的经典题目，提醒你做题的时候一定要把题目定义反复确认清楚，不能想当然地照搬相似题目的模板。
