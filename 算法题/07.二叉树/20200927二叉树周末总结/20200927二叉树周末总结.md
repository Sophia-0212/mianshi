# 20200927 二叉树周末总结

递归先明确参数／返回值、终止条件、单层处理：

- 前序：先处理根，再递归孩子，适合向下传状态。
- 中序：左→根→右，BST 得到有序序列。
- 后序：先取子树结果再合并，适合高度、平衡性等问题。
- BFS：队列分层，每层开始固定队列长度，适合层序和最短深度。

遍历时间 O(n)；DFS 辅助空间 O(h)，BFS O(w)，h 为树高，w 为最大层宽，结果另计。
