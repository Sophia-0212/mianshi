# 1365. 有多少小于当前数字的数字

**题目描述**

给你一个数组 `nums`，对于其中每个元素 `nums[i]`，请你统计数组中比它小的所有数字的数目。

```
示例：
输入: nums = [8,1,2,2,3]
输出: [4,0,1,1,3]
解释:
对于 nums[0]=8，存在4个比它小的数（1,2,2,3）
对于 nums[1]=1，不存在比它小的数
对于 nums[2]=2，存在1个比它小的数（1）
对于 nums[3]=2，存在1个比它小的数（1）
对于 nums[4]=3，存在3个比它小的数（1,2,2）
```

## 思路

- 排序＋映射：排序副本，每个值首次出现的下标就是较小元素个数，映射回原顺序。时间 O(n log n)，空间 O(n)。
- 计数＋前缀和：统计值域内频次，值 `v` 的答案为小于 `v` 的频次和。时间 O(n+U)，空间 O(n+U)（含结果），U 为值域大小。
- 暴力比较：每个数与其余数比较。时间 O(n²)，辅助空间 O(1)。



## Python

### LeetCode 写法与原有示例

标准库辅助解（结合 `sorted` 排序 + 字典去重记录首次下标，`sorted` 底层是 Timsort，稳定且时间复杂度 O(n log n)）：

```python
# 依赖说明：enumerate：Python 内置函数：遍历序列时同时得到下标和值。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。

class Solution:
    # 方法：smallerNumbersThanCurrent；按题目要求处理输入并返回结果，核心算法见方法体。
    def smallerNumbersThanCurrent(self, nums: list[int]) -> list[int]:
        sorted_nums = sorted(nums)  # 升序排序，得到一份新数组
        value_to_count = {}
        for i, v in enumerate(sorted_nums):
            # setdefault 保证只在第一次出现时写入下标
            value_to_count.setdefault(v, i)
        return [value_to_count[num] for num in nums]  # 查表组装结果
```


手写实现（面试考察点，不依赖内置排序细节，强调"首次下标即答案"这一核心洞察）：

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：smallerNumbersThanCurrent；按题目要求处理输入并返回结果，核心算法见方法体。
    def smallerNumbersThanCurrent(self, nums: list[int]) -> list[int]:
        sorted_nums = sorted(nums)          # 排序后相同值相邻，且下标连续
        value_to_count = {}                 # 记录每个值第一次出现的下标
        for i in range(len(sorted_nums)):
            v = sorted_nums[i]
            if v not in value_to_count:      # 只记录第一次出现
                value_to_count[v] = i

        result = []
        for num in nums:                    # 按原数组顺序输出答案
            result.append(value_to_count[num])
        return result
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：smallerNumbersThanCurrent；按题目要求处理输入并返回结果，核心算法见方法体。
def smallerNumbersThanCurrent(nums: list[int]) -> list[int]:
    sorted_nums = sorted(nums)          # 排序后相同值相邻，且下标连续
    value_to_count = {}                 # 记录每个值第一次出现的下标
    for i in range(len(sorted_nums)):
        v = sorted_nums[i]
        if v not in value_to_count:      # 只记录第一次出现
            value_to_count[v] = i

    result = []
    for num in nums:                    # 按原数组顺序输出答案
        result.append(value_to_count[num])
    return result


if __name__ == "__main__":
    # 用例 1：输入 ([8, 1, 2, 2, 3],)；期望 [4, 0, 1, 1, 3]。
    arg0 = [8, 1, 2, 2, 3]
    result = smallerNumbersThanCurrent(arg0)
    expected = [4, 0, 1, 1, 3]
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([6, 5, 4, 8],)；期望 [2, 1, 0, 3]。
    arg0 = [6, 5, 4, 8]
    result = smallerNumbersThanCurrent(arg0)
    expected = [2, 1, 0, 3]
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([7, 7, 7],)；期望 [0, 0, 0]。
    arg0 = [7, 7, 7]
    result = smallerNumbersThanCurrent(arg0)
    expected = [0, 0, 0]
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected
```

## Go

### LeetCode 写法与原有示例

```go
// 依赖说明：Go 标准库 sort：排序切片。
import "sort"

// 方法：smallerNumbersThanCurrent；按题目要求处理输入并返回结果，核心算法见方法体。
func smallerNumbersThanCurrent(nums []int) []int {
    sortedNums := make([]int, len(nums))
    copy(sortedNums, nums)          // 复制一份用于排序，不破坏原数组
    sort.Ints(sortedNums)           // 升序排序

    valueToCount := make(map[int]int) // 值 -> 比它小的数字个数
    for i, v := range sortedNums {
        if _, ok := valueToCount[v]; !ok {
            valueToCount[v] = i // 只在第一次出现时记录下标
        }
    }

    result := make([]int, len(nums))
    for i, num := range nums {
        result[i] = valueToCount[num] // 直接查表得到答案
    }
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。；Go 标准库 sort：排序切片。
package main

import (
    "encoding/json"
    "fmt"
    "sort"
)

// 普通函数和题目中的核心算法。
// 方法：smallerNumbersThanCurrent；按题目要求处理输入并返回结果，核心算法见方法体。
func smallerNumbersThanCurrent(nums []int) []int {
    sortedNums := make([]int, len(nums))
    copy(sortedNums, nums)          // 复制一份用于排序，不破坏原数组
    sort.Ints(sortedNums)           // 升序排序

    valueToCount := make(map[int]int) // 值 -> 比它小的数字个数
    for i, v := range sortedNums {
        if _, ok := valueToCount[v]; !ok {
            valueToCount[v] = i // 只在第一次出现时记录下标
        }
    }

    result := make([]int, len(nums))
    for i, num := range nums {
        result[i] = valueToCount[num] // 直接查表得到答案
    }
    return result
}

func main() {
    // 用例 1：输入 ([8, 1, 2, 2, 3],)；期望 [4, 0, 1, 1, 3]。
    {
        arg0 := []int{8, 1, 2, 2, 3}
        result := smallerNumbersThanCurrent(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[4, 0, 1, 1, 3] 实际=", string(data))
    }
    // 用例 2：输入 ([6, 5, 4, 8],)；期望 [2, 1, 0, 3]。
    {
        arg0 := []int{6, 5, 4, 8}
        result := smallerNumbersThanCurrent(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[2, 1, 0, 3] 实际=", string(data))
    }
    // 用例 3：输入 ([7, 7, 7],)；期望 [0, 0, 0]。
    {
        arg0 := []int{7, 7, 7}
        result := smallerNumbersThanCurrent(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[0, 0, 0] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。；sort：C++ STL algorithm 提供的排序函数。；unordered_map：C++ STL 哈希映射。

class Solution {
public:
    // 方法：smallerNumbersThanCurrent；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> smallerNumbersThanCurrent(vector<int>& nums) {
        vector<int> sorted_nums = nums;                 // 复制一份用于排序，不破坏原数组
        sort(sorted_nums.begin(), sorted_nums.end());    // 升序排序

        unordered_map<int, int> value_to_count;          // 值 -> 比它小的数字个数
        for (int i = 0; i < sorted_nums.size(); i++) {
            // 只在该值第一次出现时记录下标（即为比它小的数字个数）
            if (value_to_count.find(sorted_nums[i]) == value_to_count.end()) {
                value_to_count[sorted_nums[i]] = i;
            }
        }

        vector<int> result;
        result.reserve(nums.size());
        for (int num : nums) {
            result.push_back(value_to_count[num]);       // 直接查表得到答案
        }
        return result;
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
vector<int> smallerNumbersThanCurrent(vector<int>& nums);

// 方法：smallerNumbersThanCurrent；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> smallerNumbersThanCurrent(vector<int>& nums) {
        vector<int> sorted_nums = nums;                 // 复制一份用于排序，不破坏原数组
        sort(sorted_nums.begin(), sorted_nums.end());    // 升序排序

        unordered_map<int, int> value_to_count;          // 值 -> 比它小的数字个数
        for (int i = 0; i < sorted_nums.size(); i++) {
            // 只在该值第一次出现时记录下标（即为比它小的数字个数）
            if (value_to_count.find(sorted_nums[i]) == value_to_count.end()) {
                value_to_count[sorted_nums[i]] = i;
            }
        }

        vector<int> result;
        result.reserve(nums.size());
        for (int num : nums) {
            result.push_back(value_to_count[num]);       // 直接查表得到答案
        }
        return result;
    }

}

int main() {
    // 用例 1：输入 ([8, 1, 2, 2, 3],)；期望 [4, 0, 1, 1, 3]。
    {
        vector<int> arg0 = {8, 1, 2, 2, 3};
        auto result = interview::smallerNumbersThanCurrent(arg0);
        cout << "用例 1: 期望=[4, 0, 1, 1, 3] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([6, 5, 4, 8],)；期望 [2, 1, 0, 3]。
    {
        vector<int> arg0 = {6, 5, 4, 8};
        auto result = interview::smallerNumbersThanCurrent(arg0);
        cout << "用例 2: 期望=[2, 1, 0, 3] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([7, 7, 7],)；期望 [0, 0, 0]。
    {
        vector<int> arg0 = {7, 7, 7};
        auto result = interview::smallerNumbersThanCurrent(arg0);
        cout << "用例 3: 期望=[0, 0, 0] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 核心技巧就是把"对每个元素单独统计"这件事，转化成"对每个不同的值统计一次"，利用排序之后下标跟"比它小的数字个数"天然对应的关系，把 O(n²) 优化到了 O(n log n)。
- 哈希表在这里的作用不是简单计数，而是记录**去重后每个值第一次出现的位置**，这个细节容易出错——如果不加"仅首次记录"这个判断，重复值就会被后面的下标覆盖掉，导致结果出错。
- 这题跟"计数排序 + 前缀和"的思路本质上是相通的：如果数值范围比较小，可以直接用计数数组做前缀和，省掉排序的 O(n log n) 开销，做到线性时间。
- 容易踩的坑：别在排序之后直接拿元素值当下标去输出，必须通过哈希表把排序的结果映射回原数组的每个位置，顺序不能搞乱。
