# 189. 旋转数组

**题目描述**

给定一个数组 `nums`，将数组中的元素向右旋转 `k` 个位置，其中 `k` 是非负数。要求尽量在原地完成，使用空间复杂度为 O(1) 的算法。

```
示例：
输入: nums = [1,2,3,4,5,6,7], k = 3
输出: [5,6,7,1,2,3,4]
解释: 向右旋转1步: [7,1,2,3,4,5,6]
     向右旋转2步: [6,7,1,2,3,4,5]
     向右旋转3步: [5,6,7,1,2,3,4]

输入: nums = [-1,-100,3,99], k = 2
输出: [3,99,-1,-100]
```

## 思路

- 三步反转：先令 `k %= n`，依次反转整体、前 `k` 个、后 `n-k` 个元素，将 `AB` 变为 `BA`。时间 O(n)，辅助空间 O(1)；Python 切片实现用 O(n) 空间。
- 辅助数组：将元素写到 `(i+k)%n`，再复制回原数组。时间 O(n)，空间 O(n)。
- 逐次旋转：每次右移一位，共移 `k` 次。时间 O(nk)，空间 O(1)。



## Python

### LeetCode 写法与原有示例

标准库中的切片操作可以简洁地表达旋转（虽然切片本身会产生新的临时数组，严格意义上不是O(1)空间，但作为库函数思路参考——`nums[:] = nums[-k:] + nums[:-k]` 的原理是直接拼接"后k个"和"前n-k个"两段）：

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。

class Solution:
    # 方法：rotate；按题目要求处理输入并返回结果，核心算法见方法体。
    def rotate(self, nums: list[int], k: int) -> None:
        n = len(nums)
        k %= n  # 取模避免k大于n时越界
        nums[:] = nums[-k:] + nums[:-k]  # 后k个拼接前n-k个，原地赋值回nums```


手写实现（面试考察点，三步反转法，严格 O(1) 额外空间）：

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。

class Solution:
    # 方法：rotate；按题目要求处理输入并返回结果，核心算法见方法体。
    def rotate(self, nums: list[int], k: int) -> None:
        n = len(nums)
        k %= n  # k可能大于n，取模避免无意义的多余反转

        # 方法：reverse；按题目要求处理输入并返回结果，核心算法见方法体。
        def reverse(left: int, right: int) -> None:
            while left < right:
                nums[left], nums[right] = nums[right], nums[left]
                left += 1
                right -= 1

        reverse(0, n - 1)      # 第一步：整体反转
        reverse(0, k - 1)      # 第二步：反转前k个元素
        reverse(k, n - 1)      # 第三步：反转剩余n-k个元素```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：rotate；按题目要求处理输入并返回结果，核心算法见方法体。
def rotate(nums: list[int], k: int) -> None:
    n = len(nums)
    k %= n  # k可能大于n，取模避免无意义的多余反转

    # 方法：reverse；按题目要求处理输入并返回结果，核心算法见方法体。
    def reverse(left: int, right: int) -> None:
        while left < right:
            nums[left], nums[right] = nums[right], nums[left]
            left += 1
            right -= 1

    reverse(0, n - 1)      # 第一步：整体反转
    reverse(0, k - 1)      # 第二步：反转前k个元素
    reverse(k, n - 1)      # 第三步：反转剩余n-k个元素


if __name__ == "__main__":
    # 用例 1：输入 ([1, 2, 3, 4, 5, 6, 7], 3)；期望 [5, 6, 7, 1, 2, 3, 4]。
    arg0 = [1, 2, 3, 4, 5, 6, 7]
    arg1 = 3
    result = rotate(arg0, arg1)
    result = arg0  # 原地修改题检查修改后的数据。
    expected = [5, 6, 7, 1, 2, 3, 4]
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([1, 2], 3)；期望 [2, 1]。
    arg0 = [1, 2]
    arg1 = 3
    result = rotate(arg0, arg1)
    result = arg0  # 原地修改题检查修改后的数据。
    expected = [2, 1]
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([1], 0)；期望 [1]。
    arg0 = [1]
    arg1 = 0
    result = rotate(arg0, arg1)
    result = arg0  # 原地修改题检查修改后的数据。
    expected = [1]
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
// 方法：rotate；按题目要求处理输入并返回结果，核心算法见方法体。
func rotate(nums []int, k int) {
    n := len(nums)
    k %= n // k可能大于n，取模避免越界

    reverse(nums, 0, n-1) // 第一步：整体反转
    reverse(nums, 0, k-1) // 第二步：反转前k个元素
    reverse(nums, k, n-1) // 第三步：反转剩余n-k个元素
}

// 方法：reverse；按题目要求处理输入并返回结果，核心算法见方法体。
func reverse(nums []int, left, right int) {
    for left < right {
        nums[left], nums[right] = nums[right], nums[left]
        left++
        right--
    }
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：rotate；按题目要求处理输入并返回结果，核心算法见方法体。
func rotate(nums []int, k int) {
    n := len(nums)
    k %= n // k可能大于n，取模避免越界

    reverse(nums, 0, n-1) // 第一步：整体反转
    reverse(nums, 0, k-1) // 第二步：反转前k个元素
    reverse(nums, k, n-1) // 第三步：反转剩余n-k个元素
}

// 方法：reverse；按题目要求处理输入并返回结果，核心算法见方法体。
func reverse(nums []int, left, right int) {
    for left < right {
        nums[left], nums[right] = nums[right], nums[left]
        left++
        right--
    }
}

func main() {
    // 用例 1：输入 ([1, 2, 3, 4, 5, 6, 7], 3)；期望 [5, 6, 7, 1, 2, 3, 4]。
    {
        arg0 := []int{1, 2, 3, 4, 5, 6, 7}
        arg1 := 3
        rotate(arg0, arg1)
        data, err := json.Marshal(arg0)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[5, 6, 7, 1, 2, 3, 4] 实际=", string(data))
    }
    // 用例 2：输入 ([1, 2], 3)；期望 [2, 1]。
    {
        arg0 := []int{1, 2}
        arg1 := 3
        rotate(arg0, arg1)
        data, err := json.Marshal(arg0)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[2, 1] 实际=", string(data))
    }
    // 用例 3：输入 ([1], 0)；期望 [1]。
    {
        arg0 := []int{1}
        arg1 := 0
        rotate(arg0, arg1)
        data, err := json.Marshal(arg0)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[1] 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
public:
    // 方法：rotate；按题目要求处理输入并返回结果，核心算法见方法体。
    void rotate(vector<int>& nums, int k) {
        int n = nums.size();
        k %= n;  // k可能大于n，取模避免越界（转一圈等于没转）

        reverse(nums.begin(), nums.end());        // 第一步：整体反转
        reverse(nums.begin(), nums.begin() + k);  // 第二步：反转前k个元素
        reverse(nums.begin() + k, nums.end());    // 第三步：反转剩余n-k个元素
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
void rotate(vector<int>& nums, int k);

// 方法：rotate；按题目要求处理输入并返回结果，核心算法见方法体。
    void rotate(vector<int>& nums, int k) {
        int n = nums.size();
        k %= n;  // k可能大于n，取模避免越界（转一圈等于没转）

        reverse(nums.begin(), nums.end());        // 第一步：整体反转
        reverse(nums.begin(), nums.begin() + k);  // 第二步：反转前k个元素
        reverse(nums.begin() + k, nums.end());    // 第三步：反转剩余n-k个元素
    }

}

int main() {
    // 用例 1：输入 ([1, 2, 3, 4, 5, 6, 7], 3)；期望 [5, 6, 7, 1, 2, 3, 4]。
    {
        vector<int> arg0 = {1, 2, 3, 4, 5, 6, 7};
        int arg1 = 3;
        interview::rotate(arg0, arg1);
        cout << "用例 1: 期望=[5, 6, 7, 1, 2, 3, 4] 实际=";
        show(arg0);
        cout << "\n";
    }
    // 用例 2：输入 ([1, 2], 3)；期望 [2, 1]。
    {
        vector<int> arg0 = {1, 2};
        int arg1 = 3;
        interview::rotate(arg0, arg1);
        cout << "用例 2: 期望=[2, 1] 实际=";
        show(arg0);
        cout << "\n";
    }
    // 用例 3：输入 ([1], 0)；期望 [1]。
    {
        vector<int> arg0 = {1};
        int arg1 = 0;
        interview::rotate(arg0, arg1);
        cout << "用例 3: 期望=[1] 实际=";
        show(arg0);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 三步反转法（整体反转 + 两段局部反转）是处理"循环移位"这类问题的通用技巧，不光能用在数组向右旋转上，字符串右旋（比如卡码网 55 题）也一样能用——两者底层的逻辑是完全一样的：把数组或者字符串看成两段，通过反转的组合来实现两段的位置互换，同时保持每段内部元素的顺序不变。
- 边界处理这里的关键，就是要先对 `k` 取模 `n`，因为转 n 位等于没转，你要是不取模，`k` 一旦比数组长度还大，反转区间就直接越界了。
- 对比一下"额外数组模拟"（O(n)空间）和"逐位旋转k次"（O(n*k)时间）这两种做法，三步反转法在空间和时间上都做到了最优——O(1) 空间、O(n) 时间，是这类问题的标准解法。
- 想真正理解这个技巧，关键是要想明白"反转"和"位置互换"之间的关系：两段各自反转一次、再整体反转一次（或者反过来，先整体反转再各自反转），都能实现两段位置互换、同时段内顺序不变，这个反转组合的规律是要牢牢记住的。
