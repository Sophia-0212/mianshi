# 0209. 长度最小的子数组

**题目描述**

给定一个含有 `n` 个正整数的数组和一个正整数 `target`，找出该数组中满足其和大于等于 `target` 的长度最小的**连续**子数组，并返回其长度。如果不存在符合条件的子数组，返回 0。

```
示例：
输入: target = 7, nums = [2,3,1,2,4,3,3]
输出: 2
解释: 子数组 [4,3] 是该条件下的长度最小的子数组。
```

## 思路

- 滑动窗口：正整数保证窗口和随扩张增大。右端扩张；和达到 `target` 时记录长度，并持续收缩左端。无解返回 0。时间 O(n)，空间 O(1)。
- 枚举起点并累加终点：检查所有子数组。时间 O(n²)，空间 O(1)。



## Python

### LeetCode 写法与原有示例

这道题依赖"扩张 - 收缩"的动态过程来维护窗口边界，标准库没有能直接表达这种双指针收缩逻辑的一行解，因此只给手写实现。

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；min：Python 内置函数：返回最小值。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：minSubArrayLen；按题目要求处理输入并返回结果，核心算法见方法体。
    def minSubArrayLen(self, target: int, nums: list[int]) -> int:
        n = len(nums)
        left = 0                # 窗口左边界
        window_sum = 0           # 当前窗口元素和
        min_len = float('inf')   # 记录最小窗口长度，初始设为无穷大
        for right in range(n):   # right 负责扩张窗口
            window_sum += nums[right]  # 把新元素纳入窗口
            while window_sum >= target:  # 窗口和已满足条件，尝试收缩
                min_len = min(min_len, right - left + 1)  # 更新最小长度
                window_sum -= nums[left]  # 移出左边界元素
                left += 1                  # 左边界右移
        return 0 if min_len == float('inf') else min_len  # 未找到则返回 0```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；min：Python 内置函数：返回最小值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：minSubArrayLen；按题目要求处理输入并返回结果，核心算法见方法体。
def minSubArrayLen(target: int, nums: list[int]) -> int:
    n = len(nums)
    left = 0                # 窗口左边界
    window_sum = 0           # 当前窗口元素和
    min_len = float('inf')   # 记录最小窗口长度，初始设为无穷大
    for right in range(n):   # right 负责扩张窗口
        window_sum += nums[right]  # 把新元素纳入窗口
        while window_sum >= target:  # 窗口和已满足条件，尝试收缩
            min_len = min(min_len, right - left + 1)  # 更新最小长度
            window_sum -= nums[left]  # 移出左边界元素
            left += 1                  # 左边界右移
    return 0 if min_len == float('inf') else min_len  # 未找到则返回 0


if __name__ == "__main__":
    # 用例 1：输入 (7, [2, 3, 1, 2, 4, 3])；期望 2。
    arg0 = 7
    arg1 = [2, 3, 1, 2, 4, 3]
    result = minSubArrayLen(arg0, arg1)
    expected = 2
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 (4, [1, 4, 4])；期望 1。
    arg0 = 4
    arg1 = [1, 4, 4]
    result = minSubArrayLen(arg0, arg1)
    expected = 1
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 (11, [1, 1, 1])；期望 0。
    arg0 = 11
    arg1 = [1, 1, 1]
    result = minSubArrayLen(arg0, arg1)
    expected = 0
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
// 方法：minSubArrayLen；按题目要求处理输入并返回结果，核心算法见方法体。
func minSubArrayLen(target int, nums []int) int {
    n := len(nums)
    left := 0      // 窗口左边界
    sum := 0        // 当前窗口元素和
    minLen := n + 1 // 记录最小窗口长度，用 n+1 表示"尚未找到"（不可能达到的长度）
    for right := 0; right < n; right++ { // right 扩张窗口
        sum += nums[right]
        for sum >= target { // 满足条件，尝试收缩
            if right-left+1 < minLen {
                minLen = right - left + 1 // 更新最小长度
            }
            sum -= nums[left] // 移出左边界
            left++
        }
    }
    if minLen == n+1 { // 说明循环中从未更新过，不存在满足条件的子数组
        return 0
    }
    return minLen
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：minSubArrayLen；按题目要求处理输入并返回结果，核心算法见方法体。
func minSubArrayLen(target int, nums []int) int {
    n := len(nums)
    left := 0      // 窗口左边界
    sum := 0        // 当前窗口元素和
    minLen := n + 1 // 记录最小窗口长度，用 n+1 表示"尚未找到"（不可能达到的长度）
    for right := 0; right < n; right++ { // right 扩张窗口
        sum += nums[right]
        for sum >= target { // 满足条件，尝试收缩
            if right-left+1 < minLen {
                minLen = right - left + 1 // 更新最小长度
            }
            sum -= nums[left] // 移出左边界
            left++
        }
    }
    if minLen == n+1 { // 说明循环中从未更新过，不存在满足条件的子数组
        return 0
    }
    return minLen
}

func main() {
    // 用例 1：输入 (7, [2, 3, 1, 2, 4, 3])；期望 2。
    {
        arg0 := 7
        arg1 := []int{2, 3, 1, 2, 4, 3}
        result := minSubArrayLen(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=2 实际=", string(data))
    }
    // 用例 2：输入 (4, [1, 4, 4])；期望 1。
    {
        arg0 := 4
        arg1 := []int{1, 4, 4}
        result := minSubArrayLen(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=1 实际=", string(data))
    }
    // 用例 3：输入 (11, [1, 1, 1])；期望 0。
    {
        arg0 := 11
        arg1 := []int{1, 1, 1}
        result := minSubArrayLen(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=0 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
public:
    // 方法：minSubArrayLen；按题目要求处理输入并返回结果，核心算法见方法体。
    int minSubArrayLen(int target, vector<int>& nums) {
        int n = nums.size();
        int left = 0;           // 窗口左边界
        int sum = 0;             // 当前窗口 [left, right] 的元素和
        int minLen = INT_MAX;    // 记录满足条件的最小窗口长度
        for (int right = 0; right < n; right++) { // right 负责扩张窗口
            sum += nums[right]; // 把新元素纳入窗口
            while (sum >= target) { // 窗口和已满足条件，尝试收缩左边界找更短窗口
                minLen = min(minLen, right - left + 1); // 更新最小长度
                sum -= nums[left]; // 移出左边界元素
                left++;             // 左边界右移，收缩窗口
            }
        }
        return minLen == INT_MAX ? 0 : minLen; // 没有找到满足条件的子数组则返回 0
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
int minSubArrayLen(int target, vector<int>& nums);

// 方法：minSubArrayLen；按题目要求处理输入并返回结果，核心算法见方法体。
    int minSubArrayLen(int target, vector<int>& nums) {
        int n = nums.size();
        int left = 0;           // 窗口左边界
        int sum = 0;             // 当前窗口 [left, right] 的元素和
        int minLen = INT_MAX;    // 记录满足条件的最小窗口长度
        for (int right = 0; right < n; right++) { // right 负责扩张窗口
            sum += nums[right]; // 把新元素纳入窗口
            while (sum >= target) { // 窗口和已满足条件，尝试收缩左边界找更短窗口
                minLen = min(minLen, right - left + 1); // 更新最小长度
                sum -= nums[left]; // 移出左边界元素
                left++;             // 左边界右移，收缩窗口
            }
        }
        return minLen == INT_MAX ? 0 : minLen; // 没有找到满足条件的子数组则返回 0
    }

}

int main() {
    // 用例 1：输入 (7, [2, 3, 1, 2, 4, 3])；期望 2。
    {
        int arg0 = 7;
        vector<int> arg1 = {2, 3, 1, 2, 4, 3};
        auto result = interview::minSubArrayLen(arg0, arg1);
        cout << "用例 1: 期望=2 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 (4, [1, 4, 4])；期望 1。
    {
        int arg0 = 4;
        vector<int> arg1 = {1, 4, 4};
        auto result = interview::minSubArrayLen(arg0, arg1);
        cout << "用例 2: 期望=1 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 (11, [1, 1, 1])；期望 0。
    {
        int arg0 = 11;
        vector<int> arg1 = {1, 1, 1};
        auto result = interview::minSubArrayLen(arg0, arg1);
        cout << "用例 3: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 滑动窗口能用的前提，是"窗口指标（这里指的是区间和）随边界移动是单调变化的"，这题就是靠数组元素全是正整数这个条件，才能保证右扩张和右收缩分别让 `sum` 单调增大或减小；要是数组里有负数，这种简单的双指针收缩逻辑就直接失效了。
- 外层 `for` 控制右指针扩张，内层 `while` 控制左指针收缩，看起来是两层循环嵌套，但均摊时间复杂度还是 O(n)，因为 `left` 总共移动的步数不会超过 `n`，别被"看起来像嵌套循环"这个表象带偏，误以为是 O(n²)。
- 有个容易出错的地方：更新最小长度这个动作，一定要放在 `while` 循环内部（也就是收缩之前），因为收缩前的窗口才是刚好满足条件的那个合法窗口；还有一个常见错误是漏判"根本不存在满足条件的子数组"的情况，忘了把初始设的那个极大值（`INT_MAX`/`inf`/`n+1`）转换成 0 再返回。
- 滑动窗口跟双指针说白了是同一类技巧在不同场景下的应用：双指针常用在"原地覆盖/两端收缩"这类场景，滑动窗口则专门用来"维护一段连续区间，去找满足某种和/计数条件的最优区间"。
