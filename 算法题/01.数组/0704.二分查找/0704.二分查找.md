# 0704. 二分查找

**题目描述**

给定一个 n 个元素有序的（升序）整型数组 `nums` 和一个目标值 `target`，写一个函数搜索 `nums` 中的 `target`，如果目标值存在返回下标，否则返回 -1。

```
示例：
输入: nums = [-1,0,3,5,9,12], target = 9
输出: 4
解释: 9 出现在 nums 中并且下标为 4
```

要求：数组元素不重复，且是升序排列。

## 思路

二分查找的核心思路就是维护一个区间 `[left, right]`，每次取中间那个下标 `mid`，拿 `nums[mid]` 跟 `target` 比一下：

- `nums[mid] == target`：那就找到了，直接返回 `mid`
- `nums[mid] < target`：说明目标在右半区，所以 `left = mid + 1`
- `nums[mid] > target`：说明目标在左半区，所以 `right = mid - 1`

区间的写法上有两种常见的约定方式，这里我用的是左闭右闭 `[left, right]`：

- 初始化的时候：`left = 0`，`right = nums.length - 1`
- 循环条件是 `left <= right`，为什么呢？因为 `right` 本身也是一个有效下标，相等的时候区间里还有一个元素等着判断呢
- 收缩的时候不包含 `mid` 本身，这个很好理解，因为 `mid` 已经判断过了，没必要再判断一次

时间复杂度是 O(log n)，空间复杂度是 O(1)。

## 代码

### C++

```cpp
class Solution {
public:
    int search(vector<int>& nums, int target) {
        int left = 0, right = nums.size() - 1;  // 左闭右闭区间 [left, right]
        while (left <= right) {  // 区间不为空时继续查找
            int mid = left + (right - left) / 2;  // 防止 left+right 溢出
            if (nums[mid] == target) {
                return mid;  // 命中，直接返回下标
            } else if (nums[mid] < target) {
                left = mid + 1;  // target 在右半区，收缩左边界（mid 已排除）
            } else {
                right = mid - 1;  // target 在左半区，收缩右边界（mid 已排除）
            }
        }
        return -1;  // 区间收缩为空仍未命中，说明不存在
    }
};
```

### Java

```java
class Solution {
    public int search(int[] nums, int target) {
        int left = 0, right = nums.length - 1;  // 左闭右闭区间 [left, right]
        while (left <= right) {  // 区间不为空时继续查找
            int mid = left + (right - left) / 2;  // 防止 left+right 溢出
            if (nums[mid] == target) {
                return mid;  // 命中，直接返回下标
            } else if (nums[mid] < target) {
                left = mid + 1;  // target 在右半区，收缩左边界
            } else {
                right = mid - 1;  // target 在左半区，收缩右边界
            }
        }
        return -1;  // 未命中
    }
}
```

### Python

标准库一行解（`bisect`，底层同样是二分，`bisect_left` 返回第一个 `>= target` 的插入点）：

```python
from bisect import bisect_left

class Solution:
    def search(self, nums: list[int], target: int) -> int:
        idx = bisect_left(nums, target)  # 找到 target 应插入的最左位置
        if idx < len(nums) and nums[idx] == target:  # 校验该位置确实等于 target
            return idx
        return -1
```

手写实现（面试考察点，逐行还原二分逻辑）：

```python
class Solution:
    def search(self, nums: list[int], target: int) -> int:
        left, right = 0, len(nums) - 1  # 左闭右闭区间 [left, right]
        while left <= right:  # 区间不为空时继续查找
            mid = left + (right - left) // 2  # 取中间下标
            if nums[mid] == target:
                return mid  # 命中
            elif nums[mid] < target:
                left = mid + 1  # target 在右半区
            else:
                right = mid - 1  # target 在左半区
        return -1  # 未命中
```

### Go

```go
func search(nums []int, target int) int {
    left, right := 0, len(nums)-1 // 左闭右闭区间 [left, right]
    for left <= right {           // 区间不为空时继续查找
        mid := left + (right-left)/2 // 取中间下标
        if nums[mid] == target {
            return mid // 命中
        } else if nums[mid] < target {
            left = mid + 1 // target 在右半区
        } else {
            right = mid - 1 // target 在左半区
        }
    }
    return -1 // 未命中
}
```

## 总结

- 二分查找的关键就在于区间边界怎么定义——是左闭右闭还是左闭右开，这个一旦定下来，就必须保证循环条件、`mid` 的取值范围、边界更新方式这三者是自洽的，不能中间随便换套路。
- 这题数组没有重复元素是二分查找能直接用的前提，如果有重复元素、还要找某个目标的边界，那就得用变体了（比如 `bisect_left`/`bisect_right` 之间的差异，处理方式就不一样）。
