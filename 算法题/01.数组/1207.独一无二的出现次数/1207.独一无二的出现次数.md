# 1207. 独一无二的出现次数

**题目描述**

给你一个整数数组 `arr`，请你判断数组中每个数的出现次数是否都是独一无二的（即出现次数不能重复）。如果是，返回 `true`；否则返回 `false`。

```
示例：
输入: arr = [1,2,2,1,1,3]
输出: true
解释: 1出现3次，2出现2次，3出现1次，次数3、2、1互不相同

输入: arr = [1,2]
输出: false
解释: 1出现1次，2也出现1次，出现次数1重复了

输入: arr = [-3,0,1,-3,1,1,1,-3,10,0]
输出: true
```

## 思路

哈希表统计各值的频次，再用集合检查频次是否重复；集合大小等于不同值个数即成立。

平均时间 O(n)，空间 O(n)。



## Python

### LeetCode 写法与原有示例

标准库辅助解（`collections.Counter` 直接完成计数，底层是哈希表的封装，`Counter.values()` 拿到所有出现次数）：

```python
from collections import Counter

# 依赖说明：Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；len：Python 内置函数：返回容器中元素的数量。

class Solution:
    # 方法：uniqueOccurrences；按题目要求处理输入并返回结果，核心算法见方法体。
    def uniqueOccurrences(self, arr: list[int]) -> bool:
        counts = Counter(arr)              # 统计每个值出现的次数
        occurrence_values = counts.values() # 拿到所有次数（可能重复）
        # 转成集合去重后长度不变，说明次数互不相同
        return len(set(occurrence_values)) == len(occurrence_values)```


手写实现（面试考察点，不依赖 `Counter`，手动维护哈希表和集合）：

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。

class Solution:
    # 方法：uniqueOccurrences；按题目要求处理输入并返回结果，核心算法见方法体。
    def uniqueOccurrences(self, arr: list[int]) -> bool:
        count_map = {}                      # 值 -> 出现次数
        for num in arr:
            count_map[num] = count_map.get(num, 0) + 1  # 统计出现次数

        occurrence_set = set()              # 用于去重判断次数是否重复
        for count in count_map.values():
            occurrence_set.add(count)

        # 集合大小等于不同数值个数，说明所有次数互不相同
        return len(occurrence_set) == len(count_map)```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：uniqueOccurrences；按题目要求处理输入并返回结果，核心算法见方法体。
def uniqueOccurrences(arr: list[int]) -> bool:
    count_map = {}                      # 值 -> 出现次数
    for num in arr:
        count_map[num] = count_map.get(num, 0) + 1  # 统计出现次数

    occurrence_set = set()              # 用于去重判断次数是否重复
    for count in count_map.values():
        occurrence_set.add(count)

    # 集合大小等于不同数值个数，说明所有次数互不相同
    return len(occurrence_set) == len(count_map)


if __name__ == "__main__":
    # 用例 1：输入 ([1, 2, 2, 1, 1, 3],)；期望 True。
    arg0 = [1, 2, 2, 1, 1, 3]
    result = uniqueOccurrences(arg0)
    expected = True
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([1, 2],)；期望 False。
    arg0 = [1, 2]
    result = uniqueOccurrences(arg0)
    expected = False
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([5, 5, 5],)；期望 True。
    arg0 = [5, 5, 5]
    result = uniqueOccurrences(arg0)
    expected = True
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
// 方法：uniqueOccurrences；按题目要求处理输入并返回结果，核心算法见方法体。
func uniqueOccurrences(arr []int) bool {
    countMap := make(map[int]int) // 值 -> 出现次数
    for _, num := range arr {
        countMap[num]++ // 统计每个值出现的次数
    }

    occurrenceSet := make(map[int]bool) // 用哈希集合模拟去重
    for _, count := range countMap {
        occurrenceSet[count] = true // 把出现次数放入集合
    }

    // 集合大小等于不同数值个数，说明次数互不相同
    return len(occurrenceSet) == len(countMap)
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：uniqueOccurrences；按题目要求处理输入并返回结果，核心算法见方法体。
func uniqueOccurrences(arr []int) bool {
    countMap := make(map[int]int) // 值 -> 出现次数
    for _, num := range arr {
        countMap[num]++ // 统计每个值出现的次数
    }

    occurrenceSet := make(map[int]bool) // 用哈希集合模拟去重
    for _, count := range countMap {
        occurrenceSet[count] = true // 把出现次数放入集合
    }

    // 集合大小等于不同数值个数，说明次数互不相同
    return len(occurrenceSet) == len(countMap)
}

func main() {
    // 用例 1：输入 ([1, 2, 2, 1, 1, 3],)；期望 True。
    {
        arg0 := []int{1, 2, 2, 1, 1, 3}
        result := uniqueOccurrences(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=true 实际=", string(data))
    }
    // 用例 2：输入 ([1, 2],)；期望 False。
    {
        arg0 := []int{1, 2}
        result := uniqueOccurrences(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=false 实际=", string(data))
    }
    // 用例 3：输入 ([5, 5, 5],)；期望 True。
    {
        arg0 := []int{5, 5, 5}
        result := uniqueOccurrences(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=true 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
public:
    // 方法：uniqueOccurrences；按题目要求处理输入并返回结果，核心算法见方法体。
    bool uniqueOccurrences(vector<int>& arr) {
        unordered_map<int, int> count_map;         // 值 -> 出现次数
        for (int num : arr) {
            count_map[num]++;                       // 统计每个值出现的次数
        }

        unordered_set<int> occurrence_set;          // 用于去重判断次数是否重复
        for (auto& [value, count] : count_map) {
            occurrence_set.insert(count);           // 把出现次数放入集合
        }

        // 如果集合大小等于不同数值的个数，说明所有次数互不相同
        return occurrence_set.size() == count_map.size();
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
bool uniqueOccurrences(vector<int>& arr);

// 方法：uniqueOccurrences；按题目要求处理输入并返回结果，核心算法见方法体。
    bool uniqueOccurrences(vector<int>& arr) {
        unordered_map<int, int> count_map;         // 值 -> 出现次数
        for (int num : arr) {
            count_map[num]++;                       // 统计每个值出现的次数
        }

        unordered_set<int> occurrence_set;          // 用于去重判断次数是否重复
        for (auto& [value, count] : count_map) {
            occurrence_set.insert(count);           // 把出现次数放入集合
        }

        // 如果集合大小等于不同数值的个数，说明所有次数互不相同
        return occurrence_set.size() == count_map.size();
    }

}

int main() {
    // 用例 1：输入 ([1, 2, 2, 1, 1, 3],)；期望 True。
    {
        vector<int> arg0 = {1, 2, 2, 1, 1, 3};
        auto result = interview::uniqueOccurrences(arg0);
        cout << "用例 1: 期望=true 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([1, 2],)；期望 False。
    {
        vector<int> arg0 = {1, 2};
        auto result = interview::uniqueOccurrences(arg0);
        cout << "用例 2: 期望=false 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([5, 5, 5],)；期望 True。
    {
        vector<int> arg0 = {5, 5, 5};
        auto result = interview::uniqueOccurrences(arg0);
        cout << "用例 3: 期望=true 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 这题说白了就是"两层哈希"思路的典型应用：第一层哈希表统计每个数值的出现次数，第二层用集合对这些次数做去重检测，这种"先统计、再对统计结果去重"的模式，在很多计数类问题里都挺常见的。
- 用集合大小和原数据个数做比较来判断有没有重复，是个挺通用的技巧，比手动排序之后再比较相邻元素要简洁得多，代码可读性也更高。
- 容易搞混的地方是判断的对象是什么——这题判断的是"出现次数"有没有重复，而不是"数值"本身有没有重复，读题的时候一定要看清楚问的到底是次数还是值。
- 跟其他哈希计数题比起来，比如判断两个字符串是不是字母异位词、找出出现次数最多的元素，这题特殊的地方就在于需要对"统计结果"再做一次去重判断，算是哈希思路的一次组合应用。
