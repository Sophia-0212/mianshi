# 922. 按奇偶排序数组II

**题目描述**

给定一个非负整数数组 `nums`，其中一半整数是奇数，一半整数是偶数。对数组进行排序，使得当 `nums[i]` 为奇数时，`i` 也是奇数；当 `nums[i]` 为偶数时，`i` 也是偶数。返回任意满足上述条件的数组即可。

```
示例：
输入: nums = [4,2,5,7]
输出: [4,5,2,7]
解释: [4,7,2,5]、[2,5,4,7]等也是可行的答案

输入: nums = [2,3]
输出: [2,3]
```

## 思路

- 双指针：分别扫描奇、偶下标，找到奇下标上的偶数与偶下标上的奇数，交换。两指针每次前进 2。时间 O(n)，空间 O(1)。
- 辅助数组：奇数写奇下标，偶数写偶下标。时间 O(n)，空间 O(n)。



## Python

### LeetCode 写法与原有示例

本题没有直接对应的标准库一行原地解法（若不要求原地，可以用列表推导分别取出奇偶数再交替拼接，但这属于额外空间的做法而非库函数捷径），因此只给出双指针原地交换的手写实现：

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：sortArrayByParityII；按题目要求处理输入并返回结果，核心算法见方法体。
    def sortArrayByParityII(self, nums: list[int]) -> list[int]:
        n = len(nums)
        odd = 1  # 下一个需要修正的奇数下标
        for even in range(0, n, 2):  # even遍历所有偶数下标
            if nums[even] % 2 != 0:  # 偶数下标处是奇数值，放错了
                while odd < n and nums[odd] % 2 != 0:
                    odd += 2  # 该位置本身合法，跳过
                nums[even], nums[odd] = nums[odd], nums[even]  # 交换纠正
        return nums
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。；all：Python 内置函数：所有元素为真时返回 True。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：sortArrayByParityII；按题目要求处理输入并返回结果，核心算法见方法体。
def sortArrayByParityII(nums: list[int]) -> list[int]:
    n = len(nums)
    odd = 1  # 下一个需要修正的奇数下标
    for even in range(0, n, 2):  # even遍历所有偶数下标
        if nums[even] % 2 != 0:  # 偶数下标处是奇数值，放错了
            while odd < n and nums[odd] % 2 != 0:
                odd += 2  # 该位置本身合法，跳过
            nums[even], nums[odd] = nums[odd], nums[even]  # 交换纠正
    return nums


if __name__ == "__main__":
    # 用例 1：输入 ([4, 2, 5, 7],)；期望 [4, 5, 2, 7]。
    arg0 = [4, 2, 5, 7]
    result = sortArrayByParityII(arg0)
    expected = [4, 5, 2, 7]
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result) == sorted(expected) and all(value % 2 == index % 2 for index, value in enumerate(result))

    # 用例 2：输入 ([2, 3],)；期望 [2, 3]。
    arg0 = [2, 3]
    result = sortArrayByParityII(arg0)
    expected = [2, 3]
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result) == sorted(expected) and all(value % 2 == index % 2 for index, value in enumerate(result))

    # 用例 3：输入 ([3, 2],)；期望 [2, 3]。
    arg0 = [3, 2]
    result = sortArrayByParityII(arg0)
    expected = [2, 3]
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result) == sorted(expected) and all(value % 2 == index % 2 for index, value in enumerate(result))
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：sortArrayByParityII；按题目要求处理输入并返回结果，核心算法见方法体。
func sortArrayByParityII(nums []int) []int {
    n := len(nums)
    odd := 1 // 下一个需要修正的奇数下标
    for even := 0; even < n; even += 2 { // even遍历所有偶数下标
        if nums[even]%2 != 0 { // 偶数下标处是奇数值，放错了
            for odd < n && nums[odd]%2 != 0 {
                odd += 2 // 该位置本身合法，跳过
            }
            nums[even], nums[odd] = nums[odd], nums[even] // 交换纠正
        }
    }
    return nums
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：sortArrayByParityII；按题目要求处理输入并返回结果，核心算法见方法体。
func sortArrayByParityII(nums []int) []int {
    n := len(nums)
    odd := 1 // 下一个需要修正的奇数下标
    for even := 0; even < n; even += 2 { // even遍历所有偶数下标
        if nums[even]%2 != 0 { // 偶数下标处是奇数值，放错了
            for odd < n && nums[odd]%2 != 0 {
                odd += 2 // 该位置本身合法，跳过
            }
            nums[even], nums[odd] = nums[odd], nums[even] // 交换纠正
        }
    }
    return nums
}

func main() {
    // 用例 1：输入 ([4, 2, 5, 7],)；期望 [4, 5, 2, 7]。
    {
        arg0 := []int{4, 2, 5, 7}
        result := sortArrayByParityII(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[4, 5, 2, 7] 实际=", string(data))
    }
    // 用例 2：输入 ([2, 3],)；期望 [2, 3]。
    {
        arg0 := []int{2, 3}
        result := sortArrayByParityII(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[2, 3] 实际=", string(data))
    }
    // 用例 3：输入 ([3, 2],)；期望 [2, 3]。
    {
        arg0 := []int{3, 2}
        result := sortArrayByParityII(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[2, 3] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。

class Solution {
public:
    // 方法：sortArrayByParityII；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> sortArrayByParityII(vector<int>& nums) {
        int n = nums.size();
        int odd = 1;  // 下一个需要修正的奇数下标（值应为奇数）
        for (int even = 0; even < n; even += 2) {  // even遍历所有偶数下标
            if (nums[even] % 2 != 0) {  // 偶数下标处的值是奇数，说明放错了
                // 找到一个值为偶数的奇数下标进行交换
                while (odd < n && nums[odd] % 2 != 0) {
                    odd += 2;  // 该奇数下标本身值就是奇数，合法，跳过
                }
                swap(nums[even], nums[odd]);  // 交换后两边都被纠正
            }
        }
        return nums;
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
vector<int> sortArrayByParityII(vector<int>& nums);

// 方法：sortArrayByParityII；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> sortArrayByParityII(vector<int>& nums) {
        int n = nums.size();
        int odd = 1;  // 下一个需要修正的奇数下标（值应为奇数）
        for (int even = 0; even < n; even += 2) {  // even遍历所有偶数下标
            if (nums[even] % 2 != 0) {  // 偶数下标处的值是奇数，说明放错了
                // 找到一个值为偶数的奇数下标进行交换
                while (odd < n && nums[odd] % 2 != 0) {
                    odd += 2;  // 该奇数下标本身值就是奇数，合法，跳过
                }
                swap(nums[even], nums[odd]);  // 交换后两边都被纠正
            }
        }
        return nums;
    }

}

int main() {
    // 用例 1：输入 ([4, 2, 5, 7],)；期望 [4, 5, 2, 7]。
    {
        vector<int> arg0 = {4, 2, 5, 7};
        auto result = interview::sortArrayByParityII(arg0);
        cout << "用例 1: 期望=[4, 5, 2, 7] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([2, 3],)；期望 [2, 3]。
    {
        vector<int> arg0 = {2, 3};
        auto result = interview::sortArrayByParityII(arg0);
        cout << "用例 2: 期望=[2, 3] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([3, 2],)；期望 [2, 3]。
    {
        vector<int> arg0 = {3, 2};
        auto result = interview::sortArrayByParityII(arg0);
        cout << "用例 3: 期望=[2, 3] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 这题的双指针跟别的题不太一样，不是同向推进也不是相向而行，而是**两个指针各自负责一条"轨道"**——偶数下标轨道和奇数下标轨道，只有当某条轨道上冒出个"放错位置"的值时，才会触发跨轨道的交换，这算是双指针技巧的另一种变体玩法。
- 交换操作巧妙的地方就在于"一次交换同时纠正两个错误"：偶数下标上的奇数值和奇数下标上的偶数值正好互补，换一下两个位置就都合法了，不需要额外的中间存储，挺省事的。
- 为什么不怕换不到人？就是因为题目保证了奇偶数量各占一半，这个条件保证了交换对象必然存在，是整个算法能正确终止的前提；如果数量不对等，这套双指针策略就玩不转了。
- 跟移动零、移除元素这些双指针题比起来，这题特殊的地方就是这个"双轨道"结构，但说白了本质上还是在利用问题本身的结构性质——也就是奇偶位置的对应关系——来避免用额外空间，这是原地算法设计里挺常见的一个思路。
