# 724. 寻找数组的中心索引

**题目描述**

给你一个整数数组 `nums`，请计算数组的中心索引。数组中心索引是数组的一个下标，其左侧所有元素相加的和等于右侧所有元素相加的和。如果数组不存在中心索引，返回 -1。如果数组有多个中心索引，应返回最靠近左边的那一个。

注意：中心索引左侧或右侧不存在元素时，视为和为 0。

```
示例：
输入: nums = [1,7,3,6,5,6]
输出: 3
解释: 中心索引是3，左侧数之和 (1+7+3=11)，右侧数之和 (5+6=11)，二者相等

输入: nums = [1,2,3]
输出: -1
解释: 不存在满足条件的中心索引

输入: nums = [2,1,-1]
输出: 0
解释: 中心索引是0，左侧和为0（不存在元素），右侧数之和 (1+-1=0)
```

## 思路

- 前缀和：先求总和 `total`，从左到右维护 `leftSum`。若 `leftSum == total-leftSum-nums[i]`，返回 `i`；否则累加当前值。无解返回 -1。时间 O(n)，空间 O(1)。
- 暴力：逐个位置重新求左右和。时间 O(n²)，空间 O(1)。



## Python

### LeetCode 写法与原有示例

标准库中没有直接针对"中心索引"的一行解法，但可以借助 `itertools.accumulate` 计算前缀和数组来简化逻辑（`accumulate` 是标准库提供的累加迭代器，效果等价于手写前缀和数组）：

```python
from itertools import accumulate

# 依赖说明：Python 标准库 itertools：提供高效的迭代器工具。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。；sum：Python 内置函数：计算可迭代对象元素的总和。

# 依赖说明：Python 标准库 itertools：提供高效的迭代器工具。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。

class Solution:
    # 方法：pivotIndex；按题目要求处理输入并返回结果，核心算法见方法体。
    def pivotIndex(self, nums: list[int]) -> int:
        total = sum(nums)
        left_sum = 0
        for i, num in enumerate(nums):
            if left_sum == total - left_sum - num:  # 右侧和 = 总和 - 左侧和 - 当前值
                return i
            left_sum += num
        return -1
```


（说明：这里没有强行套用 `accumulate` 构造完整前缀和数组，因为逐个比较仍需要边遍历边判断，用滚动变量 `left_sum` 已经是最简洁直接的写法，属于手写和库函数思路一致的情况。）

手写实现（面试考察点，强调用滚动变量代替前缀和数组，节省空间）：

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；sum：Python 内置函数：计算可迭代对象元素的总和。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：pivotIndex；按题目要求处理输入并返回结果，核心算法见方法体。
    def pivotIndex(self, nums: list[int]) -> int:
        total = sum(nums)  # 数组总和
        left_sum = 0        # 当前下标左侧的累加和
        for i in range(len(nums)):
            right_sum = total - left_sum - nums[i]  # 右侧和通过总和推导
            if left_sum == right_sum:
                return i  # 找到最靠左的中心索引
            left_sum += nums[i]  # 更新左侧和
        return -1  # 不存在中心索引
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；sum：Python 内置函数：计算可迭代对象元素的总和。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：pivotIndex；按题目要求处理输入并返回结果，核心算法见方法体。
def pivotIndex(nums: list[int]) -> int:
    total = sum(nums)  # 数组总和
    left_sum = 0        # 当前下标左侧的累加和
    for i in range(len(nums)):
        right_sum = total - left_sum - nums[i]  # 右侧和通过总和推导
        if left_sum == right_sum:
            return i  # 找到最靠左的中心索引
        left_sum += nums[i]  # 更新左侧和
    return -1  # 不存在中心索引


if __name__ == "__main__":
    # 用例 1：输入 ([1, 7, 3, 6, 5, 6],)；期望 3。
    arg0 = [1, 7, 3, 6, 5, 6]
    result = pivotIndex(arg0)
    expected = 3
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([1, 2, 3],)；期望 -1。
    arg0 = [1, 2, 3]
    result = pivotIndex(arg0)
    expected = -1
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([2, 1, -1],)；期望 0。
    arg0 = [2, 1, -1]
    result = pivotIndex(arg0)
    expected = 0
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：pivotIndex；按题目要求处理输入并返回结果，核心算法见方法体。
func pivotIndex(nums []int) int {
    total := 0
    for _, num := range nums {
        total += num // 先求出数组总和
    }

    leftSum := 0 // 维护当前下标左侧的累加和
    for i, num := range nums {
        rightSum := total - leftSum - num // 右侧和 = 总和 - 左侧和 - 当前值
        if leftSum == rightSum {
            return i // 最靠左的中心索引
        }
        leftSum += num // 更新左侧和
    }
    return -1 // 不存在中心索引
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：pivotIndex；按题目要求处理输入并返回结果，核心算法见方法体。
func pivotIndex(nums []int) int {
    total := 0
    for _, num := range nums {
        total += num // 先求出数组总和
    }

    leftSum := 0 // 维护当前下标左侧的累加和
    for i, num := range nums {
        rightSum := total - leftSum - num // 右侧和 = 总和 - 左侧和 - 当前值
        if leftSum == rightSum {
            return i // 最靠左的中心索引
        }
        leftSum += num // 更新左侧和
    }
    return -1 // 不存在中心索引
}

func main() {
    // 用例 1：输入 ([1, 7, 3, 6, 5, 6],)；期望 3。
    {
        arg0 := []int{1, 7, 3, 6, 5, 6}
        result := pivotIndex(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=3 实际=", string(data))
    }
    // 用例 2：输入 ([1, 2, 3],)；期望 -1。
    {
        arg0 := []int{1, 2, 3}
        result := pivotIndex(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=-1 实际=", string(data))
    }
    // 用例 3：输入 ([2, 1, -1],)；期望 0。
    {
        arg0 := []int{2, 1, -1}
        result := pivotIndex(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=0 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。

class Solution {
public:
    // 方法：pivotIndex；按题目要求处理输入并返回结果，核心算法见方法体。
    int pivotIndex(vector<int>& nums) {
        int total = 0;
        for (int num : nums) {
            total += num;  // 先求出数组总和
        }

        int left_sum = 0;  // 维护当前下标左侧的累加和
        for (int i = 0; i < nums.size(); i++) {
            int right_sum = total - left_sum - nums[i];  // 右侧和 = 总和 - 左侧和 - 当前值
            if (left_sum == right_sum) {
                return i;  // 找到最靠左的中心索引，立即返回
            }
            left_sum += nums[i];  // 更新左侧和，为下一个下标做准备
        }
        return -1;  // 遍历完仍未找到，不存在中心索引
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
int pivotIndex(vector<int>& nums);

// 方法：pivotIndex；按题目要求处理输入并返回结果，核心算法见方法体。
    int pivotIndex(vector<int>& nums) {
        int total = 0;
        for (int num : nums) {
            total += num;  // 先求出数组总和
        }

        int left_sum = 0;  // 维护当前下标左侧的累加和
        for (int i = 0; i < nums.size(); i++) {
            int right_sum = total - left_sum - nums[i];  // 右侧和 = 总和 - 左侧和 - 当前值
            if (left_sum == right_sum) {
                return i;  // 找到最靠左的中心索引，立即返回
            }
            left_sum += nums[i];  // 更新左侧和，为下一个下标做准备
        }
        return -1;  // 遍历完仍未找到，不存在中心索引
    }

}

int main() {
    // 用例 1：输入 ([1, 7, 3, 6, 5, 6],)；期望 3。
    {
        vector<int> arg0 = {1, 7, 3, 6, 5, 6};
        auto result = interview::pivotIndex(arg0);
        cout << "用例 1: 期望=3 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([1, 2, 3],)；期望 -1。
    {
        vector<int> arg0 = {1, 2, 3};
        auto result = interview::pivotIndex(arg0);
        cout << "用例 2: 期望=-1 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([2, 1, -1],)；期望 0。
    {
        vector<int> arg0 = {2, 1, -1};
        auto result = interview::pivotIndex(arg0);
        cout << "用例 3: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 前缀和的核心价值就在于，把"每次都重新求和"这种重复计算，换成"维护一个滚动累加变量，边遍历边更新"，这样就能把时间复杂度从 O(n²) 降到 O(n)。
- 这题有个挺巧妙的地方，就是不需要真的去维护"右侧和"这个变量，而是直接用 `总和 - 左侧和 - 当前值` 推出来，省掉了一次独立的累加逻辑，这就是"用整体减去部分求剩余部分"这个思路的典型应用。
- 题目里说"中心索引两侧没有元素的话视为和为0"，这个特殊说明其实在滚动变量的初始值（就是0）和总和减法公式里都能自然满足，不需要额外写分支去判断。
- 前缀和这个技巧还广泛用在区间求和、子数组和为定值这些问题上，这题算是前缀和思想里最基础的形式，把它吃透了，对理解更复杂的前缀和变体（比如前缀和加哈希表去求子数组和的问题）会很有帮助。
