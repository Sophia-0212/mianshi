# 0205. 同构字符串

**题目描述**

给定两个字符串 `s` 和 `t`，判断它们是否是同构的。如果 `s` 中的字符可以按某种映射关系替换得到 `t`，那么这两个字符串是同构的。

映射关系必须满足：`s` 中每个字符都映射到 `t` 中唯一的字符，同时不同字符不能映射到同一个字符（即映射是一一对应的双射关系），但一个字符可以映射到它自己。

```
示例 1：
输入: s = "egg", t = "add"
输出: true
解释: e->a, g->d

示例 2：
输入: s = "foo", t = "bar"
输出: false
解释: o 需要同时映射到 a 和 r，不满足单一映射

示例 3：
输入: s = "paper", t = "title"
输出: true
```

## 思路

- 双向哈希映射：逐字符检查 `s→t`、`t→s` 均一致，防止不同字符映射到同一字符。平均时间 O(n)，空间 O(k)，k 为字符集大小。
- 集合：检查字符对数、s 不同字符数、t 不同字符数三者相等，保证一一对应。平均时间 O(n)，空间 O(n)。

长度不同直接失败。



## Python

### LeetCode 写法与原有示例

这里说一下标准库一行解的情况：这道题没有能直接对应的库函数一步搞定"双向唯一映射"的判断，因为它本质上是需要同时校验两个方向约束的定制逻辑，`zip` 顶多帮你并行遍历一下，核心的判断逻辑还是得手写。

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；zip：Python 内置函数：按位置把多个可迭代对象组合成元组序列。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。

class Solution:
    # 方法：isIsomorphic；按题目要求处理输入并返回结果，核心算法见方法体。
    def isIsomorphic(self, s: str, t: str) -> bool:
        # 借助 zip 并行遍历，用集合大小间接验证双射：
        # 如果 (s[i], t[i]) 组成的配对集合的大小，
        # 等于 s 去重后的字符数，也等于 t 去重后的字符数，
        # 就说明每个 s 字符和每个 t 字符都只参与了一组配对（双射成立）
        if len(s) != len(t):
            return False
        pairs = set(zip(s, t))
        return len(pairs) == len(set(s)) == len(set(t))```


原理说一下：`zip(s, t)` 会把两个字符串按位置配成 `(s[i], t[i])` 这样的元组序列，放进集合里就自动去重了。你想，如果双射关系是合法的，那每一对 `(a, b)` 就只会以固定的搭配出现，配对集合的大小应该恰好等于 `s` 里不同字符的个数（这个数也应该等于 `t` 里不同字符的个数）。但如果某个字符对应了不止一种搭配——比如说 `a` 既配 `x` 又配 `y`，这就说明映射不是唯一的了——配对集合的大小就会大于去重后的字符数，等式自然就不成立了。这个写法确实简洁，但可读性不如手写版本直观，面试的时候我更建议你能讲清楚双向哈希表逐字符校验的手写实现，这样面试官更容易看出你真正理解了原理。

手写实现（面试考察点：双向哈希表逐位校验，你要能清楚讲出"为什么必须两个方向都检查"）：

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；zip：Python 内置函数：按位置把多个可迭代对象组合成元组序列。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。

class Solution:
    # 方法：isIsomorphic；按题目要求处理输入并返回结果，核心算法见方法体。
    def isIsomorphic(self, s: str, t: str) -> bool:
        if len(s) != len(t):
            return False
        map_st, map_ts = {}, {}  # 双向哈希表
        for a, b in zip(s, t):
            # s->t 方向：a 已经映射过，但映射的不是当前的 b
            if a in map_st and map_st[a] != b:
                return False
            # t->s 方向：b 已经被映射过，但映射来源不是当前的 a
            if b in map_ts and map_ts[b] != a:
                return False
            map_st[a] = b
            map_ts[b] = a
        return True```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；zip：Python 内置函数：按位置把多个可迭代对象组合成元组序列。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：isIsomorphic；按题目要求处理输入并返回结果，核心算法见方法体。
def isIsomorphic(s: str, t: str) -> bool:
    if len(s) != len(t):
        return False
    map_st, map_ts = {}, {}  # 双向哈希表
    for a, b in zip(s, t):
        # s->t 方向：a 已经映射过，但映射的不是当前的 b
        if a in map_st and map_st[a] != b:
            return False
        # t->s 方向：b 已经被映射过，但映射来源不是当前的 a
        if b in map_ts and map_ts[b] != a:
            return False
        map_st[a] = b
        map_ts[b] = a
    return True


if __name__ == "__main__":
    # 用例 1：输入 ('egg', 'add')；期望 True。
    arg0 = 'egg'
    arg1 = 'add'
    result = isIsomorphic(arg0, arg1)
    expected = True
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ('foo', 'bar')；期望 False。
    arg0 = 'foo'
    arg1 = 'bar'
    result = isIsomorphic(arg0, arg1)
    expected = False
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ('paper', 'title')；期望 True。
    arg0 = 'paper'
    arg1 = 'title'
    result = isIsomorphic(arg0, arg1)
    expected = True
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
// 方法：isIsomorphic；按题目要求处理输入并返回结果，核心算法见方法体。
func isIsomorphic(s string, t string) bool {
    if len(s) != len(t) {
        return false // 长度不同直接排除
    }
    mapST := make(map[byte]byte) // s -> t 映射
    mapTS := make(map[byte]byte) // t -> s 映射
    for i := 0; i < len(s); i++ {
        a, b := s[i], t[i]
        if v, ok := mapST[a]; ok && v != b {
            return false // a 之前映射到别的字符，冲突
        }
        if v, ok := mapTS[b]; ok && v != a {
            return false // b 之前被别的字符映射，冲突
        }
        mapST[a] = b
        mapTS[b] = a
    }
    return true
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：isIsomorphic；按题目要求处理输入并返回结果，核心算法见方法体。
func isIsomorphic(s string, t string) bool {
    if len(s) != len(t) {
        return false // 长度不同直接排除
    }
    mapST := make(map[byte]byte) // s -> t 映射
    mapTS := make(map[byte]byte) // t -> s 映射
    for i := 0; i < len(s); i++ {
        a, b := s[i], t[i]
        if v, ok := mapST[a]; ok && v != b {
            return false // a 之前映射到别的字符，冲突
        }
        if v, ok := mapTS[b]; ok && v != a {
            return false // b 之前被别的字符映射，冲突
        }
        mapST[a] = b
        mapTS[b] = a
    }
    return true
}

func main() {
    // 用例 1：输入 ('egg', 'add')；期望 True。
    {
        arg0 := "egg"
        arg1 := "add"
        result := isIsomorphic(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=true 实际=", string(data))
    }
    // 用例 2：输入 ('foo', 'bar')；期望 False。
    {
        arg0 := "foo"
        arg1 := "bar"
        result := isIsomorphic(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=false 实际=", string(data))
    }
    // 用例 3：输入 ('paper', 'title')；期望 True。
    {
        arg0 := "paper"
        arg1 := "title"
        result := isIsomorphic(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=true 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
public:
    // 方法：isIsomorphic；按题目要求处理输入并返回结果，核心算法见方法体。
    bool isIsomorphic(string s, string t) {
        if (s.size() != t.size()) return false;  // 长度不同不可能同构
        unordered_map<char, char> mapST, mapTS;   // 双向哈希表
        for (int i = 0; i < s.size(); i++) {
            char a = s[i], b = t[i];
            // 检查 s->t 方向：a 是否已经映射到别的字符
            if (mapST.count(a) && mapST[a] != b) return false;
            // 检查 t->s 方向：b 是否已经被别的字符映射过
            if (mapTS.count(b) && mapTS[b] != a) return false;
            mapST[a] = b;  // 记录本次映射（重复赋值也没影响）
            mapTS[b] = a;
        }
        return true;  // 全程无冲突，是合法双射
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
bool isIsomorphic(string s, string t);

// 方法：isIsomorphic；按题目要求处理输入并返回结果，核心算法见方法体。
    bool isIsomorphic(string s, string t) {
        if (s.size() != t.size()) return false;  // 长度不同不可能同构
        unordered_map<char, char> mapST, mapTS;   // 双向哈希表
        for (int i = 0; i < s.size(); i++) {
            char a = s[i], b = t[i];
            // 检查 s->t 方向：a 是否已经映射到别的字符
            if (mapST.count(a) && mapST[a] != b) return false;
            // 检查 t->s 方向：b 是否已经被别的字符映射过
            if (mapTS.count(b) && mapTS[b] != a) return false;
            mapST[a] = b;  // 记录本次映射（重复赋值也没影响）
            mapTS[b] = a;
        }
        return true;  // 全程无冲突，是合法双射
    }

}

int main() {
    // 用例 1：输入 ('egg', 'add')；期望 True。
    {
        string arg0 = "egg";
        string arg1 = "add";
        auto result = interview::isIsomorphic(arg0, arg1);
        cout << "用例 1: 期望=true 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ('foo', 'bar')；期望 False。
    {
        string arg0 = "foo";
        string arg1 = "bar";
        auto result = interview::isIsomorphic(arg0, arg1);
        cout << "用例 2: 期望=false 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ('paper', 'title')；期望 True。
    {
        string arg0 = "paper";
        string arg1 = "title";
        auto result = interview::isIsomorphic(arg0, arg1);
        cout << "用例 3: 期望=true 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 这道题的核心陷阱就是"看起来只需要单向映射，但实际上必须双向校验"。你想，单向哈希表只能保证 `s` 里相同的字符映射到相同的 `t` 字符，但它没法阻止不同的 `s` 字符映射到同一个 `t` 字符，所以双向哈希表才是保证双射关系最直接的手段。
- 记录"上一次出现位置"的做法，跟双向哈希表其实是同一个思路的不同实现，本质上都是在给字符出现的"模式"编码，然后比较两边的模式是不是一致。理解了这一层，你就能举一反三去处理类似的模式匹配问题，比如判断两个字符串的字符出现顺序是否同构。
- 这题跟后面的"单词规律"（也就是 LeetCode 290，判断字符串数组和单词是否遵循同一种映射规律）几乎是同一个题型的变体，只是把字符级别的映射换成了单词级别的映射而已，解法完全可以套用双向哈希表的思路，面试官问到你可以直接类比过去。
- 有个容易出错的地方是遍历的时机：一定要先检查冲突，再更新映射。如果你先更新再检查，就会把原有的映射记录给覆盖掉，导致冲突检测直接失效，这个顺序千万不能反。
