# 0242. 有效的字母异位词

**题目描述**

给定两个字符串 `s` 和 `t`，判断 `t` 是否是 `s` 的字母异位词（anagram，即由相同的字符按不同顺序组成，字符出现的次数也必须完全一致）。

```
示例 1：
输入: s = "anagram", t = "nagaram"
输出: true

示例 2：
输入: s = "rat", t = "car"
输出: false
```

假设字符串只包含小写字母。

## 思路

- 计数：长度不同直接失败；26 位数组对一个串加计数、另一个减计数，全部为 0 即相同。时间 O(n)，空间 O(1)。
- 排序比较：排序后相等即成立。时间 O(n log n)，空间取决于排序及是否复制；Python 为 O(n)。



## Python

### LeetCode 写法与原有示例

标准库一行解（用 `collections.Counter` 直接比较两个字符串的字符计数是否相等，`Counter` 本质就是对哈希表计数的封装）：

```python
from collections import Counter

# 依赖说明：Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。

class Solution:
    # 方法：isAnagram；按题目要求处理输入并返回结果，核心算法见方法体。
    def isAnagram(self, s: str, t: str) -> bool:
        return Counter(s) == Counter(t)  # 比较两个字符计数字典是否完全一致
```


手写实现（面试考察点，还原数组哈希计数原理）：

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；all：Python 内置函数：所有元素为真时返回 True。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。

class Solution:
    # 方法：isAnagram；按题目要求处理输入并返回结果，核心算法见方法体。
    def isAnagram(self, s: str, t: str) -> bool:
        if len(s) != len(t):  # 长度不同直接排除
            return False
        count = [0] * 26  # 长度26的数组做哈希，下标对应 a~z
        for c in s:
            count[ord(c) - ord('a')] += 1  # 统计 s 中每个字符出现次数
        for c in t:
            count[ord(c) - ord('a')] -= 1  # t 中每出现一次对应字符就减一
        return all(v == 0 for v in count)  # 全部归零才是字母异位词
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；all：Python 内置函数：所有元素为真时返回 True。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：isAnagram；按题目要求处理输入并返回结果，核心算法见方法体。
def isAnagram(s: str, t: str) -> bool:
    if len(s) != len(t):  # 长度不同直接排除
        return False
    count = [0] * 26  # 长度26的数组做哈希，下标对应 a~z
    for c in s:
        count[ord(c) - ord('a')] += 1  # 统计 s 中每个字符出现次数
    for c in t:
        count[ord(c) - ord('a')] -= 1  # t 中每出现一次对应字符就减一
    return all(v == 0 for v in count)  # 全部归零才是字母异位词


if __name__ == "__main__":
    # 用例 1：输入 ('anagram', 'nagaram')；期望 True。
    arg0 = 'anagram'
    arg1 = 'nagaram'
    result = isAnagram(arg0, arg1)
    expected = True
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ('rat', 'car')；期望 False。
    arg0 = 'rat'
    arg1 = 'car'
    result = isAnagram(arg0, arg1)
    expected = False
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ('a', 'a')；期望 True。
    arg0 = 'a'
    arg1 = 'a'
    result = isAnagram(arg0, arg1)
    expected = True
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：isAnagram；按题目要求处理输入并返回结果，核心算法见方法体。
func isAnagram(s string, t string) bool {
    if len(s) != len(t) {
        return false // 长度不同直接排除
    }
    var count [26]int // 长度26的数组做哈希
    for _, c := range s {
        count[c-'a']++ // 统计 s 中每个字符出现次数
    }
    for _, c := range t {
        count[c-'a']-- // t 中每出现一次对应字符就减一
    }
    for _, v := range count {
        if v != 0 {
            return false // 存在未归零的位置，说明不是异位词
        }
    }
    return true
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：isAnagram；按题目要求处理输入并返回结果，核心算法见方法体。
func isAnagram(s string, t string) bool {
    if len(s) != len(t) {
        return false // 长度不同直接排除
    }
    var count [26]int // 长度26的数组做哈希
    for _, c := range s {
        count[c-'a']++ // 统计 s 中每个字符出现次数
    }
    for _, c := range t {
        count[c-'a']-- // t 中每出现一次对应字符就减一
    }
    for _, v := range count {
        if v != 0 {
            return false // 存在未归零的位置，说明不是异位词
        }
    }
    return true
}

func main() {
    // 用例 1：输入 ('anagram', 'nagaram')；期望 True。
    {
        arg0 := "anagram"
        arg1 := "nagaram"
        result := isAnagram(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=true 实际=", string(data))
    }
    // 用例 2：输入 ('rat', 'car')；期望 False。
    {
        arg0 := "rat"
        arg1 := "car"
        result := isAnagram(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=false 实际=", string(data))
    }
    // 用例 3：输入 ('a', 'a')；期望 True。
    {
        arg0 := "a"
        arg1 := "a"
        result := isAnagram(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=true 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：string：C++ STL 字符串类型。

class Solution {
public:
    // 方法：isAnagram；按题目要求处理输入并返回结果，核心算法见方法体。
    bool isAnagram(string s, string t) {
        if (s.size() != t.size()) return false;  // 长度不同直接排除
        int count[26] = {0};  // 长度26的数组做哈希，下标对应字符
        for (char c : s) {
            count[c - 'a']++;  // 统计 s 中每个字符出现次数
        }
        for (char c : t) {
            count[c - 'a']--;  // t 中每出现一次对应字符就减一
        }
        for (int i = 0; i < 26; i++) {
            if (count[i] != 0) return false;  // 只要有一位不为0，说明字符分布不同
        }
        return true;
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
bool isAnagram(string s, string t);

// 方法：isAnagram；按题目要求处理输入并返回结果，核心算法见方法体。
    bool isAnagram(string s, string t) {
        if (s.size() != t.size()) return false;  // 长度不同直接排除
        int count[26] = {0};  // 长度26的数组做哈希，下标对应字符
        for (char c : s) {
            count[c - 'a']++;  // 统计 s 中每个字符出现次数
        }
        for (char c : t) {
            count[c - 'a']--;  // t 中每出现一次对应字符就减一
        }
        for (int i = 0; i < 26; i++) {
            if (count[i] != 0) return false;  // 只要有一位不为0，说明字符分布不同
        }
        return true;
    }

}

int main() {
    // 用例 1：输入 ('anagram', 'nagaram')；期望 True。
    {
        string arg0 = "anagram";
        string arg1 = "nagaram";
        auto result = interview::isAnagram(arg0, arg1);
        cout << "用例 1: 期望=true 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ('rat', 'car')；期望 False。
    {
        string arg0 = "rat";
        string arg1 = "car";
        auto result = interview::isAnagram(arg0, arg1);
        cout << "用例 2: 期望=false 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ('a', 'a')；期望 True。
    {
        string arg0 = "a";
        string arg1 = "a";
        auto result = interview::isAnagram(arg0, arg1);
        cout << "用例 3: 期望=true 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 你记住一个信号：只要值域小且连续（比如这题就 26 个小写字母），基本就可以拿数组来模拟哈希表，比 `unordered_map`/`HashMap` 快、也更省空间。
- 这道题的核心其实是「计数」，不是「判重」，所以千万不能用 set，得老老实实记录每个字符出现了多少次。
- 用一次自增、一次自减去对比两个字符串的字符分布，比你分别统计完再逐位比较要简洁得多，这个写法值得记下来。
- 那如果字符集扩展到 Unicode 呢，比如字符串里包含了中文，这时候值域就不再小而且连续了，那就得退回去用 `unordered_map<char, int>` 或者 Python 里的 `dict` 来统计了。
