# 0349. 两个数组的交集

**题目描述**

给定两个数组 `nums1` 和 `nums2`，返回它们的交集，结果中的每个元素只能出现一次（去重），可以以任意顺序返回结果。

```
示例 1：
输入: nums1 = [1,2,2,1], nums2 = [2,2]
输出: [2]

示例 2：
输入: nums1 = [4,9,5], nums2 = [9,4,9,8]
输出: [9,4]
```

## 思路

- 集合：将一个数组存入集合，遍历另一个收集共有值，结果用集合去重。平均时间 O(m+n)，空间 O(m+n)（含结果）。
- 值域数组：标记两数组出现过的值，输出共同标记。时间 O(m+n+U)，辅助空间 O(U)，U 为值域大小。



## Python

### LeetCode 写法与原有示例

标准库一行解（`set` 直接支持 `&` 运算符求交集，语义与本题完全一致）：

```python
class Solution:
    # 方法：intersection；按题目要求处理输入并返回结果，核心算法见方法体。
    def intersection(self, nums1: list[int], nums2: list[int]) -> list[int]:
        return list(set(nums1) & set(nums2))  # 两个集合的交集运算符
```


手写实现（面试考察点，还原 set 哈希去重+查询存在性的原理）：

```python
class Solution:
    # 方法：intersection；按题目要求处理输入并返回结果，核心算法见方法体。
    def intersection(self, nums1: list[int], nums2: list[int]) -> list[int]:
        set1 = set(nums1)  # nums1 去重存入 set
        result_set = set()  # 用 set 存结果，防止重复添加
        for num in nums2:
            if num in set1:  # 判断 nums2 的元素是否在 nums1 中出现过（平均 O(1)）
                result_set.add(num)
        return list(result_set)
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：intersection；按题目要求处理输入并返回结果，核心算法见方法体。
def intersection(nums1: list[int], nums2: list[int]) -> list[int]:
    set1 = set(nums1)  # nums1 去重存入 set
    result_set = set()  # 用 set 存结果，防止重复添加
    for num in nums2:
        if num in set1:  # 判断 nums2 的元素是否在 nums1 中出现过（平均 O(1)）
            result_set.add(num)
    return list(result_set)


if __name__ == "__main__":
    # 用例 1：输入 ([1, 2, 2, 1], [2, 2])；期望 [2]。
    arg0 = [1, 2, 2, 1]
    arg1 = [2, 2]
    result = intersection(arg0, arg1)
    expected = [2]
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 2：输入 ([4, 9, 5], [9, 4, 9, 8, 4])；期望 [4, 9]。
    arg0 = [4, 9, 5]
    arg1 = [9, 4, 9, 8, 4]
    result = intersection(arg0, arg1)
    expected = [4, 9]
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 3：输入 ([1], [2])；期望 []。
    arg0 = [1]
    arg1 = [2]
    result = intersection(arg0, arg1)
    expected = []
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：intersection；按题目要求处理输入并返回结果，核心算法见方法体。
func intersection(nums1 []int, nums2 []int) []int {
    set1 := make(map[int]struct{}, len(nums1)) // nums1 去重存入 set（用空结构体节省内存）
    for _, num := range nums1 {
        set1[num] = struct{}{}
    }
    resultSet := make(map[int]struct{}) // 用 set 存结果，防止重复添加
    for _, num := range nums2 {
        if _, ok := set1[num]; ok { // 判断 nums2 的元素是否在 nums1 中出现过
            resultSet[num] = struct{}{}
        }
    }
    result := make([]int, 0, len(resultSet))
    for num := range resultSet {
        result = append(result, num) // 转换为数组返回
    }
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：intersection；按题目要求处理输入并返回结果，核心算法见方法体。
func intersection(nums1 []int, nums2 []int) []int {
    set1 := make(map[int]struct{}, len(nums1)) // nums1 去重存入 set（用空结构体节省内存）
    for _, num := range nums1 {
        set1[num] = struct{}{}
    }
    resultSet := make(map[int]struct{}) // 用 set 存结果，防止重复添加
    for _, num := range nums2 {
        if _, ok := set1[num]; ok { // 判断 nums2 的元素是否在 nums1 中出现过
            resultSet[num] = struct{}{}
        }
    }
    result := make([]int, 0, len(resultSet))
    for num := range resultSet {
        result = append(result, num) // 转换为数组返回
    }
    return result
}

func main() {
    // 用例 1：输入 ([1, 2, 2, 1], [2, 2])；期望 [2]。
    {
        arg0 := []int{1, 2, 2, 1}
        arg1 := []int{2, 2}
        result := intersection(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[2] 实际=", string(data))
    }
    // 用例 2：输入 ([4, 9, 5], [9, 4, 9, 8, 4])；期望 [4, 9]。
    {
        arg0 := []int{4, 9, 5}
        arg1 := []int{9, 4, 9, 8, 4}
        result := intersection(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[4, 9] 实际=", string(data))
    }
    // 用例 3：输入 ([1], [2])；期望 []。
    {
        arg0 := []int{1}
        arg1 := []int{2}
        result := intersection(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。；unordered_set：C++ STL 哈希集合。

class Solution {
public:
    // 方法：intersection；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> intersection(vector<int>& nums1, vector<int>& nums2) {
        unordered_set<int> set1(nums1.begin(), nums1.end());  // nums1 去重存入 set
        unordered_set<int> resultSet;  // 用 set 存结果，防止重复添加
        for (int num : nums2) {
            if (set1.count(num)) {  // 判断 nums2 的元素是否在 nums1 中出现过
                resultSet.insert(num);
            }
        }
        return vector<int>(resultSet.begin(), resultSet.end());  // 转换为数组返回
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
vector<int> intersection(vector<int>& nums1, vector<int>& nums2);

// 方法：intersection；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> intersection(vector<int>& nums1, vector<int>& nums2) {
        unordered_set<int> set1(nums1.begin(), nums1.end());  // nums1 去重存入 set
        unordered_set<int> resultSet;  // 用 set 存结果，防止重复添加
        for (int num : nums2) {
            if (set1.count(num)) {  // 判断 nums2 的元素是否在 nums1 中出现过
                resultSet.insert(num);
            }
        }
        return vector<int>(resultSet.begin(), resultSet.end());  // 转换为数组返回
    }

}

int main() {
    // 用例 1：输入 ([1, 2, 2, 1], [2, 2])；期望 [2]。
    {
        vector<int> arg0 = {1, 2, 2, 1};
        vector<int> arg1 = {2, 2};
        auto result = interview::intersection(arg0, arg1);
        cout << "用例 1: 期望=[2] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([4, 9, 5], [9, 4, 9, 8, 4])；期望 [4, 9]。
    {
        vector<int> arg0 = {4, 9, 5};
        vector<int> arg1 = {9, 4, 9, 8, 4};
        auto result = interview::intersection(arg0, arg1);
        cout << "用例 2: 期望=[4, 9] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([1], [2])；期望 []。
    {
        vector<int> arg0 = {1};
        vector<int> arg1 = {2};
        auto result = interview::intersection(arg0, arg1);
        cout << "用例 3: 期望=[] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 「求交集且去重」这种场景，说白了就是哈希表里只需要判断存在性、不需要维护键值映射的情况，所以应该用 `set`，不是 `map`。
- 数组哈希法要求值域小且连续；set 哈希法不依赖值域大小，任意整数都能处理，包括负数和大数。这题元素范围没限定，用 set 更通用。
- 结果为什么也要先用 set 暂存再转数组呢，就是为了防止第二个数组内部本身有重复元素，导致交集元素被重复添加进结果里。
- 那如果题目明确告诉你值域比较小，比如限定在 0 到 1000 之间，数组哈希法会比 set 快，因为省去了哈希函数计算和冲突处理这些开销。
