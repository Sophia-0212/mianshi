# 1002. 查找常用字符

**题目描述**

给定一个字符串数组 `words`，找出所有在 `words` 的每个字符串中都出现的字符（包括重复出现的字符），以数组形式返回结果，返回结果中的每个元素都可以以任意顺序排列。

```
示例 1：
输入: words = ["bella","label","roller"]
输出: ["e","l","l"]

示例 2：
输入: words = ["cool","lock","cook"]
输出: ["c","o"]
```

假设字符串只包含小写字母。

## 思路

分别统计各字符串的 26 个字母频次，逐字母取最小频次，再按该次数输出。

时间 O(L+26k)，辅助空间 O(1)，不计结果；L 为总字符数，k 为字符串数。



## Python

### LeetCode 写法与原有示例

这道题其实没有现成的标准库一行解，因为「多字符串按字符最小计数求交集」不是标准库内置的操作，所以还是直接手写数组哈希来实现：

```python
# 依赖说明：range：Python 内置函数：生成按步长遍历的整数序列。；min：Python 内置函数：返回最小值。

# 依赖说明：range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：commonChars；按题目要求处理输入并返回结果，核心算法见方法体。
    def commonChars(self, words: list[str]) -> list[str]:
        min_count = [float('inf')] * 26  # 记录每个字符的最小出现次数
        for word in words:
            count = [0] * 26  # 当前字符串的字符计数
            for c in word:
                count[ord(c) - ord('a')] += 1
            for i in range(26):
                min_count[i] = min(min_count[i], count[i])  # 逐维取最小值，即求交集

        result = []
        for i in range(26):
            result.extend([chr(ord('a') + i)] * min_count[i])  # 按最小次数重复添加该字符
        return result
```


> 提示：你也可以用 `collections.Counter` 配合 `&` 运算符来实现同样的效果——`Counter` 的 `&` 运算本质上就是取多重集合的交集，天然就支持重复计数。不过这里为了体现哈希数组这种通用写法，还是以手写为主。

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。；min：Python 内置函数：返回最小值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：commonChars；按题目要求处理输入并返回结果，核心算法见方法体。
def commonChars(words: list[str]) -> list[str]:
    min_count = [float('inf')] * 26  # 记录每个字符的最小出现次数
    for word in words:
        count = [0] * 26  # 当前字符串的字符计数
        for c in word:
            count[ord(c) - ord('a')] += 1
        for i in range(26):
            min_count[i] = min(min_count[i], count[i])  # 逐维取最小值，即求交集

    result = []
    for i in range(26):
        result.extend([chr(ord('a') + i)] * min_count[i])  # 按最小次数重复添加该字符
    return result


if __name__ == "__main__":
    # 用例 1：输入 (['bella', 'label', 'roller'],)；期望 ['e', 'l', 'l']。
    arg0 = ['bella', 'label', 'roller']
    result = commonChars(arg0)
    expected = ['e', 'l', 'l']
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 2：输入 (['cool', 'lock', 'cook'],)；期望 ['c', 'o']。
    arg0 = ['cool', 'lock', 'cook']
    result = commonChars(arg0)
    expected = ['c', 'o']
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 3：输入 (['a', 'b'],)；期望 []。
    arg0 = ['a', 'b']
    result = commonChars(arg0)
    expected = []
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：commonChars；按题目要求处理输入并返回结果，核心算法见方法体。
func commonChars(words []string) []string {
    minCount := [26]int{}
    for i := range minCount {
        minCount[i] = 1 << 30 // 初始化为一个足够大的数，方便取最小
    }
    for _, word := range words {
        var count [26]int // 当前字符串的字符计数
        for _, c := range word {
            count[c-'a']++
        }
        for i := 0; i < 26; i++ {
            if count[i] < minCount[i] {
                minCount[i] = count[i] // 逐维取最小值，即求交集
            }
        }
    }

    var result []string
    for i := 0; i < 26; i++ {
        for j := 0; j < minCount[i]; j++ { // 按最小次数重复添加该字符
            result = append(result, string(rune('a'+i)))
        }
    }
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：commonChars；按题目要求处理输入并返回结果，核心算法见方法体。
func commonChars(words []string) []string {
    minCount := [26]int{}
    for i := range minCount {
        minCount[i] = 1 << 30 // 初始化为一个足够大的数，方便取最小
    }
    for _, word := range words {
        var count [26]int // 当前字符串的字符计数
        for _, c := range word {
            count[c-'a']++
        }
        for i := 0; i < 26; i++ {
            if count[i] < minCount[i] {
                minCount[i] = count[i] // 逐维取最小值，即求交集
            }
        }
    }

    var result []string
    for i := 0; i < 26; i++ {
        for j := 0; j < minCount[i]; j++ { // 按最小次数重复添加该字符
            result = append(result, string(rune('a'+i)))
        }
    }
    return result
}

func main() {
    // 用例 1：输入 (['bella', 'label', 'roller'],)；期望 ['e', 'l', 'l']。
    {
        arg0 := []string{"bella", "label", "roller"}
        result := commonChars(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[\"e\", \"l\", \"l\"] 实际=", string(data))
    }
    // 用例 2：输入 (['cool', 'lock', 'cook'],)；期望 ['c', 'o']。
    {
        arg0 := []string{"cool", "lock", "cook"}
        result := commonChars(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[\"c\", \"o\"] 实际=", string(data))
    }
    // 用例 3：输入 (['a', 'b'],)；期望 []。
    {
        arg0 := []string{"a", "b"}
        result := commonChars(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。；string：C++ STL 字符串类型。

class Solution {
public:
    // 方法：commonChars；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<string> commonChars(vector<string>& words) {
        vector<int> minCount(26, INT_MAX);  // 记录每个字符的最小出现次数
        for (const string& word : words) {
            vector<int> count(26, 0);  // 当前字符串的字符计数
            for (char c : word) {
                count[c - 'a']++;
            }
            for (int i = 0; i < 26; i++) {
                minCount[i] = min(minCount[i], count[i]);  // 逐维取最小值，即求交集
            }
        }
        vector<string> result;
        for (int i = 0; i < 26; i++) {
            for (int j = 0; j < minCount[i]; j++) {  // 按最小次数重复添加该字符
                result.push_back(string(1, 'a' + i));
            }
        }
        return result;
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
vector<string> commonChars(vector<string>& words);

// 方法：commonChars；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<string> commonChars(vector<string>& words) {
        vector<int> minCount(26, INT_MAX);  // 记录每个字符的最小出现次数
        for (const string& word : words) {
            vector<int> count(26, 0);  // 当前字符串的字符计数
            for (char c : word) {
                count[c - 'a']++;
            }
            for (int i = 0; i < 26; i++) {
                minCount[i] = min(minCount[i], count[i]);  // 逐维取最小值，即求交集
            }
        }
        vector<string> result;
        for (int i = 0; i < 26; i++) {
            for (int j = 0; j < minCount[i]; j++) {  // 按最小次数重复添加该字符
                result.push_back(string(1, 'a' + i));
            }
        }
        return result;
    }

}

int main() {
    // 用例 1：输入 (['bella', 'label', 'roller'],)；期望 ['e', 'l', 'l']。
    {
        vector<string> arg0 = {"bella", "label", "roller"};
        auto result = interview::commonChars(arg0);
        cout << "用例 1: 期望=[\"e\", \"l\", \"l\"] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 (['cool', 'lock', 'cook'],)；期望 ['c', 'o']。
    {
        vector<string> arg0 = {"cool", "lock", "cook"};
        auto result = interview::commonChars(arg0);
        cout << "用例 2: 期望=[\"c\", \"o\"] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 (['a', 'b'],)；期望 []。
    {
        vector<string> arg0 = {"a", "b"};
        auto result = interview::commonChars(arg0);
        cout << "用例 3: 期望=[] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 「所有字符串都出现的字符（还要保留重复次数）」这个要求，说白了就等价于把每个字符在各个字符串里出现的次数都取一个最小值——用数组逐维做 min 运算，天然就实现了多字符串求交集。
- 值域小而且连续（就 26 个字母）这一点，依然是我们选数组而不是用 map 的判断依据。
- 这种「先各自统计成计数向量，再逐维取 min 或 max」的技巧，其实可以推广到很多多字符串比较类的问题上，比如求所有字符串的公共前缀特征之类的。
- 有一点要特别注意：初始化 `minCount` 的时候，一定要用一个足够大的初值（比如 `INT_MAX`），不然第一次取 min 的时候就会出错。
