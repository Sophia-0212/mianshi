# 0018. 四数之和

**题目描述**

给定一个整数数组 `nums` 和一个目标值 `target`，找出所有满足条件且不重复的四元组 `[nums[a], nums[b], nums[c], nums[d]]`（`a`、`b`、`c`、`d` 互不相同），使得 `nums[a] + nums[b] + nums[c] + nums[d] == target`。

```
示例：
输入: nums = [1,0,-1,0,-2,2], target = 0
输出: [[-2,-1,1,2],[-2,0,0,2],[-1,0,0,1]]
```

## 思路

四数之和你可以理解成三数之和往上再套一层。三数之和的套路是"固定一个数 + 双指针找另外两个数"，这里呢，说白了就是**再加一层循环，多固定一个数**，把问题规模降到双指针能处理的两个数级别。整体框架还是那句话：排序 + 两层固定 + 双指针。

这里延续三数之和的结论：这题同样要求结果里不能有重复的四元组，去重逻辑本来就复杂，哈希法在这上面不好用；排序完之后用双指针，利用相邻元素相等这个特性在遍历时直接跳过重复分支，依然是这道题的最优解法。

具体怎么做呢：

1. 对数组排序。
2. 外层循环固定第一个数 `nums[i]`（`i` 从 0 到 n-4）：
   - **第一层去重**：`i > 0 && nums[i] == nums[i-1]` 时跳过。
   - **剪枝优化**：如果 `nums[i]` 加上后面三个最小的数（即紧随其后的三个数）都已经大于 `target`，说明后面不可能再凑出 target（因为数组已排序，后面的数只会更大或相等），可以直接 break。同理，如果 `nums[i]` 加上数组末尾三个最大的数都小于 `target`，说明当前 `i` 无论如何都凑不出 target，可以直接 continue 跳到下一个 `i`。
3. 内层循环固定第二个数 `nums[j]`（`j` 从 `i+1` 到 n-3），逻辑和外层类似：
   - **第二层去重**：`j > i+1 && nums[j] == nums[j-1]` 时跳过。
   - 同样可以做类似的剪枝（两数之和最小值超过target就break，最大值仍小于target就continue）。
4. 在剩下的区间 `[j+1, n-1]` 用双指针 `left`、`right` 寻找另外两个数，使得四数之和等于 target：
   - 计算 `sum = nums[i] + nums[j] + nums[left] + nums[right]`：
     - `sum == target`：记录结果，`left++`、`right--`，并分别跳过重复值（**第三、四层去重**）。
     - `sum > target`：`right--`。
     - `sum < target`：`left++`。

四数之和比三数之和多了一层去重，你看外层就从原来的一层变成了两层，也就是 `i` 和 `j` 两个。这个东西本质上就是双指针法的递归式推广，你可以这么理解：**N 数之和这类问题，都可以看成先固定住 N-2 个数，剩下最后两个数交给双指针去处理**。两数之和就是双指针最基本的那种情况，三数之和相当于固定一个数，四数之和相当于固定两个数，这个思路可以一直往下套。

这里要特别提一下，本题的剪枝跟三数之和不太一样：三数之和的目标固定就是 0，所以只要判断一下 `nums[i] > 0` 就能剪枝了；但四数之和的 target 可以是任意值，也可能是负数，所以剪枝条件得写成跟 target 去比较，而且要同时兼顾两个方向——"最小值都已经超过 target 了"（这时候直接提前结束）和"最大值都还凑不够 target"（这时候跳过当前这个元素），可不能想当然地照抄三数之和里 `>0` 的写法。

**复杂度这块说一下**：设数组长度是 n。
- 时间复杂度是 O(n³)：两层固定循环加起来是 O(n²)，内层双指针扫描是 O(n)，排序的 O(n log n) 相比之下就不用管了。
- 空间复杂度是 O(1)，这里不算排序本身占用的额外空间。

## 代码

### C++

```cpp
class Solution {
public:
    vector<vector<int>> fourSum(vector<int>& nums, int target) {
        vector<vector<int>> result;
        sort(nums.begin(), nums.end());  // 排序，为双指针和去重提供基础
        int n = nums.size();

        for (int i = 0; i < n - 3; i++) {
            if (i > 0 && nums[i] == nums[i - 1]) continue;  // 第一层去重
            // 剪枝：用 long 防止溢出
            if ((long)nums[i] + nums[i+1] + nums[i+2] + nums[i+3] > target) break;  // 最小组合已超target，后面更不可能
            if ((long)nums[i] + nums[n-1] + nums[n-2] + nums[n-3] < target) continue;  // 最大组合仍不够，当前i无解

            for (int j = i + 1; j < n - 2; j++) {
                if (j > i + 1 && nums[j] == nums[j - 1]) continue;  // 第二层去重
                if ((long)nums[i] + nums[j] + nums[j+1] + nums[j+2] > target) break;  // 剪枝
                if ((long)nums[i] + nums[j] + nums[n-1] + nums[n-2] < target) continue;  // 剪枝

                int left = j + 1, right = n - 1;
                while (left < right) {
                    long sum = (long)nums[i] + nums[j] + nums[left] + nums[right];
                    if (sum == target) {
                        result.push_back({nums[i], nums[j], nums[left], nums[right]});
                        while (left < right && nums[left] == nums[left + 1]) left++;  // 第三层去重
                        while (left < right && nums[right] == nums[right - 1]) right--;  // 第四层去重
                        left++;
                        right--;
                    } else if (sum > target) {
                        right--;  // 和太大，右指针左移
                    } else {
                        left++;  // 和太小，左指针右移
                    }
                }
            }
        }
        return result;
    }
};
```

### Java

```java
class Solution {
    public List<List<Integer>> fourSum(int[] nums, int target) {
        List<List<Integer>> result = new ArrayList<>();
        Arrays.sort(nums);  // 排序，为双指针和去重提供基础
        int n = nums.length;

        for (int i = 0; i < n - 3; i++) {
            if (i > 0 && nums[i] == nums[i - 1]) continue;  // 第一层去重
            if ((long) nums[i] + nums[i+1] + nums[i+2] + nums[i+3] > target) break;  // 剪枝
            if ((long) nums[i] + nums[n-1] + nums[n-2] + nums[n-3] < target) continue;  // 剪枝

            for (int j = i + 1; j < n - 2; j++) {
                if (j > i + 1 && nums[j] == nums[j - 1]) continue;  // 第二层去重
                if ((long) nums[i] + nums[j] + nums[j+1] + nums[j+2] > target) break;  // 剪枝
                if ((long) nums[i] + nums[j] + nums[n-1] + nums[n-2] < target) continue;  // 剪枝

                int left = j + 1, right = n - 1;
                while (left < right) {
                    long sum = (long) nums[i] + nums[j] + nums[left] + nums[right];
                    if (sum == target) {
                        result.add(Arrays.asList(nums[i], nums[j], nums[left], nums[right]));
                        while (left < right && nums[left] == nums[left + 1]) left++;  // 第三层去重
                        while (left < right && nums[right] == nums[right - 1]) right--;  // 第四层去重
                        left++;
                        right--;
                    } else if (sum > target) {
                        right--;  // 和太大，右指针左移
                    } else {
                        left++;  // 和太小，左指针右移
                    }
                }
            }
        }
        return result;
    }
}
```

### Python

跟三数之和一样，这题也不适合用哈希法去重，所以还是直接给你排序加双指针的手写实现：

```python
class Solution:
    def fourSum(self, nums: list[int], target: int) -> list[list[int]]:
        nums.sort()  # 排序，为双指针和去重提供基础
        n = len(nums)
        result = []

        for i in range(n - 3):
            if i > 0 and nums[i] == nums[i - 1]:  # 第一层去重
                continue
            if nums[i] + nums[i+1] + nums[i+2] + nums[i+3] > target:  # 最小组合已超target
                break
            if nums[i] + nums[n-1] + nums[n-2] + nums[n-3] < target:  # 最大组合仍不够
                continue

            for j in range(i + 1, n - 2):
                if j > i + 1 and nums[j] == nums[j - 1]:  # 第二层去重
                    continue
                if nums[i] + nums[j] + nums[j+1] + nums[j+2] > target:  # 剪枝
                    break
                if nums[i] + nums[j] + nums[n-1] + nums[n-2] < target:  # 剪枝
                    continue

                left, right = j + 1, n - 1
                while left < right:
                    total = nums[i] + nums[j] + nums[left] + nums[right]
                    if total == target:
                        result.append([nums[i], nums[j], nums[left], nums[right]])
                        while left < right and nums[left] == nums[left + 1]:
                            left += 1  # 第三层去重
                        while left < right and nums[right] == nums[right - 1]:
                            right -= 1  # 第四层去重
                        left += 1
                        right -= 1
                    elif total > target:
                        right -= 1  # 和太大，右指针左移
                    else:
                        left += 1  # 和太小，左指针右移
        return result
```

### Go

```go
import "sort"

func fourSum(nums []int, target int) [][]int {
    sort.Ints(nums) // 排序，为双指针和去重提供基础
    n := len(nums)
    result := [][]int{}

    for i := 0; i < n-3; i++ {
        if i > 0 && nums[i] == nums[i-1] {
            continue // 第一层去重
        }
        if nums[i]+nums[i+1]+nums[i+2]+nums[i+3] > target {
            break // 最小组合已超target
        }
        if nums[i]+nums[n-1]+nums[n-2]+nums[n-3] < target {
            continue // 最大组合仍不够
        }

        for j := i + 1; j < n-2; j++ {
            if j > i+1 && nums[j] == nums[j-1] {
                continue // 第二层去重
            }
            if nums[i]+nums[j]+nums[j+1]+nums[j+2] > target {
                break // 剪枝
            }
            if nums[i]+nums[j]+nums[n-1]+nums[n-2] < target {
                continue // 剪枝
            }

            left, right := j+1, n-1
            for left < right {
                sum := nums[i] + nums[j] + nums[left] + nums[right]
                if sum == target {
                    result = append(result, []int{nums[i], nums[j], nums[left], nums[right]})
                    for left < right && nums[left] == nums[left+1] {
                        left++ // 第三层去重
                    }
                    for left < right && nums[right] == nums[right-1] {
                        right-- // 第四层去重
                    }
                    left++
                    right--
                } else if sum > target {
                    right-- // 和太大，右指针左移
                } else {
                    left++ // 和太小，左指针右移
                }
            }
        }
    }
    return result
}
```

## 总结

- 四数之和说白了就是三数之和的推广：外层多加一层固定循环，也就是多固定一个数，内层还是老套路用双指针处理最后两个数，去重逻辑也跟着从两层扩展成了四层，也就是 `i`、`j`、`left`、`right`。
- N 数之和这类问题存在一个通用的递归模式：固定住 N-2 个数之后，剩下两个数用双指针去解决，这个模式还能继续往上套，比方说五数之和之类更高阶的变体，一般会用递归来实现会更通用一些。
- 剪枝逻辑一定要跟 target 比较，不能像三数之和那样简单套用 `>0` 的判断，因为这里的 target 可以是任意值，甚至是负数，而且要同时兼顾两个方向的剪枝——"最小组合超过 target 就 break"和"最大组合还不够 target 就 continue"，这两个都得处理到。
- 算和的过程中要留意可能的整数溢出，这题的数据范围下 4 个 int 加起来是有可能超出 int 范围的，所以 C++ 和 Java 里建议用 `long` 类型来做中间计算，避免溢出出问题。
