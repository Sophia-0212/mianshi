# 0207. 课程表

[LeetCode 原题](https://leetcode.com/problems/course-schedule/)

## 题意与思路

有向边 b→a 表示先学 b 再学 a。Kahn 拓扑排序不断移除入度为零的课程；能移除全部课程才无环。

时间 O(V+E)，空间 O(V+E)。

## 输入约定与测试

n=0 且无依赖返回 true；课程编号必须属于 [0,n)。非法编号、负 n 拒绝。

- 正常：`[2, [[1, 0]]]` → `True`。
- 环：`[2, [[1, 0], [0, 1]]]` → `False`。
- 空课程：`[0, []]` → `True`。
- 自环：`[1, [[0, 0]]]` → `False`。
- 非法编号：`[2, [[2, 0]]]` → `拒绝非法输入`。

三种实现均为普通函数，不依赖 `Solution`。节点或状态结构仅保存数据；`solve` 是可直接调用的入口。每段包含独立入口与断言。

## Python

保存为 `main.py`，运行 `python3 main.py`。

```python
def course_order(n, edges):
    if n < 0:
        raise ValueError('课程数不能为负')
    graph = [[] for _ in range(n)]
    degree = [0] * n
    for edge in edges:
        if len(edge) != 2 or any((x < 0 or x >= n for x in edge)):
            raise ValueError('非法边')
        (a, b) = edge
        graph[b].append(a)
        degree[a] += 1
    queue = [i for (i, d) in enumerate(degree) if d == 0]
    head = 0
    while head < len(queue):
        u = queue[head]
        head += 1
        for v in graph[u]:
            degree[v] -= 1
            if degree[v] == 0:
                queue.append(v)
    return queue if len(queue) == n else []

def solve(n, edges):
    return len(course_order(n, edges)) == n
if __name__ == '__main__':
    actual = solve(2, [[1, 0]])
    expected = True
    print('用例 1：期望', expected, '实际', actual)
    assert actual == expected
    actual = solve(2, [[1, 0], [0, 1]])
    expected = False
    print('用例 2：期望', expected, '实际', actual)
    assert actual == expected
    actual = solve(0, [])
    expected = True
    print('用例 3：期望', expected, '实际', actual)
    assert actual == expected
    actual = solve(1, [[0, 0]])
    expected = False
    print('用例 4：期望', expected, '实际', actual)
    assert actual == expected
    try:
        solve(2, [[2, 0]])
    except (ValueError, IndexError):
        print('用例 5：已拒绝非法输入')
    else:
        raise AssertionError('应拒绝非法输入')
```

## Go

保存为 `main.go`，运行 `go run main.go`。

```go
package main

import (
	"fmt"
	"reflect"
)

func courseOrder(n int, edges [][]int) []int {
	if n < 0 {
		panic("课程数不能为负")
	}
	graph := make([][]int, n)
	degree := make([]int, n)
	for _, e := range edges {
		if len(e) != 2 || e[0] < 0 || e[0] >= n || e[1] < 0 || e[1] >= n {
			panic("非法边")
		}
		a, b := e[0], e[1]
		graph[b] = append(graph[b], a)
		degree[a]++
	}
	q := []int{}
	for i, d := range degree {
		if d == 0 {
			q = append(q, i)
		}
	}
	for head := 0; head < len(q); head++ {
		for _, v := range graph[q[head]] {
			degree[v]--
			if degree[v] == 0 {
				q = append(q, v)
			}
		}
	}
	if len(q) != n {
		return []int{}
	}
	return q
}

func solve(n int, edges [][]int) bool { return len(courseOrder(n, edges)) == n }

func main() {
	{ // 正常
		actual := solve(2, [][]int{[]int{1, 0}})
		expected := true
		fmt.Printf("用例 1：期望 %v 实际 %v\n", expected, actual)
		if !reflect.DeepEqual(actual, expected) {
			panic("结果不符")
		}
	}
	{ // 环
		actual := solve(2, [][]int{[]int{1, 0}, []int{0, 1}})
		expected := false
		fmt.Printf("用例 2：期望 %v 实际 %v\n", expected, actual)
		if !reflect.DeepEqual(actual, expected) {
			panic("结果不符")
		}
	}
	{ // 空课程
		actual := solve(0, [][]int{})
		expected := true
		fmt.Printf("用例 3：期望 %v 实际 %v\n", expected, actual)
		if !reflect.DeepEqual(actual, expected) {
			panic("结果不符")
		}
	}
	{ // 自环
		actual := solve(1, [][]int{[]int{0, 0}})
		expected := false
		fmt.Printf("用例 4：期望 %v 实际 %v\n", expected, actual)
		if !reflect.DeepEqual(actual, expected) {
			panic("结果不符")
		}
	}
	func() {
		defer func() {
			if recover() == nil {
				panic("应拒绝非法输入")
			}
			fmt.Println("用例 5：已拒绝非法输入")
		}()
		solve(2, [][]int{[]int{2, 0}})
	}()
}
```

## C++

保存为 `main.cpp`，运行 `c++ -std=c++17 main.cpp -o main && ./main`。

```cpp
#include <cassert>
#include <iostream>
#include <stdexcept>
#include <vector>

using namespace std;
vector<int> courseOrder(int n,const vector<vector<int>>& edges) {
    if(n<0)throw invalid_argument("negative count");
    vector<vector<int>> graph(n);
    vector<int> degree(n);
    for(auto& e:edges) {
        if(e.size()!=2||e[0]<0||e[0]>=n||e[1]<0||e[1]>=n)throw invalid_argument("bad edge");
        graph[e[1]].push_back(e[0]);
        ++degree[e[0]];
    }
    vector<int> q;
    for(int i=0;i<n;++i)if(!degree[i])q.push_back(i);
    for(size_t h=0;h<q.size();++h)for(int v:graph[q[h]])if(--degree[v]==0)q.push_back(v);
    if((int)q.size()!=n)return {};
    return q;
}
bool solve(int n,const vector<vector<int>>& edges) {
    return (int)courseOrder(n,edges).size()==n;
}
template<class T> void show(const vector<T>& xs);
template<class T> void show(const T& x) {
    cout << boolalpha << x;
}
template<class T> void show(const vector<T>& xs) {
    cout << "[";
    for (size_t i=0; i<xs.size(); ++i) {
        if(i) cout << ", ";
        show(xs[i]);
    }
    cout << "]";
}
int main() {
    {
        // 正常
        auto actual = solve(2, vector<vector<int>> {
            vector<int> {1, 0}
        });
        auto expected = true;
        cout << "用例 1：期望 ";
        show(expected);
        cout << " 实际 ";
        show(actual);
        cout << "\n";
        assert(actual == expected);
    } {
        // 环
        auto actual = solve(2, vector<vector<int>> {
            vector<int> {1, 0}, vector<int> {0, 1}
        });
        auto expected = false;
        cout << "用例 2：期望 ";
        show(expected);
        cout << " 实际 ";
        show(actual);
        cout << "\n";
        assert(actual == expected);
    } {
        // 空课程
        auto actual = solve(0, vector<vector<int>> {});
        auto expected = true;
        cout << "用例 3：期望 ";
        show(expected);
        cout << " 实际 ";
        show(actual);
        cout << "\n";
        assert(actual == expected);
    } {
        // 自环
        auto actual = solve(1, vector<vector<int>> {
            vector<int> {0, 0}
        });
        auto expected = false;
        cout << "用例 4：期望 ";
        show(expected);
        cout << " 实际 ";
        show(actual);
        cout << "\n";
        assert(actual == expected);
    } {
        bool rejected=false;
        try {
            solve(2, vector<vector<int>> {
                vector<int> {2, 0}
            });
        } catch(const invalid_argument&) {
            rejected=true;
        }
        assert(rejected);
        cout << "用例 5：已拒绝非法输入\n";
    }
}
```
