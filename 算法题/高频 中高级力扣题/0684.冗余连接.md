> 本文归档自算法题目录，归入“后端高频 Top 50”专题。

# 684. 冗余连接

**题目描述**

给定一个无向图，它原本是一棵树（n 个节点，n-1 条边，无环、连通），现在多加了一条边，导致图中出现了一个环。`edges` 数组按顺序给出所有边（包括多余的那一条），要求找出这条可以删除、使图重新变成一棵树的边。如果有多个符合要求的答案，返回数组 `edges` 中最后出现的那一条。

```
示例：
输入: edges = [[1,2],[1,3],[2,3]]
输出: [2,3]
解释: 节点1、2、3原本应该构成一棵树，但1-2、1-3、2-3三条边形成了环，
     其中[2,3]是最后加入且导致成环的边，删除它后剩下[1,2]、[1,3]恰好构成一棵树
```

## 思路

并查集按输入顺序处理边：端点不同根则合并，同根则当前边闭环，返回该边；它是环中最后出现的边。

当前仅路径压缩：时间 O(n log n) 上界，空间 O(n)；再加按秩／大小合并，时间可为 O(nα(n))。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：findRedundantConnection；按题目要求处理输入并返回结果，核心算法见方法体。
    def findRedundantConnection(self, edges: list[list[int]]) -> list[int]:
        n = len(edges)
        parent = list(range(n + 1))  # 节点编号从1开始，初始化每个节点的父节点是自己

        # 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
        def find(x: int) -> int:
            if parent[x] != x:
                parent[x] = find(parent[x])  # 路径压缩
            return parent[x]

        for u, v in edges:
            root_u, root_v = find(u), find(v)
            if root_u == root_v:
                return [u, v]  # 两端点已同根，这条边是多余边
            parent[root_u] = root_v  # 否则合并两个集合

        return []  # 理论上不会执行到这里
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：findRedundantConnection；按题目要求处理输入并返回结果，核心算法见方法体。
def findRedundantConnection(edges: list[list[int]]) -> list[int]:
    n = len(edges)
    parent = list(range(n + 1))  # 节点编号从1开始，初始化每个节点的父节点是自己

    # 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
    def find(x: int) -> int:
        if parent[x] != x:
            parent[x] = find(parent[x])  # 路径压缩
        return parent[x]

    for u, v in edges:
        root_u, root_v = find(u), find(v)
        if root_u == root_v:
            return [u, v]  # 两端点已同根，这条边是多余边
        parent[root_u] = root_v  # 否则合并两个集合

    return []  # 理论上不会执行到这里


if __name__ == "__main__":
    # 用例 1：输入 ([[1, 2], [1, 3], [2, 3]],)；期望 [2, 3]。
    arg0 = [[1, 2], [1, 3], [2, 3]]
    result = findRedundantConnection(arg0)
    expected = [2, 3]
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([[1, 2], [2, 3], [3, 4], [1, 4], [1, 5]],)；期望 [1, 4]。
    arg0 = [[1, 2], [2, 3], [3, 4], [1, 4], [1, 5]]
    result = findRedundantConnection(arg0)
    expected = [1, 4]
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([[1, 2], [2, 3], [1, 3]],)；期望 [1, 3]。
    arg0 = [[1, 2], [2, 3], [1, 3]]
    result = findRedundantConnection(arg0)
    expected = [1, 3]
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected

    # 补充边界断言
    assert findRedundantConnection([]) == []
    print("补充边界断言：通过")
```

## Go

### LeetCode 写法与原有示例

```go
var parent []int

// 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
func find(x int) int {
    if parent[x] != x {
        parent[x] = find(parent[x]) // 路径压缩
    }
    return parent[x]
}

// 方法：findRedundantConnection；按题目要求处理输入并返回结果，核心算法见方法体。
func findRedundantConnection(edges [][]int) []int {
    n := len(edges)
    parent = make([]int, n+1) // 节点编号从1开始
    for i := 0; i <= n; i++ {
        parent[i] = i // 初始化
    }
    for _, edge := range edges {
        u, v := edge[0], edge[1]
        rootU, rootV := find(u), find(v)
        if rootU == rootV {
            return edge // 两端点已同根，是多余边
        }
        parent[rootU] = rootV // 合并集合
    }
    return []int{} // 理论上不会执行到这里
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
var parent []int

// 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
func find(x int) int {
    if parent[x] != x {
        parent[x] = find(parent[x]) // 路径压缩
    }
    return parent[x]
}

// 方法：findRedundantConnection；按题目要求处理输入并返回结果，核心算法见方法体。
func findRedundantConnection(edges [][]int) []int {
    n := len(edges)
    parent = make([]int, n+1) // 节点编号从1开始
    for i := 0; i <= n; i++ {
        parent[i] = i // 初始化
    }
    for _, edge := range edges {
        u, v := edge[0], edge[1]
        rootU, rootV := find(u), find(v)
        if rootU == rootV {
            return edge // 两端点已同根，是多余边
        }
        parent[rootU] = rootV // 合并集合
    }
    return []int{} // 理论上不会执行到这里
}

func main() {
    // 用例 1：输入 ([[1, 2], [1, 3], [2, 3]],)；期望 [2, 3]。
    {
        arg0 := [][]int{[]int{1, 2}, []int{1, 3}, []int{2, 3}}
        result := findRedundantConnection(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[2, 3] 实际=", string(data))
    }
    // 用例 2：输入 ([[1, 2], [2, 3], [3, 4], [1, 4], [1, 5]],)；期望 [1, 4]。
    {
        arg0 := [][]int{[]int{1, 2}, []int{2, 3}, []int{3, 4}, []int{1, 4}, []int{1, 5}}
        result := findRedundantConnection(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[1, 4] 实际=", string(data))
    }
    // 用例 3：输入 ([[1, 2], [2, 3], [1, 3]],)；期望 [1, 3]。
    {
        arg0 := [][]int{[]int{1, 2}, []int{2, 3}, []int{1, 3}}
        result := findRedundantConnection(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[1, 3] 实际=", string(data))
    }
    // 补充边界断言
    if !(len(findRedundantConnection([][]int{})) == 0) { panic("空输入或边界结果错误") }
    fmt.Println("补充边界断言：通过")
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。

class Solution {
public:
    vector<int> parent;

    // 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
    int find(int x) {
        if (parent[x] != x) {
            parent[x] = find(parent[x]);  // 路径压缩
        }
        return parent[x];
    }

    // 方法：findRedundantConnection；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> findRedundantConnection(vector<vector<int>>& edges) {
        int n = edges.size();
        parent.resize(n + 1);  // 节点编号从1开始，开n+1大小
        for (int i = 0; i <= n; i++) {
            parent[i] = i;  // 初始化，每个节点的父节点是自己
        }
        for (auto& edge : edges) {
            int u = edge[0], v = edge[1];
            int rootU = find(u), rootV = find(v);
            if (rootU == rootV) {
                return edge;  // 两端点已同根，这条边会成环，就是多余边
            }
            parent[rootU] = rootV;  // 否则正常合并两个集合
        }
        return {};  // 理论上不会执行到这里（题目保证一定存在多余边）
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {
vector<int> parent;
// 普通函数与辅助函数声明。
int find(int x);
vector<int> findRedundantConnectionImpl(vector<vector<int>>& edges);

// 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
    int find(int x) {
        if (parent[x] != x) {
            parent[x] = find(parent[x]);  // 路径压缩
        }
        return parent[x];
    }

// 方法：findRedundantConnection；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> findRedundantConnectionImpl(vector<vector<int>>& edges) {
        int n = edges.size();
        parent.resize(n + 1);  // 节点编号从1开始，开n+1大小
        for (int i = 0; i <= n; i++) {
            parent[i] = i;  // 初始化，每个节点的父节点是自己
        }
        for (auto& edge : edges) {
            int u = edge[0], v = edge[1];
            int rootU = find(u), rootV = find(v);
            if (rootU == rootV) {
                return edge;  // 两端点已同根，这条边会成环，就是多余边
            }
            parent[rootU] = rootV;  // 否则正常合并两个集合
        }
        return {};  // 理论上不会执行到这里（题目保证一定存在多余边）
    }
vector<int> findRedundantConnection(vector<vector<int>>& edges) {
    // 清空上次调用的辅助状态，三组输入可以连续独立运行。
    parent = {};
    return findRedundantConnectionImpl(edges);
}
}

int main() {
    // 用例 1：输入 ([[1, 2], [1, 3], [2, 3]],)；期望 [2, 3]。
    {
        vector<vector<int>> arg0 = {{1, 2}, {1, 3}, {2, 3}};
        auto result = interview::findRedundantConnection(arg0);
        cout << "用例 1: 期望=[2, 3] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([[1, 2], [2, 3], [3, 4], [1, 4], [1, 5]],)；期望 [1, 4]。
    {
        vector<vector<int>> arg0 = {{1, 2}, {2, 3}, {3, 4}, {1, 4}, {1, 5}};
        auto result = interview::findRedundantConnection(arg0);
        cout << "用例 2: 期望=[1, 4] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([[1, 2], [2, 3], [1, 3]],)；期望 [1, 3]。
    {
        vector<vector<int>> arg0 = {{1, 2}, {2, 3}, {1, 3}};
        auto result = interview::findRedundantConnection(arg0);
        cout << "用例 3: 期望=[1, 3] 实际=";
        show(result);
        cout << "\n";
    }
    // 补充边界断言
    { vector<vector<int>> a;
      if (!(interview::findRedundantConnection(a).empty())) { cerr << "空输入或边界结果错误\n"; return 1; }
      cout << "补充边界断言：通过\n"; }
    return 0;
}
```

## 总结

- 咱们这道题的本质就是：树加上一条多余的边，必然会形成一个环，所以判断"多余边"说白了就是判断"这条边的两个端点，在加入它之前是不是已经连通了"。
- 做法上，按数组顺序正向遍历，用并查集逐条做 `union`，一旦遇到两端点已经同根的边，那就是答案了，这个做法天然就满足题目"返回最后出现的多余边"的要求，不用额外处理。
- 这其实是并查集处理"动态加边过程中检测环"这类场景的标准模板，相比每次都用 DFS/BFS 重新判断连通性，效率从 O(e×(n+e)) 直接降到了 O(n)，提升还是很明显的。
- 另外提一下，本题是 685 题（有向图版冗余连接）的基础，你把本题里"逐条 union、检测同根即成环"这个逻辑理解透了，其实就是理解 685 题里"纯环情况"那个分支的前提条件。
