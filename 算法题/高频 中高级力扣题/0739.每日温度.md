> 本文归档自算法题目录，归入“后端高频 Top 50”专题。

# 0739. 每日温度

**题目描述**

给定一个整数数组 `temperatures`，表示每天的气温，返回一个数组 `answer`，其中 `answer[i]` 表示对于第 `i` 天，下一个更高气温出现在几天之后。如果气温在这之后都不会升高，则该位置用 0 代替。

```
示例：
输入: temperatures = [73,74,75,71,69,72,76,73]
输出: [1,1,4,2,1,1,0,0]
解释：
第0天(73°)，下一个更高温度在第1天(74°)，间隔1天
第2天(75°)，下一个更高温度在第6天(76°)，间隔4天
第6天(76°)是全局最高，之后没有更高的，输出0
```

## 思路

- 单调栈：存未找到更暖日的下标，温度从栈底到栈顶非递增。当前温度更高时连续弹出 `j`，令 `answer[j]=i-j`，再将 `i` 入栈；剩余答案为 0。时间 O(n)，空间 O(n)。
- 暴力：每一天向右找首个更高温度。时间 O(n²)，辅助空间 O(1)，不计结果。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。

class Solution:
    # 方法：dailyTemperatures；按题目要求处理输入并返回结果，核心算法见方法体。
    def dailyTemperatures(self, temperatures: list[int]) -> list[int]:
        n = len(temperatures)
        answer = [0] * n          # 默认0，表示后面没有更高的
        stack = []                 # 栈中存下标，温度从栈底到栈顶递减
        for i, cur in enumerate(temperatures):
            # 当前温度比栈顶下标对应温度高，说明栈顶那天等到了更高温度
            while stack and cur > temperatures[stack[-1]]:
                j = stack.pop()
                answer[j] = i - j  # 结算：间隔天数
            stack.append(i)        # 当前下标入栈，等待被结算
        return answer
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：dailyTemperatures；按题目要求处理输入并返回结果，核心算法见方法体。
def dailyTemperatures(temperatures: list[int]) -> list[int]:
    n = len(temperatures)
    answer = [0] * n          # 默认0，表示后面没有更高的
    stack = []                 # 栈中存下标，温度从栈底到栈顶递减
    for i, cur in enumerate(temperatures):
        # 当前温度比栈顶下标对应温度高，说明栈顶那天等到了更高温度
        while stack and cur > temperatures[stack[-1]]:
            j = stack.pop()
            answer[j] = i - j  # 结算：间隔天数
        stack.append(i)        # 当前下标入栈，等待被结算
    return answer


if __name__ == "__main__":
    # 用例 1：输入 ([73, 74, 75, 71, 69, 72, 76, 73],)；期望 [1, 1, 4, 2, 1, 1, 0, 0]。
    arg0 = [73, 74, 75, 71, 69, 72, 76, 73]
    result = dailyTemperatures(arg0)
    expected = [1, 1, 4, 2, 1, 1, 0, 0]
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([30, 40, 50, 60],)；期望 [1, 1, 1, 0]。
    arg0 = [30, 40, 50, 60]
    result = dailyTemperatures(arg0)
    expected = [1, 1, 1, 0]
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([30, 30],)；期望 [0, 0]。
    arg0 = [30, 30]
    result = dailyTemperatures(arg0)
    expected = [0, 0]
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：dailyTemperatures；按题目要求处理输入并返回结果，核心算法见方法体。
func dailyTemperatures(temperatures []int) []int {
    n := len(temperatures)
    answer := make([]int, n)      // 默认0
    stack := make([]int, 0, n)    // 栈中存下标，温度从栈底到栈顶递减
    for i := 0; i < n; i++ {
        // 当前温度比栈顶下标对应温度高，弹出并结算
        for len(stack) > 0 && temperatures[i] > temperatures[stack[len(stack)-1]] {
            j := stack[len(stack)-1]
            stack = stack[:len(stack)-1] // 弹出栈顶
            answer[j] = i - j            // 结算间隔天数
        }
        stack = append(stack, i) // 当前下标入栈
    }
    return answer
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：dailyTemperatures；按题目要求处理输入并返回结果，核心算法见方法体。
func dailyTemperatures(temperatures []int) []int {
    n := len(temperatures)
    answer := make([]int, n)      // 默认0
    stack := make([]int, 0, n)    // 栈中存下标，温度从栈底到栈顶递减
    for i := 0; i < n; i++ {
        // 当前温度比栈顶下标对应温度高，弹出并结算
        for len(stack) > 0 && temperatures[i] > temperatures[stack[len(stack)-1]] {
            j := stack[len(stack)-1]
            stack = stack[:len(stack)-1] // 弹出栈顶
            answer[j] = i - j            // 结算间隔天数
        }
        stack = append(stack, i) // 当前下标入栈
    }
    return answer
}

func main() {
    // 用例 1：输入 ([73, 74, 75, 71, 69, 72, 76, 73],)；期望 [1, 1, 4, 2, 1, 1, 0, 0]。
    {
        arg0 := []int{73, 74, 75, 71, 69, 72, 76, 73}
        result := dailyTemperatures(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[1, 1, 4, 2, 1, 1, 0, 0] 实际=", string(data))
    }
    // 用例 2：输入 ([30, 40, 50, 60],)；期望 [1, 1, 1, 0]。
    {
        arg0 := []int{30, 40, 50, 60}
        result := dailyTemperatures(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[1, 1, 1, 0] 实际=", string(data))
    }
    // 用例 3：输入 ([30, 30],)；期望 [0, 0]。
    {
        arg0 := []int{30, 30}
        result := dailyTemperatures(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[0, 0] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。；stack：C++ STL 栈容器。

class Solution {
public:
    // 方法：dailyTemperatures；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> dailyTemperatures(vector<int>& temperatures) {
        int n = temperatures.size();
        vector<int> answer(n, 0);      // 默认0，表示后面没有更高的
        stack<int> st;                 // 栈中存下标，对应温度从栈底到栈顶递减
        for (int i = 0; i < n; i++) {
            // 当前温度比栈顶下标对应的温度高，说明栈顶那天等到了更高温度
            while (!st.empty() && temperatures[i] > temperatures[st.top()]) {
                int j = st.top();
                st.pop();
                answer[j] = i - j;     // 结算：间隔天数 = 当前下标 - 栈顶下标
            }
            st.push(i);                // 当前下标入栈，等待被后面更高的温度结算
        }
        return answer;
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
vector<int> dailyTemperatures(vector<int>& temperatures);

// 方法：dailyTemperatures；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> dailyTemperatures(vector<int>& temperatures) {
        int n = temperatures.size();
        vector<int> answer(n, 0);      // 默认0，表示后面没有更高的
        stack<int> st;                 // 栈中存下标，对应温度从栈底到栈顶递减
        for (int i = 0; i < n; i++) {
            // 当前温度比栈顶下标对应的温度高，说明栈顶那天等到了更高温度
            while (!st.empty() && temperatures[i] > temperatures[st.top()]) {
                int j = st.top();
                st.pop();
                answer[j] = i - j;     // 结算：间隔天数 = 当前下标 - 栈顶下标
            }
            st.push(i);                // 当前下标入栈，等待被后面更高的温度结算
        }
        return answer;
    }

}

int main() {
    // 用例 1：输入 ([73, 74, 75, 71, 69, 72, 76, 73],)；期望 [1, 1, 4, 2, 1, 1, 0, 0]。
    {
        vector<int> arg0 = {73, 74, 75, 71, 69, 72, 76, 73};
        auto result = interview::dailyTemperatures(arg0);
        cout << "用例 1: 期望=[1, 1, 4, 2, 1, 1, 0, 0] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([30, 40, 50, 60],)；期望 [1, 1, 1, 0]。
    {
        vector<int> arg0 = {30, 40, 50, 60};
        auto result = interview::dailyTemperatures(arg0);
        cout << "用例 2: 期望=[1, 1, 1, 0] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([30, 30],)；期望 [0, 0]。
    {
        vector<int> arg0 = {30, 30};
        auto result = interview::dailyTemperatures(arg0);
        cout << "用例 3: 期望=[0, 0] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 单调栈的本质说白了就是"延迟结算"：遇到还确定不了答案的元素，先压进栈里暂存着，等到未来某个元素能触发结算条件了（这里就是"比栈顶更大"），才一次性批量弹出计算，这样就避免了暴力法里的重复扫描。
- 这道题栈里维护的是下标而不是数值本身，因为最终答案得算下标差（也就是间隔天数），要是只存数值，位置信息就丢了。
- 判断"该入栈还是该弹栈"的关键，就在于比较新元素和栈顶元素的大小关系，这决定了栈该维护成什么单调性方向（递增栈是用来找更小值的，递减栈是用来找更大值的）。这道题要找的是"下一个更大元素"，所以用的是栈内自底向上非递增的写法（也就是说，找到更大值就弹栈）。
- 时间复杂度能从 O(n²) 降到 O(n)，关键就在于每个下标只会被压入和弹出各一次，总的操作次数是线性的。
