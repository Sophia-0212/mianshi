@@ 2
两次二分分别找左右边界：命中时记录下标，找左边界继续向左，找右边界继续向右。未命中返回 `[-1,-1]`。也可用 `bisect_left`、`bisect_right-1` 求边界。

时间 O(log n)，空间 O(1)。

@@ 8
二分查找：维护闭区间 `[left,right]`，初值 `[0,n-1]`。当 `left<=right`，比较中点与目标：相等返回；偏小令 `left=mid+1`，偏大令 `right=mid-1`。区间为空返回 -1。也可用 `bisect_left` 定位后检查是否相等。

时间 O(log n)，空间 O(1)。

@@ 19
双指针从两端向中间交换，直到相遇或交错。时间 O(n)，空间 O(1)。

切片反转后赋回原数组也可实现；时间 O(n)，空间 O(n)。

@@ 37
使用虚拟头节点和 `size`。按下标访问；插入、删除先定位前驱，再修改连接。

`get/delete` 要求 `0<=index<size`；插入允许 `index==size`，负下标按头插处理，超过 `size` 忽略。

头插时间 O(1)，访问、尾插及按下标增删最坏 O(n)；存储空间 O(n)，单次操作辅助空间 O(1)。维护尾指针可将尾插降为 O(1)。

@@ 44
反复计算各位数字平方和：得到 1 则成功，进入不含 1 的循环则失败。

- 哈希集合记录中间数，出现重复即可判环。时间 O(log n)，空间 O(log n) 为上界。
- Floyd 快慢指针：慢指针变换一次，快指针变换两次，相遇后判断是否为 1。时间 O(log n)，辅助空间 O(1)。

固定整数范围下，两者均可视为常数复杂度。

@@ 45
- 双向哈希映射：逐字符检查 `s→t`、`t→s` 均一致，防止不同字符映射到同一字符。平均时间 O(n)，空间 O(k)，k 为字符集大小。
- 集合：检查字符对数、s 不同字符数、t 不同字符数三者相等，保证一一对应。平均时间 O(n)，空间 O(n)。

长度不同直接失败。

@@ 56
双栈：入队写输入栈，出队或查看队头读输出栈。仅当输出栈为空时，将输入栈全部倒入，反转顺序。

入队时间 O(1)，出队/查看最坏 O(n)、均摊 O(1)；空间 O(n)。

@@ 58
先用哈希表统计频次，再选前 `k` 个：

- 小顶堆：堆大小超过 `k` 就弹出最低频元素。平均时间 O(n+m log(k+1))，空间 O(m+k)。
- 大顶堆：全部 `m` 个不同值建堆，弹出 `k` 次。平均时间 O(n+m+k log m)，空间 O(m)。
- 按频次排序后取前 k 个：平均时间 O(n+m log m)，空间 O(m)。
- Python 可调用 `Counter(nums).most_common(k)`。

m 为不同值个数。

@@ 79
遍历时检查左孩子：若非空且没有孩子，累加其值；继续处理左右子树。左叶子须同时满足“左孩子”和“叶子”。

可用递归 DFS 或队列 BFS。时间 O(n)；DFS 空间 O(h)，BFS 空间 O(w)，h 为树高，w 为最大层宽。

@@ 80
按 BST 有序性查找目标，删除后返回新子树根：

- 无孩子：返回空。
- 仅一个孩子：返回该孩子。
- 两个孩子：用右子树最小值替换当前值，再删除该后继节点；也可对称地用左子树最大值替换并删除。

时间 O(h)，递归空间 O(h)，h 为树高。

@@ 101
回溯：每层处理一位数字，依次选择其对应字母；处理完全部数字时保存字符串，返回时撤销选择。空输入返回空列表。也可用 `itertools.product` 枚举各位字母的笛卡尔积。

最坏时间 O(n·4ⁿ)，辅助空间 O(n)，不计结果，n 为数字位数。

@@ 102
回溯：找到空格，尝试 1～9，同行、同列、同九宫格无冲突才填入并递归。失败则清空；某空格全部尝试失败立即返回，棋盘无空格即成功。

搜索时间上界 O(9ᵐ)，栈空间 O(m)，m 为空格数；固定 9×9 棋盘下空间 O(1)。

@@ 105
回溯：每层遍历全部元素，用 `used` 跳过当前路径已用下标；选满 `n` 个保存排列，返回时撤销选择及标记。也可调用 `itertools.permutations`。

时间 O(n·n!)，辅助空间 O(n)，不计结果。

@@ 109
- 回溯：从 `startIndex` 递增选数，下一层从 `i+1` 开始，选满 k 个保存，返回时撤销。未剪枝时间上界 O(2ⁿ+k·C(n,k))，辅助空间 O(k)。
- 库函数：`itertools.combinations` 枚举组合后转列表。时间 O(n+k·C(n,k))，辅助空间 O(n)，不计结果。

@@ 111
回溯：进入每层都保存当前路径，包括空集；枚举后续元素，选入后从 `i+1` 递归，返回时撤销。也可枚举长度 0～n，用 `itertools.combinations` 生成各长度子集。

时间 O(n·2ⁿ)，辅助空间 O(n)，不计结果。

@@ 150
- 区间 DP：`dp[i][j]` 表示 `s[i..j]` 是否回文，初值为假；当两端相等且（长度≤2 或内部回文）时为真。`i` 倒序、`j` 正序，记录最长区间。时间、空间 O(n²)。
- 中心扩展：枚举单字符、双字符中心，向两侧扩展相等字符，记录最长区间。时间 O(n²)，辅助空间 O(1)，返回子串需 O(n) 空间。

@@ 164
DP：`dp[i][j]` 为两串前 `i、j` 个字符的编辑距离。初值 `dp[i][0]=i、dp[0][j]=j`，逐行正序：

- 末字符相同：`dp[i][j]=dp[i-1][j-1]`。
- 不同：`dp[i][j]=1+min(dp[i-1][j],dp[i][j-1],dp[i-1][j-1])`，依次为删除、插入、替换。

返回 `dp[n][m]`。时间、空间 O(nm)；滚动保存两行，空间可降至 O(m)。

@@ 165
DP：`dp[i][j]` 为 `s` 前 `i` 个字符中组成 `t` 前 `j` 个字符的方案数。`dp[i][0]=1`，其余为 0，逐行正序：

- 末字符相同：`dp[i][j]=dp[i-1][j]+dp[i-1][j-1]`，分别为不用、使用当前字符。
- 不同：`dp[i][j]=dp[i-1][j]`。

返回 `dp[n][m]`。时间、空间 O(nm)；一维滚动须按 j 倒序，空间可降至 O(m)。

@@ 169
- LCS：求最长公共子序列长度 `L`，删除其余字符，答案 `n+m-2L`。
- 删除 DP：`dp[i][j]` 为两前缀变相同的最少删除数。边界 `dp[i][0]=i、dp[0][j]=j`；字符相等取左上，不等取 `1+min(dp[i-1][j],dp[i][j-1])`。逐行正序，返回 `dp[n][m]`。

两种方法均为时间、空间 O(nm)；滚动保存两行可将空间降至 O(m)。

@@ 170
- 区间 DP：`dp[i][j]` 表示是否回文，初值为假；两端相等且（长度≤2 或内部回文）时为真。`i` 倒序、`j` 正序，统计真值个数。时间、空间 O(n²)。
- 暴力：枚举所有子串并逐一检查回文。时间 O(n³)，用下标检查时辅助空间 O(1)。

@@ 172
DP：`dp[i][j]` 为分别以两数组下标 `i-1、j-1` 结尾的最长公共连续段长度。边界为 0；元素相等取 `dp[i-1][j-1]+1`，否则清零。逐行正序，返回全部状态最大值。

时间、空间 O(nm)；一维滚动按 j 倒序，不匹配仍清零，空间 O(m)。

@@ 174
DP：`dp[i][j]` 为两串前 `i、j` 个字符的最长公共子序列长度，边界为 0。末字符相同取 `dp[i-1][j-1]+1`，不同取 `max(dp[i-1][j],dp[i][j-1])`。

逐行正序，返回 `dp[n][m]`。时间、空间 O(nm)；滚动保存两行可将空间降至 O(m)。

@@ 180
状态 DP：`hold、cash` 为持有／不持有时的最大现金，初值 `-prices[0]、0`。每天根据旧值更新：`hold'=max(hold,-price)`，`cash'=max(cash,hold+price)`，返回 `cash`。

仅一次交易，买入从 0 元出发。时间 O(n)，滚动空间 O(1)，完整状态表 O(n)。

贪心：维护此前最低价，用今日价减最低价更新最大利润。时间 O(n)，空间 O(1)。

@@ 215
DFS：遍历网格，遇未访问陆地，岛屿数加一；标记并递归其上下左右邻居。越界、水域或已访问处返回。

可用独立 `visited`，或原地把陆地改为水。时间 O(mn)，最坏空间 O(mn)，包括递归栈。BFS 改用队列扩展，时间、最坏空间相同。

@@ 222
先用 DFS/BFS 给每个岛屿编号并记录面积；再枚举水格，将四邻不同岛屿的面积加一，取最大值。邻岛编号须去重，避免重复累计。

全陆地返回 n²，全水返回 1。时间、空间 O(n²)。
