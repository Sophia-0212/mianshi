@@ 207
- 前后缀最大值：每列水量为 `min(左最大,右最大)-当前高度`，累加。时间 O(n)，空间 O(n)。
- 双指针：维护左右已扫描最大高度，每次结算较小最大值所在侧并内移。时间 O(n)，空间 O(1)。
- 单调栈：存下标，高度从栈底到栈顶非递增。遇更高柱弹出槽底，若仍有左边界，累加 `(min(左右高度)-槽底高度)×间距`。时间 O(n)，空间 O(n)。
@@ 208
- 单调栈：存下标，高度从栈底到栈顶非递减。遇更矮柱弹出 `mid`，弹出后栈顶为 `left`（空则 -1），面积为 `heights[mid]*(i-left-1)`。末尾补 0，结算剩余正高度。时间 O(n)，空间 O(n)。
- 暴力：以每根柱为高度，向两边扩到更矮柱前。时间 O(n²)，空间 O(1)。
@@ 209
单调栈扫描 `nums2`，遇更大值时连续弹栈，记录“弹出值→当前值”；未找到的默认 -1。再按 `nums1` 顺序查表。值不重复，因此可用值作键。

平均时间 O(m+n)，空间 O(n)，m、n 为两数组长度。
@@ 210
单调栈存下标，对应值从栈底到栈顶非递增。遍历 `2n` 次，用 `i%n` 模拟循环；遇更大值时弹栈填写答案。仅第一轮入栈，未匹配项保留 -1。

时间 O(n)，空间 O(n)。
@@ 211
- 单调栈：存未找到更暖日的下标，温度从栈底到栈顶非递增。当前温度更高时连续弹出 `j`，令 `answer[j]=i-j`，再将 `i` 入栈；剩余答案为 0。时间 O(n)，空间 O(n)。
- 暴力：每一天向右找首个更高温度。时间 O(n²)，辅助空间 O(1)，不计结果。
@@ 212
BFS：单词为节点，相差一个字母即相邻。从 `beginWord` 分层搜索，枚举每位的 26 种替换，字典中未访问的词入队并立即标记。首次到达终点返回层数，包含起点；终点不在字典或不可达返回 0。

时间 O(NL²)，空间 O(NL)，N 为单词数，L 为词长，含字符串构造与哈希开销。
@@ 213
从四条边界的 `O` 出发，用 DFS/BFS 标记所有连通的 `O`。再次遍历：未标记的 `O` 改为 `X`，已标记的恢复为 `O`。

时间 O(mn)，空间 O(mn)（递归栈或队列）。
@@ 214
BFS：遍历网格，遇未访问陆地，岛屿数加一并入队；不断扩展上下左右相邻陆地，入队时立即标记，避免重复入队。

DFS 也可标记整片陆地。两种方法均为时间 O(mn)，最坏空间 O(mn)。
@@ 215
DFS：遍历网格，遇未访问陆地，岛屿数加一；标记并递归其上下左右邻居。越界、水域或已访问处返回。

可用独立 `visited`，或原地把陆地改为水。时间 O(mn)，最坏空间 O(mn)，包括递归栈。
@@ 216
- 反向多源 DFS/BFS：分别从太平洋的上／左边界、大西洋的下／右边界出发，只向高度不低于当前格的邻居扩展。两次标记的交集即答案。时间、空间 O(mn)。
- 正向逐格搜索：每格单独判断能否流向两海。最坏时间 O(m²n²)，空间 O(mn)。
@@ 217
- 直接统计：每块陆地检查四边，邻接水域或越界就加一。时间 O(mn)，空间 O(1)。
- DFS：访问陆地并标记；遇水域或越界累加一条边，已访问陆地直接返回。时间 O(mn)，空间 O(mn)。

{{CODE0}}
@@ 218
并查集按输入顺序处理边：端点不同根则合并，同根则当前边闭环，返回该边；它是环中最后出现的边。

当前仅路径压缩：时间 O(n log n) 上界，空间 O(n)；再加按秩／大小合并，时间可为 O(nα(n))。
@@ 219
先检查入度：

- 有入度为 2 的节点：优先跳过较晚入边，用并查集检查剩余边。无环则删较晚边，否则删较早边。
- 无入度为 2 的节点：按输入顺序合并端点，首次同根的边即应删除。

当前仅路径压缩：时间 O(n log n) 上界，空间 O(n)；加按秩合并可为 O(nα(n))。
@@ 220
DFS：遇未访问陆地，标记后返回 `1+四邻面积之和`；越界、水域、已访问处返回 0。遍历所有连通块，取最大面积。

时间 O(mn)，空间 O(mn)，含访问标记与递归栈。
@@ 221
DFS＋回溯：路径从 0 开始，依次加入邻居并递归，返回时撤销；到达 `n-1` 保存路径副本。DAG 无环，无需防环标记；不能用永久 `visited` 阻止不同路径共用节点。

最坏时间 O(n·2ⁿ)，辅助空间 O(n)，不计结果。
@@ 222
先用 DFS 给每个岛屿编号并记录面积；再枚举水格，将四邻不同岛屿的面积加一，取最大值。邻岛编号须去重，避免重复累计。

全陆地返回 n²，全水返回 1。时间、空间 O(n²)。
@@ 223
把房间视为节点，钥匙视为有向边。从 0 做 DFS/BFS，访问时标记，最后检查是否访问全部房间。

时间 O(V+E)，辅助空间 O(V)，V 为房间数，E 为钥匙总数。
@@ 224
从边界陆地出发做 DFS/BFS，标记所有与边界连通的陆地；剩余未标记陆地格数即飞地数量。

时间、空间 O(mn)，空间含访问标记及递归栈／队列。
@@ 225
- 并查集：合并每条无向边的端点，比较起终点根是否相同。当前仅路径压缩，时间 O((V+E)log V) 上界，辅助空间 O(V)；加按秩合并可为 O(V+Eα(V))。
- DFS/BFS：建邻接表，从起点搜索，访问到终点即成功。时间、空间 O(V+E)，含建图。
@@ 226
并查集维护不相交集合：`find` 找根，`union` 连接不同根，同根表示连通。

路径压缩把查找路径上的节点直接连到根：

{{CODE0}}

按秩／大小合并将小树接到大树。两者结合后，M 次操作时间 O((N+M)α(N))，空间 O(N)；仅路径压缩可用 O((N+M)log N) 上界。

适合动态加边、连通性查询、无向图判环；不直接处理删边和一般有向图可达性。
@@ 227
BFS 用队列逐层扩展，入队时标记，防止重复。无权图首次到达目标即最短路径；统计层数时先固定本层队列长度。

{{CODE0}}

邻接表遍历时间 O(V+E)，辅助空间 O(V)，图存储 O(V+E)。与 DFS 相比，BFS 更适合无权最短路；两者均可查连通性。
@@ 228
图存储：邻接表空间 O(V+E)，适合遍历邻居；邻接矩阵空间 O(V²)，可 O(1) 判断两点是否直接相连。

DFS 标记当前节点，递归未访问邻居。普通遍历不撤销标记；枚举路径时按题意撤销，允许节点出现在不同路径中。

{{CODE0}}

邻接表遍历时间 O(V+E)，矩阵遍历 O(V²)；辅助空间 O(V)，不计图存储。枚举全部路径可能为指数级。
@@ 229
按 `(二进制1的个数,数值)` 升序排序。

计数可用逐位右移，或反复 `x &= x-1` 消去最低位的 1；也可调用位计数函数。

预计算排序键：时间 O(nw+n log n)，空间 O(n)。比较器每次重算：时间 O(wn log n)，辅助空间由排序决定。w 为整数位数，固定字长时视为常数。
@@ 230
从右向左找首个 `nums[i]<nums[i+1]`，再从右向左找首个大于 `nums[i]` 的值，交换后反转后缀。让尽量靠右的一位最小幅度增大，其后恢复最小顺序。

若无升序对，整体反转。时间 O(n)，空间 O(1)。
@@ 231
- 坐标模拟：左右改变 x，上下改变 y，最终 `x==0 && y==0` 即回到原点。
- 字符计数：判断 `L、R` 次数相等，且 `U、D` 次数相等。

两种方法均为时间 O(n)，空间 O(1)。
