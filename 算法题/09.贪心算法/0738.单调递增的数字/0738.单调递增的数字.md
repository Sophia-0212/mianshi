
# 0738. 单调递增的数字

**题目描述**

给定一个非负整数 `N`，找出并返回小于或等于 `N` 的最大的整数，同时这个整数需要满足其各个位数上的数字是单调递增的。

（当且仅当每个相邻位数上的数字 `x` 和 `y` 满足 `x <= y` 时，我们称这个整数是单调递增的。）

```
示例 1：
输入: N = 10
输出: 9

示例 2：
输入: N = 1234
输出: 1234

示例 3：
输入: N = 332
输出: 299
```

## 思路

- 贪心：从右向左，若前位大于后位，将前位减一并记录后缀起点；最终把该起点及之后全部改成 9，得到不超过原数的最大合法值。时间 O(d)，空间 O(d)，d 为位数。
- 递减枚举：从 N 向下逐个检查各位是否非递减。时间 O(Nd)，辅助空间 O(1)。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：monotoneIncreasingDigits；按题目要求处理输入并返回结果，核心算法见方法体。
    def monotoneIncreasingDigits(self, N: int) -> int:
        s = list(str(N))            # 转成字符列表方便按位修改
        flag_idx = len(s)           # 记录从哪个下标开始需要置9，默认不处理
        for i in range(len(s) - 1, 0, -1):  # 从右往左遍历，比较相邻两位
            if s[i - 1] > s[i]:      # 前一位比当前位大，出现断点
                s[i - 1] = str(int(s[i - 1]) - 1)  # 前一位减1消除断点
                flag_idx = i         # 记录断点位置
        for i in range(flag_idx, len(s)):
            s[i] = '9'               # 断点之后全部置9，最大化数值
        return int(''.join(s))```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：monotoneIncreasingDigits；按题目要求处理输入并返回结果，核心算法见方法体。
def monotoneIncreasingDigits(N: int) -> int:
    s = list(str(N))            # 转成字符列表方便按位修改
    flag_idx = len(s)           # 记录从哪个下标开始需要置9，默认不处理
    for i in range(len(s) - 1, 0, -1):  # 从右往左遍历，比较相邻两位
        if s[i - 1] > s[i]:      # 前一位比当前位大，出现断点
            s[i - 1] = str(int(s[i - 1]) - 1)  # 前一位减1消除断点
            flag_idx = i         # 记录断点位置
    for i in range(flag_idx, len(s)):
        s[i] = '9'               # 断点之后全部置9，最大化数值
    return int(''.join(s))


if __name__ == "__main__":
    # 用例 1：输入 (10,)；期望 9。
    arg0 = 10
    result = monotoneIncreasingDigits(arg0)
    expected = 9
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 (332,)；期望 299。
    arg0 = 332
    result = monotoneIncreasingDigits(arg0)
    expected = 299
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 (1234,)；期望 1234。
    arg0 = 1234
    result = monotoneIncreasingDigits(arg0)
    expected = 1234
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
// 方法：monotoneIncreasingDigits；按题目要求处理输入并返回结果，核心算法见方法体。
func monotoneIncreasingDigits(N int) int {
    s := []byte(strconv.Itoa(N)) // 转成字节切片方便按位修改
    flagIdx := len(s)             // 记录需要置9的起始下标，默认不处理
    for i := len(s) - 1; i > 0; i-- { // 从右往左遍历
        if s[i-1] > s[i] { // 出现递减断点
            s[i-1]-- // 前一位减1消除断点（字节运算，'3'-1='2'）
            flagIdx = i // 记录断点位置
        }
    }
    for i := flagIdx; i < len(s); i++ {
        s[i] = '9' // 断点之后全部置9
    }
    result, _ := strconv.Atoi(string(s))
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。；Go 标准库 strconv：字符串与数字转换。
package main

import (
    "encoding/json"
    "fmt"
    "strconv"
)

// 普通函数和题目中的核心算法。
// 方法：monotoneIncreasingDigits；按题目要求处理输入并返回结果，核心算法见方法体。
func monotoneIncreasingDigits(N int) int {
    s := []byte(strconv.Itoa(N)) // 转成字节切片方便按位修改
    flagIdx := len(s)             // 记录需要置9的起始下标，默认不处理
    for i := len(s) - 1; i > 0; i-- { // 从右往左遍历
        if s[i-1] > s[i] { // 出现递减断点
            s[i-1]-- // 前一位减1消除断点（字节运算，'3'-1='2'）
            flagIdx = i // 记录断点位置
        }
    }
    for i := flagIdx; i < len(s); i++ {
        s[i] = '9' // 断点之后全部置9
    }
    result, _ := strconv.Atoi(string(s))
    return result
}

func main() {
    // 用例 1：输入 (10,)；期望 9。
    {
        arg0 := 10
        result := monotoneIncreasingDigits(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=9 实际=", string(data))
    }
    // 用例 2：输入 (332,)；期望 299。
    {
        arg0 := 332
        result := monotoneIncreasingDigits(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=299 实际=", string(data))
    }
    // 用例 3：输入 (1234,)；期望 1234。
    {
        arg0 := 1234
        result := monotoneIncreasingDigits(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=1234 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
public:
    // 方法：monotoneIncreasingDigits；按题目要求处理输入并返回结果，核心算法见方法体。
    int monotoneIncreasingDigits(int N) {
        string s = to_string(N);              // 转成字符串方便按位处理
        int flagIdx = s.size();                // 记录从哪个下标开始需要置9，初始设为size（表示不需要置9）
        for (int i = s.size() - 1; i > 0; i--) { // 从右往左遍历，比较相邻两位
            if (s[i - 1] > s[i]) {              // 前一位比当前位大，出现断点
                s[i - 1]--;                      // 前一位减1，消除断点
                flagIdx = i;                     // 记录断点位置，之后统一处理
            }
        }
        for (int i = flagIdx; i < s.size(); i++) {
            s[i] = '9';                          // 断点之后全部置为9，让数值最大化
        }
        return stoi(s);
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
int monotoneIncreasingDigits(int N);

// 方法：monotoneIncreasingDigits；按题目要求处理输入并返回结果，核心算法见方法体。
    int monotoneIncreasingDigits(int N) {
        string s = to_string(N);              // 转成字符串方便按位处理
        int flagIdx = s.size();                // 记录从哪个下标开始需要置9，初始设为size（表示不需要置9）
        for (int i = s.size() - 1; i > 0; i--) { // 从右往左遍历，比较相邻两位
            if (s[i - 1] > s[i]) {              // 前一位比当前位大，出现断点
                s[i - 1]--;                      // 前一位减1，消除断点
                flagIdx = i;                     // 记录断点位置，之后统一处理
            }
        }
        for (int i = flagIdx; i < s.size(); i++) {
            s[i] = '9';                          // 断点之后全部置为9，让数值最大化
        }
        return stoi(s);
    }

}

int main() {
    // 用例 1：输入 (10,)；期望 9。
    {
        int arg0 = 10;
        auto result = interview::monotoneIncreasingDigits(arg0);
        cout << "用例 1: 期望=9 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 (332,)；期望 299。
    {
        int arg0 = 332;
        auto result = interview::monotoneIncreasingDigits(arg0);
        cout << "用例 2: 期望=299 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 (1234,)；期望 1234。
    {
        int arg0 = 1234;
        auto result = interview::monotoneIncreasingDigits(arg0);
        cout << "用例 3: 期望=1234 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 本题的核心是"从后往前扫描，遇到递减断点就把前一位减1、断点之后全部置9"的贪心策略，本质上是在满足单调递增和不超过原数的约束下，让每一位尽可能取最大值。
- 必须从后往前遍历的原因在于：修改前一位可能导致新的断点出现在更靠前的位置，而从后往前的遍历顺序恰好跟"需要重新检查"的方向一致，能自然地把连锁反应纳入后续比较，不需要额外回退逻辑；从前往后遍历则会跳过已修改位可能引发的新断点，得到错误结果。
- 用 332 → 329 → 299 这个例子可以清晰地看到连锁反应是怎么一步步向左传播、直到没有新断点产生为止的。
- 该题是贪心算法中"局部调整不会破坏全局最优性，且调整顺序至关重要"的典型例子，遍历方向的选择直接决定了算法的正确性。
