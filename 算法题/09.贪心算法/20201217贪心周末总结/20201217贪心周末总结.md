# 贪心算法周末总结（2020.12.17）

- 加油站：累计余量为负就重置起点，总余量非负才有解。时间 O(n)，空间 O(1)。
- 分发糖果：先向右满足左邻约束，再向左取最大值满足右邻约束。时间 O(n)，空间 O(n)。
- 重建队列：身高降序、同高 k 升序，再插入第 k 位；后加入的矮者不影响高者计数。时间 O(n²)，空间 O(n)。

多个约束先确定处理顺序；后一次处理须保留先前已满足的条件。
