# 0239. 滑动窗口最大值

**题目描述**

给定一个整数数组 `nums` 和一个窗口大小 `k`，窗口从数组最左侧开始，每次向右滑动一位，直到到达数组最右侧。返回每次窗口滑动后窗口中的最大值。

```
示例：
输入: nums = [1,3,-1,-3,5,3,6,7], k = 3
输出: [3,3,5,5,6,7]
解释:
窗口位置                最大值
---------------        -----
[1  3  -1] -3  5  3  6  7       3
 1 [3  -1  -3] 5  3  6  7       3
 1  3 [-1  -3  5] 3  6  7       5
 1  3  -1 [-3  5  3] 6  7       5
 1  3  -1  -3 [5  3  6] 7       6
 1  3  -1  -3  5 [3  6  7]      7
```

要求：`1 <= k <= nums.length`，返回长度为 `n - k + 1` 的结果数组。

## 思路

- 单调双端队列：存下标，对应值从队头到队尾非递增。新值入队前弹出更小的队尾，滑出窗口的下标从队头删除；窗口满后取队头值。时间 O(n)，辅助空间 O(k)，不计结果。
- 暴力：每个窗口重新求最大值。时间 O(nk)，辅助空间 O(1)。



## Python

### LeetCode 写法与原有示例

Python 的 `collections.deque` 是双端队列的标准库实现，两端操作都是 O(1)，这里直接使用库解；单调队列的逻辑本身仍需手写（标准库不会帮你维护单调性）。

```python
from collections import deque

# 依赖说明：Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。

class Solution:
    # 方法：maxSlidingWindow；按题目要求处理输入并返回结果，核心算法见方法体。
    def maxSlidingWindow(self, nums: list[int], k: int) -> list[int]:
        dq = deque()  # 存下标，保持对应值单调递减
        result = []

        for i, num in enumerate(nums):
            # 步骤1：从队尾淘汰所有小于新元素的下标
            while dq and nums[dq[-1]] < num:
                dq.pop()
            dq.append(i)

            # 步骤2：淘汰滑出窗口左边界的队头
            if dq[0] <= i - k:
                dq.popleft()

            # 步骤3：窗口形成后记录队头对应的最大值
            if i >= k - 1:
                result.append(nums[dq[0]])

        return result
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
from collections import deque

# 方法：maxSlidingWindow；按题目要求处理输入并返回结果，核心算法见方法体。
def maxSlidingWindow(nums: list[int], k: int) -> list[int]:
    dq = deque()  # 存下标，保持对应值单调递减
    result = []

    for i, num in enumerate(nums):
        # 步骤1：从队尾淘汰所有小于新元素的下标
        while dq and nums[dq[-1]] < num:
            dq.pop()
        dq.append(i)

        # 步骤2：淘汰滑出窗口左边界的队头
        if dq[0] <= i - k:
            dq.popleft()

        # 步骤3：窗口形成后记录队头对应的最大值
        if i >= k - 1:
            result.append(nums[dq[0]])

    return result


if __name__ == "__main__":
    # 用例 1：输入 ([1, 3, -1, -3, 5, 3, 6, 7], 3)；期望 [3, 3, 5, 5, 6, 7]。
    arg0 = [1, 3, -1, -3, 5, 3, 6, 7]
    arg1 = 3
    result = maxSlidingWindow(arg0, arg1)
    expected = [3, 3, 5, 5, 6, 7]
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([1], 1)；期望 [1]。
    arg0 = [1]
    arg1 = 1
    result = maxSlidingWindow(arg0, arg1)
    expected = [1]
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([9, 2, 5], 3)；期望 [9]。
    arg0 = [9, 2, 5]
    arg1 = 3
    result = maxSlidingWindow(arg0, arg1)
    expected = [9]
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：maxSlidingWindow；按题目要求处理输入并返回结果，核心算法见方法体。
func maxSlidingWindow(nums []int, k int) []int {
    var dq []int // 双端队列，存下标，保持对应值单调递减（用 slice 模拟）
    result := make([]int, 0, len(nums)-k+1)

    for i, num := range nums {
        // 步骤1：从队尾淘汰所有小于新元素的下标
        for len(dq) > 0 && nums[dq[len(dq)-1]] < num {
            dq = dq[:len(dq)-1] // 弹出队尾
        }
        dq = append(dq, i) // 新下标入队尾

        // 步骤2：淘汰滑出窗口左边界的队头
        if dq[0] <= i-k {
            dq = dq[1:] // 弹出队头
        }

        // 步骤3：窗口形成后记录队头对应的最大值
        if i >= k-1 {
            result = append(result, nums[dq[0]])
        }
    }
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：maxSlidingWindow；按题目要求处理输入并返回结果，核心算法见方法体。
func maxSlidingWindow(nums []int, k int) []int {
    var dq []int // 双端队列，存下标，保持对应值单调递减（用 slice 模拟）
    result := make([]int, 0, len(nums)-k+1)

    for i, num := range nums {
        // 步骤1：从队尾淘汰所有小于新元素的下标
        for len(dq) > 0 && nums[dq[len(dq)-1]] < num {
            dq = dq[:len(dq)-1] // 弹出队尾
        }
        dq = append(dq, i) // 新下标入队尾

        // 步骤2：淘汰滑出窗口左边界的队头
        if dq[0] <= i-k {
            dq = dq[1:] // 弹出队头
        }

        // 步骤3：窗口形成后记录队头对应的最大值
        if i >= k-1 {
            result = append(result, nums[dq[0]])
        }
    }
    return result
}

func main() {
    // 用例 1：输入 ([1, 3, -1, -3, 5, 3, 6, 7], 3)；期望 [3, 3, 5, 5, 6, 7]。
    {
        arg0 := []int{1, 3, -1, -3, 5, 3, 6, 7}
        arg1 := 3
        result := maxSlidingWindow(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[3, 3, 5, 5, 6, 7] 实际=", string(data))
    }
    // 用例 2：输入 ([1], 1)；期望 [1]。
    {
        arg0 := []int{1}
        arg1 := 1
        result := maxSlidingWindow(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[1] 实际=", string(data))
    }
    // 用例 3：输入 ([9, 2, 5], 3)；期望 [9]。
    {
        arg0 := []int{9, 2, 5}
        arg1 := 3
        result := maxSlidingWindow(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[9] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。

class Solution {
public:
    // 方法：maxSlidingWindow；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        deque<int> dq;  // 双端队列，存下标，保持对应值单调递减
        vector<int> result;

        for (int i = 0; i < nums.size(); i++) {
            // 步骤1：新元素入队前，从队尾淘汰所有小于它的元素
            while (!dq.empty() && nums[dq.back()] < nums[i]) {
                dq.pop_back();
            }
            dq.push_back(i);

            // 步骤2：淘汰滑出窗口左边界的队头
            if (dq.front() <= i - k) {
                dq.pop_front();
            }

            // 步骤3：窗口形成后，队头即当前窗口最大值
            if (i >= k - 1) {
                result.push_back(nums[dq.front()]);
            }
        }
        return result;
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
vector<int> maxSlidingWindow(vector<int>& nums, int k);

// 方法：maxSlidingWindow；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        deque<int> dq;  // 双端队列，存下标，保持对应值单调递减
        vector<int> result;

        for (int i = 0; i < nums.size(); i++) {
            // 步骤1：新元素入队前，从队尾淘汰所有小于它的元素
            while (!dq.empty() && nums[dq.back()] < nums[i]) {
                dq.pop_back();
            }
            dq.push_back(i);

            // 步骤2：淘汰滑出窗口左边界的队头
            if (dq.front() <= i - k) {
                dq.pop_front();
            }

            // 步骤3：窗口形成后，队头即当前窗口最大值
            if (i >= k - 1) {
                result.push_back(nums[dq.front()]);
            }
        }
        return result;
    }

}

int main() {
    // 用例 1：输入 ([1, 3, -1, -3, 5, 3, 6, 7], 3)；期望 [3, 3, 5, 5, 6, 7]。
    {
        vector<int> arg0 = {1, 3, -1, -3, 5, 3, 6, 7};
        int arg1 = 3;
        auto result = interview::maxSlidingWindow(arg0, arg1);
        cout << "用例 1: 期望=[3, 3, 5, 5, 6, 7] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([1], 1)；期望 [1]。
    {
        vector<int> arg0 = {1};
        int arg1 = 1;
        auto result = interview::maxSlidingWindow(arg0, arg1);
        cout << "用例 2: 期望=[1] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([9, 2, 5], 3)；期望 [9]。
    {
        vector<int> arg0 = {9, 2, 5};
        int arg1 = 3;
        auto result = interview::maxSlidingWindow(arg0, arg1);
        cout << "用例 3: 期望=[9] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 单调队列这东西说白了就是"只保留未来还有可能成为最大值的候选元素"，靠在队尾淘汰掉"更早进入窗口但值更小"的元素，把每个元素的比较次数分摊到 O(1)，整体就做到 O(n) 了。
- 队列里为什么存下标而不是值？因为你得通过下标才能判断这个元素是不是已经滑出窗口了，这是维护窗口左边界淘汰逻辑的前提，这点一定要记住。
- 两个淘汰方向千万不能搞混：队尾淘汰是为了维护单调性，淘汰的是"没用的小元素"；队头淘汰是为了维护窗口范围，淘汰的是"过期的元素"。这两者要维护的目标完全不同，缺一不可。
- 还有一点要提一下，单调队列和后面会讲到的单调栈是两种不同的工具，别搞混了：单调栈通常解决的是"下一个更大/更小元素"这类问题，只需要维护一端；单调队列则专门用来解决"滑动窗口最值"这类问题，需要双端操作。适用场景不一样，不要混着用。
