# 122. 买卖股票的最佳时机 II（动态规划）

**题目描述**

给定一个数组 `prices`，其中 `prices[i]` 表示某支股票第 `i` 天的价格。可以进行多次买卖，但每次买入前必须先把之前买的卖出（即不能同时持有多份），求能获得的最大利润。

```
示例：
输入: prices = [7,1,5,3,6,4]
输出: 7
解释: 第2天买入(1)，第3天卖出(5)，利润4；第4天买入(3)，第5天卖出(6)，利润3。总利润 = 4 + 3 = 7。
```

这篇我用动态规划（状态机 DP）来讲这道题，跟常见的"利润拆解"贪心解法不一样——贪心是把整体涨幅拆成每天涨跌的和，只累加正数，这个思路更简洁，但只适用于"不限交易次数"这种最宽松的场景；一旦题目加上交易次数限制，比如后面的 123、188 题，贪心的"利润拆解"就不好使了，必须靠状态机 DP。所以我这里重点讲 DP，贪心只做个简单对比。

## 思路

状态 DP：持有 `hold`、不持有 `cash`，初值 `-prices[0]、0`。每天用旧状态更新：`hold'=max(hold,cash-price)`，`cash'=max(cash,hold+price)`。允许多次交易，买入沿用已有利润，返回 `cash`。

时间 O(n)，滚动空间 O(1)，完整状态表 O(n)。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；max：Python 内置函数：返回最大值。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
    def maxProfit(self, prices: list[int]) -> int:
        n = len(prices)
        if n == 0:
            return 0
        dp0, dp1 = -prices[0], 0  # dp0持有 dp1不持有，第一天初始化
        for i in range(1, n):
            # 持有：延续昨天 或 用昨天不持有的现金今天买入（可反复交易）
            new_dp0 = max(dp0, dp1 - prices[i])
            # 不持有：延续昨天 或 今天卖出
            new_dp1 = max(dp1, dp0 + prices[i])
            dp0, dp1 = new_dp0, new_dp1  # 滚动更新
        return dp1```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；max：Python 内置函数：返回最大值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
def maxProfit(prices: list[int]) -> int:
    n = len(prices)
    if n == 0:
        return 0
    dp0, dp1 = -prices[0], 0  # dp0持有 dp1不持有，第一天初始化
    for i in range(1, n):
        # 持有：延续昨天 或 用昨天不持有的现金今天买入（可反复交易）
        new_dp0 = max(dp0, dp1 - prices[i])
        # 不持有：延续昨天 或 今天卖出
        new_dp1 = max(dp1, dp0 + prices[i])
        dp0, dp1 = new_dp0, new_dp1  # 滚动更新
    return dp1


if __name__ == "__main__":
    # 用例 1：输入 ([7, 1, 5, 3, 6, 4],)；期望 7。
    arg0 = [7, 1, 5, 3, 6, 4]
    result = maxProfit(arg0)
    expected = 7
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([1, 2, 3, 4, 5],)；期望 4。
    arg0 = [1, 2, 3, 4, 5]
    result = maxProfit(arg0)
    expected = 4
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([7, 6, 4, 3, 1],)；期望 0。
    arg0 = [7, 6, 4, 3, 1]
    result = maxProfit(arg0)
    expected = 0
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
// 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
func maxProfit(prices []int) int {
    n := len(prices)
    if n == 0 {
        return 0
    }
    dp0, dp1 := -prices[0], 0 // dp0持有 dp1不持有，第一天初始化
    for i := 1; i < n; i++ {
        // 持有：延续昨天 或 用昨天不持有的现金今天买入
        newDp0 := max(dp0, dp1-prices[i])
        // 不持有：延续昨天 或 今天卖出
        newDp1 := max(dp1, dp0+prices[i])
        dp0, dp1 = newDp0, newDp1 // 滚动更新
    }
    return dp1
}

// 方法：max；按题目要求处理输入并返回结果，核心算法见方法体。
func max(a, b int) int {
    if a > b {
        return a
    }
    return b
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
func maxProfit(prices []int) int {
    n := len(prices)
    if n == 0 {
        return 0
    }
    dp0, dp1 := -prices[0], 0 // dp0持有 dp1不持有，第一天初始化
    for i := 1; i < n; i++ {
        // 持有：延续昨天 或 用昨天不持有的现金今天买入
        newDp0 := max(dp0, dp1-prices[i])
        // 不持有：延续昨天 或 今天卖出
        newDp1 := max(dp1, dp0+prices[i])
        dp0, dp1 = newDp0, newDp1 // 滚动更新
    }
    return dp1
}

// 方法：max；按题目要求处理输入并返回结果，核心算法见方法体。
func max(a, b int) int {
    if a > b {
        return a
    }
    return b
}

func main() {
    // 用例 1：输入 ([7, 1, 5, 3, 6, 4],)；期望 7。
    {
        arg0 := []int{7, 1, 5, 3, 6, 4}
        result := maxProfit(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=7 实际=", string(data))
    }
    // 用例 2：输入 ([1, 2, 3, 4, 5],)；期望 4。
    {
        arg0 := []int{1, 2, 3, 4, 5}
        result := maxProfit(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=4 实际=", string(data))
    }
    // 用例 3：输入 ([7, 6, 4, 3, 1],)；期望 0。
    {
        arg0 := []int{7, 6, 4, 3, 1}
        result := maxProfit(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=0 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
public:
    // 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
    int maxProfit(vector<int>& prices) {
        int n = prices.size();
        if (n == 0) return 0;
        vector<vector<int>> dp(n, vector<int>(2, 0)); // dp[i][0]持有 dp[i][1]不持有
        dp[0][0] = -prices[0]; // 第一天买入
        dp[0][1] = 0;          // 第一天不买
        for (int i = 1; i < n; i++) {
            // 持有：延续昨天 或 用昨天不持有的利润今天买入（可反复买卖）
            dp[i][0] = max(dp[i - 1][0], dp[i - 1][1] - prices[i]);
            // 不持有：延续昨天 或 今天卖出
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][0] + prices[i]);
        }
        return dp[n - 1][1]; // 最后一天不持有即为最大利润
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
int maxProfit(vector<int>& prices);

// 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
    int maxProfit(vector<int>& prices) {
        int n = prices.size();
        if (n == 0) return 0;
        vector<vector<int>> dp(n, vector<int>(2, 0)); // dp[i][0]持有 dp[i][1]不持有
        dp[0][0] = -prices[0]; // 第一天买入
        dp[0][1] = 0;          // 第一天不买
        for (int i = 1; i < n; i++) {
            // 持有：延续昨天 或 用昨天不持有的利润今天买入（可反复买卖）
            dp[i][0] = max(dp[i - 1][0], dp[i - 1][1] - prices[i]);
            // 不持有：延续昨天 或 今天卖出
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][0] + prices[i]);
        }
        return dp[n - 1][1]; // 最后一天不持有即为最大利润
    }

}

int main() {
    // 用例 1：输入 ([7, 1, 5, 3, 6, 4],)；期望 7。
    {
        vector<int> arg0 = {7, 1, 5, 3, 6, 4};
        auto result = interview::maxProfit(arg0);
        cout << "用例 1: 期望=7 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([1, 2, 3, 4, 5],)；期望 4。
    {
        vector<int> arg0 = {1, 2, 3, 4, 5};
        auto result = interview::maxProfit(arg0);
        cout << "用例 2: 期望=4 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([7, 6, 4, 3, 1],)；期望 0。
    {
        vector<int> arg0 = {7, 6, 4, 3, 1};
        auto result = interview::maxProfit(arg0);
        cout << "用例 3: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 122 题的状态定义跟 121 题完全一致（持有/不持有两种），差异只体现在"买入"这一项的递推公式上，说明状态维度的设计取决于"是否需要区分更多局面"，而不是取决于交易次数本身。
- 核心区别在这里：121 题买入基准固定为 0（`-prices[i]`），因为只有一次机会；122 题买入基准是滚动的 `dp[i-1][1]`（历史最优不持有现金），因为可以反复交易，每次买入都要站在之前积累的利润上出发。
- "卖出"的递推公式在两题中完全相同，为什么呢？因为卖出动作本身不受交易次数限制的影响，只有"买入的本金来源"会随约束变化。
- 贪心的"利润拆解"解法（只累加每日正涨幅）在本题中跟 DP 结果等价且更简洁，但它依赖"不限次数"这个宽松条件；一旦加上次数限制，贪心思路就失效了，DP 状态机才是唯一能自然扩展的框架。
