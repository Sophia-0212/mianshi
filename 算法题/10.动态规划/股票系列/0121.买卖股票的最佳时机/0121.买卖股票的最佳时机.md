# 121. 买卖股票的最佳时机

**题目描述**

给定一个数组 `prices`，其中 `prices[i]` 表示某支股票第 `i` 天的价格。规定只能进行一次买入和一次卖出（必须先买后卖，即卖出的那一天一定在买入的那一天之后），求能获得的最大利润。如果无法获利，返回 0。

```
示例：
输入: prices = [7,1,5,3,6,4]
输出: 5
解释: 第 2 天（价格 = 1）买入，第 5 天（价格 = 6）卖出，利润 = 6 - 1 = 5。
```

## 思路

状态 DP：`hold、cash` 为持有／不持有时的最大现金，初值 `-prices[0]、0`。每天根据旧值更新：`hold'=max(hold,-price)`，`cash'=max(cash,hold+price)`，返回 `cash`。

仅一次交易，买入从 0 元出发。时间 O(n)，滚动空间 O(1)，完整状态表 O(n)。

贪心：维护此前最低价，用今日价减最低价更新最大利润。时间 O(n)，空间 O(1)。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；max：Python 内置函数：返回最大值。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
    def maxProfit(self, prices: list[int]) -> int:
        n = len(prices)
        if n == 0:
            return 0
        # dp[0]表示持有状态现金，dp[1]表示不持有状态现金
        dp0, dp1 = -prices[0], 0  # 第一天买入 / 第一天不买
        for i in range(1, n):
            # 持有：延续昨天持有 或 今天以0基准买入（只能买一次）
            new_dp0 = max(dp0, -prices[i])
            # 不持有：延续昨天不持有 或 今天用昨天的持有状态卖出
            new_dp1 = max(dp1, dp0 + prices[i])
            dp0, dp1 = new_dp0, new_dp1  # 滚动更新，省去二维数组
        return dp1  # 最终不持有状态即最大利润```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；max：Python 内置函数：返回最大值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
def maxProfit(prices: list[int]) -> int:
    n = len(prices)
    if n == 0:
        return 0
    # dp[0]表示持有状态现金，dp[1]表示不持有状态现金
    dp0, dp1 = -prices[0], 0  # 第一天买入 / 第一天不买
    for i in range(1, n):
        # 持有：延续昨天持有 或 今天以0基准买入（只能买一次）
        new_dp0 = max(dp0, -prices[i])
        # 不持有：延续昨天不持有 或 今天用昨天的持有状态卖出
        new_dp1 = max(dp1, dp0 + prices[i])
        dp0, dp1 = new_dp0, new_dp1  # 滚动更新，省去二维数组
    return dp1  # 最终不持有状态即最大利润


if __name__ == "__main__":
    # 用例 1：输入 ([7, 1, 5, 3, 6, 4],)；期望 5。
    arg0 = [7, 1, 5, 3, 6, 4]
    result = maxProfit(arg0)
    expected = 5
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([7, 6, 4, 3, 1],)；期望 0。
    arg0 = [7, 6, 4, 3, 1]
    result = maxProfit(arg0)
    expected = 0
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([1],)；期望 0。
    arg0 = [1]
    result = maxProfit(arg0)
    expected = 0
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
// 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
func maxProfit(prices []int) int {
    n := len(prices)
    if n == 0 {
        return 0
    }
    dp0, dp1 := -prices[0], 0 // dp0持有 dp1不持有，第一天初始化
    for i := 1; i < n; i++ {
        // 持有：延续昨天 或 以0为基准今天买入
        newDp0 := max(dp0, -prices[i])
        // 不持有：延续昨天 或 今天卖出昨天持有的股票
        newDp1 := max(dp1, dp0+prices[i])
        dp0, dp1 = newDp0, newDp1 // 滚动更新
    }
    return dp1 // 最大利润
}

// 方法：max；按题目要求处理输入并返回结果，核心算法见方法体。
func max(a, b int) int {
    if a > b {
        return a
    }
    return b
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
func maxProfit(prices []int) int {
    n := len(prices)
    if n == 0 {
        return 0
    }
    dp0, dp1 := -prices[0], 0 // dp0持有 dp1不持有，第一天初始化
    for i := 1; i < n; i++ {
        // 持有：延续昨天 或 以0为基准今天买入
        newDp0 := max(dp0, -prices[i])
        // 不持有：延续昨天 或 今天卖出昨天持有的股票
        newDp1 := max(dp1, dp0+prices[i])
        dp0, dp1 = newDp0, newDp1 // 滚动更新
    }
    return dp1 // 最大利润
}

// 方法：max；按题目要求处理输入并返回结果，核心算法见方法体。
func max(a, b int) int {
    if a > b {
        return a
    }
    return b
}

func main() {
    // 用例 1：输入 ([7, 1, 5, 3, 6, 4],)；期望 5。
    {
        arg0 := []int{7, 1, 5, 3, 6, 4}
        result := maxProfit(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=5 实际=", string(data))
    }
    // 用例 2：输入 ([7, 6, 4, 3, 1],)；期望 0。
    {
        arg0 := []int{7, 6, 4, 3, 1}
        result := maxProfit(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=0 实际=", string(data))
    }
    // 用例 3：输入 ([1],)；期望 0。
    {
        arg0 := []int{1}
        result := maxProfit(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=0 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
public:
    // 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
    int maxProfit(vector<int>& prices) {
        int n = prices.size();
        if (n == 0) return 0;
        vector<vector<int>> dp(n, vector<int>(2, 0)); // dp[i][0]持有 dp[i][1]不持有
        dp[0][0] = -prices[0]; // 第一天买入，花掉本金
        dp[0][1] = 0;          // 第一天不买，现金为0
        for (int i = 1; i < n; i++) {
            // 持有：昨天已持有 或 今天从0基准买入（只能买一次）
            dp[i][0] = max(dp[i - 1][0], -prices[i]);
            // 不持有：昨天已不持有 或 今天卖出昨天持有的股票
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][0] + prices[i]);
        }
        return dp[n - 1][1]; // 最后一天不持有状态即为最大利润
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
int maxProfit(vector<int>& prices);

// 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
    int maxProfit(vector<int>& prices) {
        int n = prices.size();
        if (n == 0) return 0;
        vector<vector<int>> dp(n, vector<int>(2, 0)); // dp[i][0]持有 dp[i][1]不持有
        dp[0][0] = -prices[0]; // 第一天买入，花掉本金
        dp[0][1] = 0;          // 第一天不买，现金为0
        for (int i = 1; i < n; i++) {
            // 持有：昨天已持有 或 今天从0基准买入（只能买一次）
            dp[i][0] = max(dp[i - 1][0], -prices[i]);
            // 不持有：昨天已不持有 或 今天卖出昨天持有的股票
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][0] + prices[i]);
        }
        return dp[n - 1][1]; // 最后一天不持有状态即为最大利润
    }

}

int main() {
    // 用例 1：输入 ([7, 1, 5, 3, 6, 4],)；期望 5。
    {
        vector<int> arg0 = {7, 1, 5, 3, 6, 4};
        auto result = interview::maxProfit(arg0);
        cout << "用例 1: 期望=5 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([7, 6, 4, 3, 1],)；期望 0。
    {
        vector<int> arg0 = {7, 6, 4, 3, 1};
        auto result = interview::maxProfit(arg0);
        cout << "用例 2: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([1],)；期望 0。
    {
        vector<int> arg0 = {1};
        auto result = interview::maxProfit(arg0);
        cout << "用例 3: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 状态机 DP 最关键的第一步，就是想清楚"状态"到底是什么：本题只有"持有/不持有"两种，用 `dp[i][0]`、`dp[i][1]` 分别表示，之后所有股票系列题基本都是在这个基础上加状态维度。
- 递推公式的核心思路是"今天的状态是从昨天的哪些状态转移过来的"：持有来自"延续持有"或者"买入"，不持有来自"延续不持有"或者"卖出"，这套转移逻辑是通用的。
- 本题"只能买一次"体现在哪呢？就体现在买入公式里用的是固定基准 `-prices[i]`，而不是"昨天不持有的现金 - prices[i]"，这是跟 122 题最本质的区别，也是你判断一道股票题属于哪种子类型的关键线索。
- 贪心解法（维护历史最低价）在这道题上和 DP 是等价的，但说到底它只是个特例简化，状态机 DP 才是真正能推广到多次交易、限次交易、含手续费等各种变体的通用框架。
