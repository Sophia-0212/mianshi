# 309. 最佳买卖股票时机含冷冻期

**题目描述**

给定一个数组 `prices`，其中 `prices[i]` 表示某支股票第 `i` 天的价格。可以完成任意多次买入和卖出（不限次数，但同一时刻最多持有一股，必须先卖出才能再买入），唯一的额外限制是：卖出股票后，第二天处于"冷冻期"，冷冻期这一天不能买入股票（可以正常进入下一个冷冻期之后的交易）。求能获得的最大利润。

```
示例：
输入: prices = [1,2,3,0,2]
输出: 3
解释: 对应的一种交易方案为：第 1 天买入(price=1)，第 2 天卖出(price=2)，
     第 3 天冷冻期不能买入，第 4 天买入(price=0)，第 5 天卖出(price=2)，
     利润 = (2-1) + (2-0) = 3。
```

## 思路

状态 DP：`a` 持有、`b` 空仓且可买、`c` 今日卖出、`d` 今日冷冻。初值 `a=-prices[0]`，其余为 0。每天用旧值更新：

`a'=max(a,b-price,d-price)`；`b'=max(b,d)`；`c'=a+price`；`d'=c`。

今日买入不能来自昨日卖出状态，返回 `max(b,c,d)`。

时间 O(n)，滚动空间 O(1)，完整状态表 O(n)。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；max：Python 内置函数：返回最大值。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
    def maxProfit(self, prices: list[int]) -> int:
        n = len(prices)
        if n == 0:
            return 0
        # dp0持有 dp1保持卖出 dp2今日卖出 dp3冷冻期
        dp0, dp1, dp2, dp3 = -prices[0], 0, 0, 0  # 第一天初始化
        for i in range(1, n):
            # 持有：延续昨天 或 从"保持卖出"/"冷冻期结束"买入（不能从"今日卖出"直接买）
            new_dp0 = max(dp0, dp1 - prices[i], dp3 - prices[i])
            # 保持卖出：延续昨天 或 冷冻期结束转入
            new_dp1 = max(dp1, dp3)
            # 今日卖出：只能来自昨天持有
            new_dp2 = dp0 + prices[i]
            # 冷冻期：只能来自昨天刚卖出
            new_dp3 = dp2
            dp0, dp1, dp2, dp3 = new_dp0, new_dp1, new_dp2, new_dp3  # 同步滚动更新
        return max(dp1, dp2, dp3)  # 不持有状态中取最大
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；max：Python 内置函数：返回最大值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
def maxProfit(prices: list[int]) -> int:
    n = len(prices)
    if n == 0:
        return 0
    # dp0持有 dp1保持卖出 dp2今日卖出 dp3冷冻期
    dp0, dp1, dp2, dp3 = -prices[0], 0, 0, 0  # 第一天初始化
    for i in range(1, n):
        # 持有：延续昨天 或 从"保持卖出"/"冷冻期结束"买入（不能从"今日卖出"直接买）
        new_dp0 = max(dp0, dp1 - prices[i], dp3 - prices[i])
        # 保持卖出：延续昨天 或 冷冻期结束转入
        new_dp1 = max(dp1, dp3)
        # 今日卖出：只能来自昨天持有
        new_dp2 = dp0 + prices[i]
        # 冷冻期：只能来自昨天刚卖出
        new_dp3 = dp2
        dp0, dp1, dp2, dp3 = new_dp0, new_dp1, new_dp2, new_dp3  # 同步滚动更新
    return max(dp1, dp2, dp3)  # 不持有状态中取最大


if __name__ == "__main__":
    # 用例 1：输入 ([1, 2, 3, 0, 2],)；期望 3。
    arg0 = [1, 2, 3, 0, 2]
    result = maxProfit(arg0)
    expected = 3
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([1],)；期望 0。
    arg0 = [1]
    result = maxProfit(arg0)
    expected = 0
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([2, 1],)；期望 0。
    arg0 = [2, 1]
    result = maxProfit(arg0)
    expected = 0
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
func maxProfit(prices []int) int {
    n := len(prices)
    if n == 0 {
        return 0
    }
    // dp0持有 dp1保持卖出 dp2今日卖出 dp3冷冻期
    dp0, dp1, dp2, dp3 := -prices[0], 0, 0, 0 // 第一天初始化
    for i := 1; i < n; i++ {
        // 持有：延续昨天 或 从"保持卖出"/"冷冻期结束"买入
        newDp0 := max(dp0, max(dp1-prices[i], dp3-prices[i]))
        // 保持卖出：延续昨天 或 冷冻期结束转入
        newDp1 := max(dp1, dp3)
        // 今日卖出：只能来自昨天持有
        newDp2 := dp0 + prices[i]
        // 冷冻期：只能来自昨天刚卖出
        newDp3 := dp2
        dp0, dp1, dp2, dp3 = newDp0, newDp1, newDp2, newDp3 // 同步滚动更新
    }
    return max(dp1, max(dp2, dp3)) // 不持有状态中取最大
}

// 方法：max；按题目要求处理输入并返回结果，核心算法见方法体。
func max(a, b int) int {
    if a > b {
        return a
    }
    return b
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
func maxProfit(prices []int) int {
    n := len(prices)
    if n == 0 {
        return 0
    }
    // dp0持有 dp1保持卖出 dp2今日卖出 dp3冷冻期
    dp0, dp1, dp2, dp3 := -prices[0], 0, 0, 0 // 第一天初始化
    for i := 1; i < n; i++ {
        // 持有：延续昨天 或 从"保持卖出"/"冷冻期结束"买入
        newDp0 := max(dp0, max(dp1-prices[i], dp3-prices[i]))
        // 保持卖出：延续昨天 或 冷冻期结束转入
        newDp1 := max(dp1, dp3)
        // 今日卖出：只能来自昨天持有
        newDp2 := dp0 + prices[i]
        // 冷冻期：只能来自昨天刚卖出
        newDp3 := dp2
        dp0, dp1, dp2, dp3 = newDp0, newDp1, newDp2, newDp3 // 同步滚动更新
    }
    return max(dp1, max(dp2, dp3)) // 不持有状态中取最大
}

// 方法：max；按题目要求处理输入并返回结果，核心算法见方法体。
func max(a, b int) int {
    if a > b {
        return a
    }
    return b
}

func main() {
    // 用例 1：输入 ([1, 2, 3, 0, 2],)；期望 3。
    {
        arg0 := []int{1, 2, 3, 0, 2}
        result := maxProfit(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=3 实际=", string(data))
    }
    // 用例 2：输入 ([1],)；期望 0。
    {
        arg0 := []int{1}
        result := maxProfit(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=0 实际=", string(data))
    }
    // 用例 3：输入 ([2, 1],)；期望 0。
    {
        arg0 := []int{2, 1}
        result := maxProfit(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=0 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。

class Solution {
public:
    // 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
    int maxProfit(vector<int>& prices) {
        int n = prices.size();
        if (n == 0) return 0;
        // dp[i][0]持有 dp[i][1]保持卖出 dp[i][2]今日卖出 dp[i][3]冷冻期
        vector<vector<int>> dp(n, vector<int>(4, 0));
        dp[0][0] = -prices[0]; // 第一天买入
        dp[0][1] = 0;          // 第一天不操作，保持可买入状态
        dp[0][2] = 0;          // 第一天不可能卖出，占位
        dp[0][3] = 0;          // 第一天不可能冷冻期，占位
        for (int i = 1; i < n; i++) {
            // 持有：延续昨天持有，或从"保持卖出"/"冷冻期结束"买入（不能从"今日卖出"直接买）
            dp[i][0] = max(dp[i - 1][0], max(dp[i - 1][1] - prices[i], dp[i - 1][3] - prices[i]));
            // 保持卖出：延续昨天保持卖出，或昨天冷冻期结束转入
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][3]);
            // 今日卖出：唯一来源是昨天持有，今天卖出
            dp[i][2] = dp[i - 1][0] + prices[i];
            // 冷冻期：唯一来源是昨天刚卖出
            dp[i][3] = dp[i - 1][2];
        }
        // 最终答案在所有"不持有"状态里取最大值（持有状态不可能是全局最优）
        return max({dp[n - 1][1], dp[n - 1][2], dp[n - 1][3]});
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
int maxProfit(vector<int>& prices);

// 方法：maxProfit；按题目要求处理输入并返回结果，核心算法见方法体。
    int maxProfit(vector<int>& prices) {
        int n = prices.size();
        if (n == 0) return 0;
        // dp[i][0]持有 dp[i][1]保持卖出 dp[i][2]今日卖出 dp[i][3]冷冻期
        vector<vector<int>> dp(n, vector<int>(4, 0));
        dp[0][0] = -prices[0]; // 第一天买入
        dp[0][1] = 0;          // 第一天不操作，保持可买入状态
        dp[0][2] = 0;          // 第一天不可能卖出，占位
        dp[0][3] = 0;          // 第一天不可能冷冻期，占位
        for (int i = 1; i < n; i++) {
            // 持有：延续昨天持有，或从"保持卖出"/"冷冻期结束"买入（不能从"今日卖出"直接买）
            dp[i][0] = max(dp[i - 1][0], max(dp[i - 1][1] - prices[i], dp[i - 1][3] - prices[i]));
            // 保持卖出：延续昨天保持卖出，或昨天冷冻期结束转入
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][3]);
            // 今日卖出：唯一来源是昨天持有，今天卖出
            dp[i][2] = dp[i - 1][0] + prices[i];
            // 冷冻期：唯一来源是昨天刚卖出
            dp[i][3] = dp[i - 1][2];
        }
        // 最终答案在所有"不持有"状态里取最大值（持有状态不可能是全局最优）
        return max({dp[n - 1][1], dp[n - 1][2], dp[n - 1][3]});
    }

}

int main() {
    // 用例 1：输入 ([1, 2, 3, 0, 2],)；期望 3。
    {
        vector<int> arg0 = {1, 2, 3, 0, 2};
        auto result = interview::maxProfit(arg0);
        cout << "用例 1: 期望=3 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([1],)；期望 0。
    {
        vector<int> arg0 = {1};
        auto result = interview::maxProfit(arg0);
        cout << "用例 2: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([2, 1],)；期望 0。
    {
        vector<int> arg0 = {2, 1};
        auto result = interview::maxProfit(arg0);
        cout << "用例 3: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 冷冻期约束的核心处理方式是把 122 题的"不持有"状态拆成三种更细的时序状态：保持卖出、今日卖出、冷冻期，用状态编号本身携带"距离上次卖出过去了几天"的信息，弥补线性 DP 无法直接"回看更早历史"的局限。
- 最容易出错的一步是"持有"状态的买入来源：只能从"保持卖出"或"冷冻期结束"转移，绝不能从"今日卖出"直接买入，因为那等价于跳过了冷冻期这一天的限制。
- 状态2（今日卖出）和状态3（冷冻期）不能合并，因为它们对"明天能否买入"的影响不同：合并后递推公式无法区分这两种历史，会导致允许在冷冻期当天买入的错误结果。
- 这套"给状态拆出时序信息"的技巧具有通用性：只要约束是"某个动作后的第几天不能做另一个动作"，都可以通过拆分状态编号、让状态本身携带"计时器"信息来解决，冷冻期天数越长，需要拆分的状态就越多。
