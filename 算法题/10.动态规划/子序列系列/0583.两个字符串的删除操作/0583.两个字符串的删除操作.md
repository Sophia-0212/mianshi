# 583. 两个字符串的删除操作

**题目描述**

给定两个字符串 `word1` 和 `word2`，每次可以删除任意一个字符串中的一个字符，求使 `word1` 和 `word2` 相同所需的最小步数。

```
示例：
输入: word1 = "sea", word2 = "eat"
输出: 2
解释: 第一步将 "sea" 变为 "ea"，第二步将 "eat" 变为 "ea"
```

## 思路

- LCS：求最长公共子序列长度 `L`，删除其余字符，答案 `n+m-2L`。
- 删除 DP：`dp[i][j]` 为两前缀变相同的最少删除数。边界 `dp[i][0]=i、dp[0][j]=j`；字符相等取左上，不等取 `1+min(dp[i-1][j],dp[i][j-1])`。逐行正序，返回 `dp[n][m]`。

两种方法均为时间、空间 O(nm)；滚动保存两行可将空间降至 O(m)。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；min：Python 内置函数：返回最小值。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：minDistance；按题目要求处理输入并返回结果，核心算法见方法体。
    def minDistance(self, word1: str, word2: str) -> int:
        n, m = len(word1), len(word2)
        dp = [[0] * (m + 1) for _ in range(n + 1)]
        for i in range(n + 1):
            dp[i][0] = i  # word2为空，需删除i次
        for j in range(m + 1):
            dp[0][j] = j  # word1为空，需删除j次

        for i in range(1, n + 1):
            for j in range(1, m + 1):
                if word1[i - 1] == word2[j - 1]:
                    dp[i][j] = dp[i - 1][j - 1]  # 字符相等，继承左上角
                else:
                    # 不相等：删word1[i-1]，或删word2[j-1]，取更小步数
                    dp[i][j] = min(dp[i - 1][j] + 1, dp[i][j - 1] + 1)
        return dp[n][m]
```


下面的代码用的是思路二，也就是直接定义删除步数这种写法，这样更能体现出本题作为编辑距离铺垫题的价值。

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；min：Python 内置函数：返回最小值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：minDistance；按题目要求处理输入并返回结果，核心算法见方法体。
def minDistance(word1: str, word2: str) -> int:
    n, m = len(word1), len(word2)
    dp = [[0] * (m + 1) for _ in range(n + 1)]
    for i in range(n + 1):
        dp[i][0] = i  # word2为空，需删除i次
    for j in range(m + 1):
        dp[0][j] = j  # word1为空，需删除j次

    for i in range(1, n + 1):
        for j in range(1, m + 1):
            if word1[i - 1] == word2[j - 1]:
                dp[i][j] = dp[i - 1][j - 1]  # 字符相等，继承左上角
            else:
                # 不相等：删word1[i-1]，或删word2[j-1]，取更小步数
                dp[i][j] = min(dp[i - 1][j] + 1, dp[i][j - 1] + 1)
    return dp[n][m]


if __name__ == "__main__":
    # 用例 1：输入 ('sea', 'eat')；期望 2。
    arg0 = 'sea'
    arg1 = 'eat'
    result = minDistance(arg0, arg1)
    expected = 2
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ('abc', 'abc')；期望 0。
    arg0 = 'abc'
    arg1 = 'abc'
    result = minDistance(arg0, arg1)
    expected = 0
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ('', 'a')；期望 1。
    arg0 = ''
    arg1 = 'a'
    result = minDistance(arg0, arg1)
    expected = 1
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：minDistance；按题目要求处理输入并返回结果，核心算法见方法体。
func minDistance(word1 string, word2 string) int {
    n, m := len(word1), len(word2)
    dp := make([][]int, n+1)
    for i := range dp {
        dp[i] = make([]int, m+1)
    }
    for i := 0; i <= n; i++ {
        dp[i][0] = i // word2为空，需删除i次
    }
    for j := 0; j <= m; j++ {
        dp[0][j] = j // word1为空，需删除j次
    }

    for i := 1; i <= n; i++ {
        for j := 1; j <= m; j++ {
            if word1[i-1] == word2[j-1] {
                dp[i][j] = dp[i-1][j-1] // 字符相等，继承左上角
            } else {
                del1 := dp[i-1][j] + 1 // 删word1[i-1]
                del2 := dp[i][j-1] + 1 // 删word2[j-1]
                if del1 < del2 {
                    dp[i][j] = del1
                } else {
                    dp[i][j] = del2
                }
            }
        }
    }
    return dp[n][m]
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：minDistance；按题目要求处理输入并返回结果，核心算法见方法体。
func minDistance(word1 string, word2 string) int {
    n, m := len(word1), len(word2)
    dp := make([][]int, n+1)
    for i := range dp {
        dp[i] = make([]int, m+1)
    }
    for i := 0; i <= n; i++ {
        dp[i][0] = i // word2为空，需删除i次
    }
    for j := 0; j <= m; j++ {
        dp[0][j] = j // word1为空，需删除j次
    }

    for i := 1; i <= n; i++ {
        for j := 1; j <= m; j++ {
            if word1[i-1] == word2[j-1] {
                dp[i][j] = dp[i-1][j-1] // 字符相等，继承左上角
            } else {
                del1 := dp[i-1][j] + 1 // 删word1[i-1]
                del2 := dp[i][j-1] + 1 // 删word2[j-1]
                if del1 < del2 {
                    dp[i][j] = del1
                } else {
                    dp[i][j] = del2
                }
            }
        }
    }
    return dp[n][m]
}

func main() {
    // 用例 1：输入 ('sea', 'eat')；期望 2。
    {
        arg0 := "sea"
        arg1 := "eat"
        result := minDistance(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=2 实际=", string(data))
    }
    // 用例 2：输入 ('abc', 'abc')；期望 0。
    {
        arg0 := "abc"
        arg1 := "abc"
        result := minDistance(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=0 实际=", string(data))
    }
    // 用例 3：输入 ('', 'a')；期望 1。
    {
        arg0 := ""
        arg1 := "a"
        result := minDistance(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=1 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。；string：C++ STL 字符串类型。

class Solution {
public:
    // 方法：minDistance；按题目要求处理输入并返回结果，核心算法见方法体。
    int minDistance(string word1, string word2) {
        int n = word1.size(), m = word2.size();
        vector<vector<int>> dp(n + 1, vector<int>(m + 1, 0));
        for (int i = 0; i <= n; i++) dp[i][0] = i;  // word2为空，需删除i次
        for (int j = 0; j <= m; j++) dp[0][j] = j;  // word1为空，需删除j次

        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m; j++) {
                if (word1[i - 1] == word2[j - 1]) {
                    dp[i][j] = dp[i - 1][j - 1];  // 字符相等，不用删除，继承左上角
                } else {
                    // 不相等：删word1[i-1]，或删word2[j-1]，取步数更少的
                    dp[i][j] = min(dp[i - 1][j] + 1, dp[i][j - 1] + 1);
                }
            }
        }
        return dp[n][m];
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
int minDistance(string word1, string word2);

// 方法：minDistance；按题目要求处理输入并返回结果，核心算法见方法体。
    int minDistance(string word1, string word2) {
        int n = word1.size(), m = word2.size();
        vector<vector<int>> dp(n + 1, vector<int>(m + 1, 0));
        for (int i = 0; i <= n; i++) dp[i][0] = i;  // word2为空，需删除i次
        for (int j = 0; j <= m; j++) dp[0][j] = j;  // word1为空，需删除j次

        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m; j++) {
                if (word1[i - 1] == word2[j - 1]) {
                    dp[i][j] = dp[i - 1][j - 1];  // 字符相等，不用删除，继承左上角
                } else {
                    // 不相等：删word1[i-1]，或删word2[j-1]，取步数更少的
                    dp[i][j] = min(dp[i - 1][j] + 1, dp[i][j - 1] + 1);
                }
            }
        }
        return dp[n][m];
    }

}

int main() {
    // 用例 1：输入 ('sea', 'eat')；期望 2。
    {
        string arg0 = "sea";
        string arg1 = "eat";
        auto result = interview::minDistance(arg0, arg1);
        cout << "用例 1: 期望=2 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ('abc', 'abc')；期望 0。
    {
        string arg0 = "abc";
        string arg1 = "abc";
        auto result = interview::minDistance(arg0, arg1);
        cout << "用例 2: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ('', 'a')；期望 1。
    {
        string arg0 = "";
        string arg1 = "a";
        auto result = interview::minDistance(arg0, arg1);
        cout << "用例 3: 期望=1 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 这道题可以转化成"先求最长公共子序列，再用总长度减去 2 倍 LCS 长度"，这个思路展示了问题之间互相转化的能力，不过直接定义删除步数的 dp 其实更贴近题目本身的语义。
- 直接定义的时候，字符不相等那里的递推公式，就是"删 word1 或者删 word2，取比较优的那个"，这是"求最小步数"这类 DP 的典型写法，跟 1143 题"取两个方向最大值"的结构是一样的，只是优化方向反过来了——一个求 min，一个求 max。
- 这一步其实是通往 72 题编辑距离的关键：本题只有"删除"这一种操作，而 72 题会在这个基础上再引入"插入"和"替换"，递推公式也会从两个方向的 min 扩展成三个方向的 min。
