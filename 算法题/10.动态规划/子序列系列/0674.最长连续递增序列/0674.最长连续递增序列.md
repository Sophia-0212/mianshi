# 0674. 最长连续递增序列

**题目描述**

给定一个未经排序的整数数组 `nums`，找出其中最长且**连续递增**的子序列，并返回该序列的长度。

这里的"连续"指的是下标连续，也就是说子序列必须是原数组中一段连续的区间，不能像普通子序列那样跳着选元素。

```
示例：
输入: nums = [1,3,5,4,7]
输出: 3
解释: 最长连续递增序列是 [1,3,5]，长度为 3。
尽管 [1,3,4,7] 也是递增的子序列，但它不是连续的，因为 5 和 4 在原数组中是连续的两个下标，
中间不能跳过 4 而直接从 5 跳到 7。
```

## 思路

DP：`dp[i]` 为以 `i` 结尾的连续递增长度，初值 1。若 `nums[i]>nums[i-1]` 则 `dp[i]=dp[i-1]+1`，否则重置为 1，取全局最大。

时间 O(n)，空间 O(n)；改用连续长度计数器，空间 O(1)。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；max：Python 内置函数：返回最大值。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：findLengthOfLCIS；按题目要求处理输入并返回结果，核心算法见方法体。
    def findLengthOfLCIS(self, nums: list[int]) -> int:
        n = len(nums)
        dp = [1] * n  # dp[i] 初始化为 1，每个元素自身构成长度为1的序列
        result = 1    # 记录最长长度，至少为 1
        for i in range(1, n):
            if nums[i] > nums[i - 1]:
                dp[i] = dp[i - 1] + 1  # 递增，接续前一个数的长度 +1
            else:
                dp[i] = 1  # 不递增，断开，重新从 1 开始计数
            result = max(result, dp[i])  # 更新全局最长长度
        return result
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；max：Python 内置函数：返回最大值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：findLengthOfLCIS；按题目要求处理输入并返回结果，核心算法见方法体。
def findLengthOfLCIS(nums: list[int]) -> int:
    n = len(nums)
    dp = [1] * n  # dp[i] 初始化为 1，每个元素自身构成长度为1的序列
    result = 1    # 记录最长长度，至少为 1
    for i in range(1, n):
        if nums[i] > nums[i - 1]:
            dp[i] = dp[i - 1] + 1  # 递增，接续前一个数的长度 +1
        else:
            dp[i] = 1  # 不递增，断开，重新从 1 开始计数
        result = max(result, dp[i])  # 更新全局最长长度
    return result


if __name__ == "__main__":
    # 用例 1：输入 ([1, 3, 5, 4, 7],)；期望 3。
    arg0 = [1, 3, 5, 4, 7]
    result = findLengthOfLCIS(arg0)
    expected = 3
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([2, 2, 2],)；期望 1。
    arg0 = [2, 2, 2]
    result = findLengthOfLCIS(arg0)
    expected = 1
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([1],)；期望 1。
    arg0 = [1]
    result = findLengthOfLCIS(arg0)
    expected = 1
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：findLengthOfLCIS；按题目要求处理输入并返回结果，核心算法见方法体。
func findLengthOfLCIS(nums []int) int {
    n := len(nums)
    dp := make([]int, n)
    for i := range dp {
        dp[i] = 1 // dp[i] 初始化为 1，每个元素自身构成长度为1的序列
    }
    result := 1 // 记录最长长度，至少为 1
    for i := 1; i < n; i++ {
        if nums[i] > nums[i-1] {
            dp[i] = dp[i-1] + 1 // 递增，接续前一个数的长度 +1
        } else {
            dp[i] = 1 // 不递增，断开，重新从 1 开始计数
        }
        if dp[i] > result {
            result = dp[i] // 更新全局最长长度
        }
    }
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：findLengthOfLCIS；按题目要求处理输入并返回结果，核心算法见方法体。
func findLengthOfLCIS(nums []int) int {
    n := len(nums)
    dp := make([]int, n)
    for i := range dp {
        dp[i] = 1 // dp[i] 初始化为 1，每个元素自身构成长度为1的序列
    }
    result := 1 // 记录最长长度，至少为 1
    for i := 1; i < n; i++ {
        if nums[i] > nums[i-1] {
            dp[i] = dp[i-1] + 1 // 递增，接续前一个数的长度 +1
        } else {
            dp[i] = 1 // 不递增，断开，重新从 1 开始计数
        }
        if dp[i] > result {
            result = dp[i] // 更新全局最长长度
        }
    }
    return result
}

func main() {
    // 用例 1：输入 ([1, 3, 5, 4, 7],)；期望 3。
    {
        arg0 := []int{1, 3, 5, 4, 7}
        result := findLengthOfLCIS(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=3 实际=", string(data))
    }
    // 用例 2：输入 ([2, 2, 2],)；期望 1。
    {
        arg0 := []int{2, 2, 2}
        result := findLengthOfLCIS(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=1 实际=", string(data))
    }
    // 用例 3：输入 ([1],)；期望 1。
    {
        arg0 := []int{1}
        result := findLengthOfLCIS(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=1 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。

class Solution {
public:
    // 方法：findLengthOfLCIS；按题目要求处理输入并返回结果，核心算法见方法体。
    int findLengthOfLCIS(vector<int>& nums) {
        int n = nums.size();
        vector<int> dp(n, 1);  // dp[i] 初始化为 1，每个元素自身构成长度为1的序列
        int result = 1;        // 记录最长长度，至少为 1
        for (int i = 1; i < n; i++) {
            if (nums[i] > nums[i - 1]) {
                dp[i] = dp[i - 1] + 1;  // 递增，接续前一个数的长度 +1
            } else {
                dp[i] = 1;  // 不递增，断开，重新从 1 开始计数
            }
            result = max(result, dp[i]);  // 更新全局最长长度
        }
        return result;
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
int findLengthOfLCIS(vector<int>& nums);

// 方法：findLengthOfLCIS；按题目要求处理输入并返回结果，核心算法见方法体。
    int findLengthOfLCIS(vector<int>& nums) {
        int n = nums.size();
        vector<int> dp(n, 1);  // dp[i] 初始化为 1，每个元素自身构成长度为1的序列
        int result = 1;        // 记录最长长度，至少为 1
        for (int i = 1; i < n; i++) {
            if (nums[i] > nums[i - 1]) {
                dp[i] = dp[i - 1] + 1;  // 递增，接续前一个数的长度 +1
            } else {
                dp[i] = 1;  // 不递增，断开，重新从 1 开始计数
            }
            result = max(result, dp[i]);  // 更新全局最长长度
        }
        return result;
    }

}

int main() {
    // 用例 1：输入 ([1, 3, 5, 4, 7],)；期望 3。
    {
        vector<int> arg0 = {1, 3, 5, 4, 7};
        auto result = interview::findLengthOfLCIS(arg0);
        cout << "用例 1: 期望=3 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([2, 2, 2],)；期望 1。
    {
        vector<int> arg0 = {2, 2, 2};
        auto result = interview::findLengthOfLCIS(arg0);
        cout << "用例 2: 期望=1 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([1],)；期望 1。
    {
        vector<int> arg0 = {1};
        auto result = interview::findLengthOfLCIS(arg0);
        cout << "用例 3: 期望=1 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 这道题的核心递推就是比较相邻的两个元素 `nums[i]` 和 `nums[i-1]`，一旦不满足递增条件，就必须把 `dp[i]` 清零重置为 1。这也是"连续子序列"跟"不连续子序列"最本质的区别：连续子序列一旦断开就没有回头路了，而不连续子序列断开之后，还有可能从更早的位置续上。
- 300 题（不要求连续）得枚举所有 `j < i` 的前驱再取最大值，时间复杂度是 O(n^2)；而本题（要求连续）因为候选前驱是唯一的，只需要一层 O(n) 遍历就能解决。这也提醒我们做题的时候，一定要先看清楚"连续"跟"不连续"这类关键限定词，它会直接决定你 DP 状态转移该怎么设计。
- `dp[i]` 只依赖 `dp[i-1]`，所以完全可以用一个滚动变量去替代整个数组，把空间复杂度从 O(n) 优化到 O(1)，代价就是牺牲了 dp 数组本身的可读性和可调试性。
- 以后遇到"连续区间"这类问题的时候，可以优先考虑这种只看相邻元素关系的线性遍历思路，往往会比套用一般子序列那种双重循环的模板要高效得多。
