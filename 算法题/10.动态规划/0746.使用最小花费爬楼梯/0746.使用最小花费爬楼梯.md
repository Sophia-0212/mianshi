# 0746. 使用最小花费爬楼梯

**题目描述**

给你一个整数数组 `cost`，其中 `cost[i]` 表示从楼梯第 i 个台阶向上爬需要支付的体力值。一旦你支付了这个体力值，你就可以选择向上爬一个或者两个台阶。你可以选择从下标为 0 或下标为 1 的台阶开始爬楼梯。请你计算并返回达到楼梯顶部的最低花费。这里"楼梯顶部"指的是数组的下一个下标位置，也就是下标为 `cost.length` 的位置，它本身不在数组范围内，不需要额外花费。

```
示例：
输入: cost = [10, 15, 20]
输出: 15
解释: 最低花费方式是从下标 1 开始，支付 15，然后向上爬两个台阶到达楼顶（下标 3，超出数组范围，无需再付费）。
```

## 思路

DP：`dp[i]` 表示到第 `i` 阶并支付费用后的最小花费。初值为 `cost[0]、cost[1]`，递推 `dp[i]=min(dp[i-1],dp[i-2])+cost[i]`；楼顶免费，返回最后两项较小值。

时间 O(n)，数组空间 O(n)；代码滚动保存前两项，空间 O(1)。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；min：Python 内置函数：返回最小值。

# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：minCostClimbingStairs；按题目要求处理输入并返回结果，核心算法见方法体。
    def minCostClimbingStairs(self, cost: list[int]) -> int:
        n = len(cost)
        if n == 1:
            return cost[0]  # 只有一个台阶，直接站上去即为总花费
        prev2 = cost[0]  # dp[0]：从下标0出发的花费
        prev1 = cost[1]  # dp[1]：从下标1出发的花费
        for i in range(2, n):
            curr = min(prev1, prev2) + cost[i]  # 核心递推：选更便宜的前置台阶跳过来，再加上站上第i阶自身的花费
            prev2, prev1 = prev1, curr  # 同步滚动更新
        return min(prev1, prev2)  # 楼顶在数组之外，取最后两个台阶花费中较小的一个```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。；min：Python 内置函数：返回最小值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：minCostClimbingStairs；按题目要求处理输入并返回结果，核心算法见方法体。
def minCostClimbingStairs(cost: list[int]) -> int:
    n = len(cost)
    if n == 1:
        return cost[0]  # 只有一个台阶，直接站上去即为总花费
    prev2 = cost[0]  # dp[0]：从下标0出发的花费
    prev1 = cost[1]  # dp[1]：从下标1出发的花费
    for i in range(2, n):
        curr = min(prev1, prev2) + cost[i]  # 核心递推：选更便宜的前置台阶跳过来，再加上站上第i阶自身的花费
        prev2, prev1 = prev1, curr  # 同步滚动更新
    return min(prev1, prev2)  # 楼顶在数组之外，取最后两个台阶花费中较小的一个


if __name__ == "__main__":
    # 用例 1：输入 ([10, 15, 20],)；期望 15。
    arg0 = [10, 15, 20]
    result = minCostClimbingStairs(arg0)
    expected = 15
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([1, 100, 1, 1, 1, 100, 1, 1, 100, 1],)；期望 6。
    arg0 = [1, 100, 1, 1, 1, 100, 1, 1, 100, 1]
    result = minCostClimbingStairs(arg0)
    expected = 6
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([0, 0],)；期望 0。
    arg0 = [0, 0]
    result = minCostClimbingStairs(arg0)
    expected = 0
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
// 方法：minCostClimbingStairs；按题目要求处理输入并返回结果，核心算法见方法体。
func minCostClimbingStairs(cost []int) int {
    n := len(cost)
    if n == 1 {
        return cost[0] // 只有一个台阶，直接站上去即为总花费
    }
    prev2 := cost[0] // dp[0]：从下标0出发的花费
    prev1 := cost[1] // dp[1]：从下标1出发的花费
    for i := 2; i < n; i++ {
        curr := prev1 // 先假设 dp[i-1] 更小
        if prev2 < prev1 {
            curr = prev2
        }
        curr += cost[i] // 核心递推：选更便宜的前置台阶跳过来，再加上站上第i阶自身的花费
        prev2 = prev1   // 滚动更新 dp[i-2]
        prev1 = curr    // 滚动更新 dp[i-1]
    }
    if prev1 < prev2 { // 楼顶在数组之外，取最后两个台阶花费中较小的一个
        return prev1
    }
    return prev2
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：minCostClimbingStairs；按题目要求处理输入并返回结果，核心算法见方法体。
func minCostClimbingStairs(cost []int) int {
    n := len(cost)
    if n == 1 {
        return cost[0] // 只有一个台阶，直接站上去即为总花费
    }
    prev2 := cost[0] // dp[0]：从下标0出发的花费
    prev1 := cost[1] // dp[1]：从下标1出发的花费
    for i := 2; i < n; i++ {
        curr := prev1 // 先假设 dp[i-1] 更小
        if prev2 < prev1 {
            curr = prev2
        }
        curr += cost[i] // 核心递推：选更便宜的前置台阶跳过来，再加上站上第i阶自身的花费
        prev2 = prev1   // 滚动更新 dp[i-2]
        prev1 = curr    // 滚动更新 dp[i-1]
    }
    if prev1 < prev2 { // 楼顶在数组之外，取最后两个台阶花费中较小的一个
        return prev1
    }
    return prev2
}

func main() {
    // 用例 1：输入 ([10, 15, 20],)；期望 15。
    {
        arg0 := []int{10, 15, 20}
        result := minCostClimbingStairs(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=15 实际=", string(data))
    }
    // 用例 2：输入 ([1, 100, 1, 1, 1, 100, 1, 1, 100, 1],)；期望 6。
    {
        arg0 := []int{1, 100, 1, 1, 1, 100, 1, 1, 100, 1}
        result := minCostClimbingStairs(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=6 实际=", string(data))
    }
    // 用例 3：输入 ([0, 0],)；期望 0。
    {
        arg0 := []int{0, 0}
        result := minCostClimbingStairs(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=0 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
public:
    // 方法：minCostClimbingStairs；按题目要求处理输入并返回结果，核心算法见方法体。
    int minCostClimbingStairs(vector<int>& cost) {
        int n = cost.size();
        if (n == 1) return cost[0];  // 只有一个台阶，直接站上去即为总花费
        int prev2 = cost[0];  // dp[0]：从下标0出发的花费
        int prev1 = cost[1];  // dp[1]：从下标1出发的花费
        for (int i = 2; i < n; i++) {
            int curr = min(prev1, prev2) + cost[i];  // 核心递推：选更便宜的前置台阶跳过来，再加上站上第i阶自身的花费
            prev2 = prev1;  // 滚动更新 dp[i-2]
            prev1 = curr;   // 滚动更新 dp[i-1]
        }
        return min(prev1, prev2);  // 楼顶在数组之外，取最后两个台阶花费中较小的一个
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
int minCostClimbingStairs(vector<int>& cost);

// 方法：minCostClimbingStairs；按题目要求处理输入并返回结果，核心算法见方法体。
    int minCostClimbingStairs(vector<int>& cost) {
        int n = cost.size();
        if (n == 1) return cost[0];  // 只有一个台阶，直接站上去即为总花费
        int prev2 = cost[0];  // dp[0]：从下标0出发的花费
        int prev1 = cost[1];  // dp[1]：从下标1出发的花费
        for (int i = 2; i < n; i++) {
            int curr = min(prev1, prev2) + cost[i];  // 核心递推：选更便宜的前置台阶跳过来，再加上站上第i阶自身的花费
            prev2 = prev1;  // 滚动更新 dp[i-2]
            prev1 = curr;   // 滚动更新 dp[i-1]
        }
        return min(prev1, prev2);  // 楼顶在数组之外，取最后两个台阶花费中较小的一个
    }

}

int main() {
    // 用例 1：输入 ([10, 15, 20],)；期望 15。
    {
        vector<int> arg0 = {10, 15, 20};
        auto result = interview::minCostClimbingStairs(arg0);
        cout << "用例 1: 期望=15 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([1, 100, 1, 1, 1, 100, 1, 1, 100, 1],)；期望 6。
    {
        vector<int> arg0 = {1, 100, 1, 1, 1, 100, 1, 1, 100, 1};
        auto result = interview::minCostClimbingStairs(arg0);
        cout << "用例 2: 期望=6 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([0, 0],)；期望 0。
    {
        vector<int> arg0 = {0, 0};
        auto result = interview::minCostClimbingStairs(arg0);
        cout << "用例 3: 期望=0 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 这道题的关键是把dp[i]的含义定义清楚——"到达下标i需要的最小花费，且这个花费已经包含站上第i阶本身的cost[i]"，你要是定义模糊了，后面的初始化和最终答案都容易搞混。
- 递推公式 dp[i] = min(dp[i-1], dp[i-2]) + cost[i] 里，`min` 部分体现的是"选择更便宜的路径"，`+cost[i]` 部分体现的是"站上当前台阶必须付费"，这两部分分工不一样，缺一不可。
- 初始化 dp[0]=cost[0]、dp[1]=cost[1]，是因为题目允许从下标0或1任意一个位置直接起跳，这两个起点本身不需要经过更早的台阶。
- 最终答案不是dp[n-1]，而是min(dp[n-1], dp[n-2])，为什么呢？因为楼顶在数组下标范围之外，最后一步可以从倒数第二个台阶直接两步跨越到楼顶，所以必须把这种情况也纳入比较。
