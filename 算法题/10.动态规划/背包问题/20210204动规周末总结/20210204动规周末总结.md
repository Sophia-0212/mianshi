# 2021.02.04 动规周末总结

完全背包的物品可重复，目标决定转移：

- 求方案数：`dp[0]=1`，其余为 0，用累加；组合物品在外，排列容量在外。

```
dp[j] += dp[j - coins[i]]
```

- 求最少个数：`dp[0]=0`，其余为不可达，用 min；容量正序。

```
dp[j] = min(dp[j], dp[j - coins[i]] + 1)
```

零钱兑换的物品是给定面值；完全平方数的物品是 1²～⌊√n⌋²。前者时间 O(kA)，后者 O(n√n)，空间分别 O(A)、O(n)。
