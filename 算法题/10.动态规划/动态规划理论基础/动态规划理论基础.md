# 动态规划理论基础

DP 保存重复子问题的结果。状态需包含后续决策所需信息，转移依赖已计算的状态。

1. 定义状态与下标含义。
2. 按最后一步或选／不选划分来源。
3. 初始化空状态、最小规模和不可达状态。
4. 按依赖方向遍历；滚动覆盖前确认仍需哪些旧值。
5. 用最小示例核对边界与转移。

时间＝状态数×每状态转移开销；空间＝实际保存状态＋调用栈。贪心只保留当前选择，DP 比较多个子状态。
