KMP 的失配表只由模式串决定。

- 前缀函数：`pi[i]` 为模式串前缀 `P[0..i]` 的最长相等真前后缀长度；如 `abaab` 对应长度为 2。
- 构建时失配令 `j=pi[j-1]`，相等则 `j++`，记录 `pi[i]=j`。
- 匹配主串时采用相同回退规则，主串指针不回退。时间 O(n+m)，空间 O(m)。

传统 `next` 也有“首项 -1、存回退下标”或从 1 编号的定义，须与匹配代码配套；不能混用。`nextval` 在回退字符仍相同时继续沿失配链接跳转，省去必然失败的比较。
