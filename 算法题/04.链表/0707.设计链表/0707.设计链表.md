# 0707. 设计链表

**题目描述**

设计实现一个链表，支持以下操作：

- `get(index)`：获取链表中第 `index` 个节点的值（从 0 开始），若 `index` 无效返回 -1
- `addAtHead(val)`：在链表头部插入一个值为 `val` 的节点
- `addAtTail(val)`：在链表尾部插入一个值为 `val` 的节点
- `addAtIndex(index, val)`：在第 `index` 个节点之前插入值为 `val` 的节点。若 `index` 等于链表长度，则插入到末尾；若 `index` 大于链表长度，则不插入；若 `index` 小于 0，视为在头部插入
- `deleteAtIndex(index)`：删除第 `index` 个节点，若 `index` 无效则不操作

```
示例：
MyLinkedList list = new MyLinkedList();
list.addAtHead(1);        // 链表: 1
list.addAtTail(3);        // 链表: 1 -> 3
list.addAtIndex(1, 2);    // 链表: 1 -> 2 -> 3
list.get(1);               // 返回 2
list.deleteAtIndex(1);     // 链表: 1 -> 3
list.get(1);               // 返回 3
```

## 思路

使用虚拟头节点和 `size`。按下标访问；插入、删除先定位前驱，再修改连接。

`get/delete` 要求 `0<=index<size`；插入允许 `index==size`，负下标按头插处理，超过 `size` 忽略。

头插时间 O(1)，访问、尾插及按下标增删最坏 O(n)；存储空间 O(n)，单次操作辅助空间 O(1)。维护尾指针可将尾插降为 O(1)。



## Python

### LeetCode 写法与原有示例

这道题是链表结构本身的实现题，Python 没有内置链表类型，标准库也没有对应的一行解，只能手写实现：

```python
# 依赖说明：range：Python 内置函数：生成按步长遍历的整数序列。

class ListNode:
    # 方法：__init__；按题目要求处理输入并返回结果，核心算法见方法体。
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next


class MyLinkedList:
    # 方法：__init__；按题目要求处理输入并返回结果，核心算法见方法体。
    def __init__(self):
        self.dummy = ListNode(0)  # 虚拟头节点
        self.size = 0               # 链表长度

    # 方法：get；按题目要求处理输入并返回结果，核心算法见方法体。
    def get(self, index: int) -> int:
        if index < 0 or index >= self.size:  # 越界返回 -1
            return -1
        cur = self.dummy.next
        for _ in range(index):  # 走 index 步到达目标节点
            cur = cur.next
        return cur.val

    # 方法：addAtHead；按题目要求处理输入并返回结果，核心算法见方法体。
    def addAtHead(self, val: int) -> None:
        self.addAtIndex(0, val)

    # 方法：addAtTail；按题目要求处理输入并返回结果，核心算法见方法体。
    def addAtTail(self, val: int) -> None:
        self.addAtIndex(self.size, val)

    # 方法：addAtIndex；按题目要求处理输入并返回结果，核心算法见方法体。
    def addAtIndex(self, index: int, val: int) -> None:
        if index > self.size:  # 超出长度不插入
            return
        if index < 0:           # 负数视为头部插入
            index = 0
        prev = self.dummy
        for _ in range(index):  # 找到插入位置的前驱
            prev = prev.next
        new_node = ListNode(val)
        new_node.next = prev.next  # 新节点指向原后继
        prev.next = new_node        # 前驱指向新节点
        self.size += 1

    # 方法：deleteAtIndex；按题目要求处理输入并返回结果，核心算法见方法体。
    def deleteAtIndex(self, index: int) -> None:
        if index < 0 or index >= self.size:  # 越界不操作
            return
        prev = self.dummy
        for _ in range(index):  # 找到待删除节点的前驱
            prev = prev.next
        prev.next = prev.next.next  # 跳过待删除节点
        self.size -= 1```


链表节点定义：

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；isinstance：Python 内置函数：判断对象是否属于指定类型。；range：Python 内置函数：生成按步长遍历的整数序列。；zip：Python 内置函数：按位置把多个可迭代对象组合成元组序列。；max：Python 内置函数：返回最大值。

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；isinstance：Python 内置函数：判断对象是否属于指定类型。；range：Python 内置函数：生成按步长遍历的整数序列。

class ListNode:
    def __init__(self, val=0, next=None):
        self.val, self.next = val, next

def build_list(values):
    # 用虚拟头节点简化链表的逐个追加。
    dummy = tail = ListNode()
    for value in values:
        tail.next = ListNode(value)
        tail = tail.next
    return dummy.next

def list_values(head):
    result = []
    while head:
        result.append(head.val)
        head = head.next
    return result

def build_cycle(values, pos):
    head = build_list(values)
    nodes, node = [], head
    while node:
        nodes.append(node)
        node = node.next
    if nodes and pos >= 0:
        nodes[-1].next = nodes[pos]
    return head

def attach(prefix, shared):
    # 两条链表连接同一段节点，构造真正的相交而非数值相等。
    head = build_list(prefix)
    if head is None:
        return shared
    tail = head
    while tail.next:
        tail = tail.next
    tail.next = shared
    return head

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
def runOperations(operations, arguments):
    # 普通函数维护真实单链表；只将 get 的结果加入输出。
    dummy = ListNode()
    size, answers = 0, []
    for operation, args in zip(operations, arguments):
        if operation == 'get':
            index = args[0]
            if not 0 <= index < size:
                answers.append(-1)
                continue
            node = dummy.next
            for _ in range(index):
                node = node.next
            answers.append(node.val)
        elif operation == 'deleteAtIndex':
            index = args[0]
            if not 0 <= index < size:
                continue
            previous = dummy
            for _ in range(index):
                previous = previous.next
            previous.next = previous.next.next
            size -= 1
        else:
            index = 0 if operation == 'addAtHead' else size if operation == 'addAtTail' else max(0, args[0])
            value = args[-1]
            if index > size:
                continue
            previous = dummy
            for _ in range(index):
                previous = previous.next
            previous.next = ListNode(value, previous.next)
            size += 1
    return answers


if __name__ == "__main__":
    # 用例 1：输入 (['addAtHead', 'addAtTail', 'addAtIndex', 'get', 'deleteAtIndex', 'get'], [[1], [3], [1, 2], [1], [1], [1]])；期望 [2, 3]。
    arg0 = ['addAtHead', 'addAtTail', 'addAtIndex', 'get', 'deleteAtIndex', 'get']
    arg1 = [[1], [3], [1, 2], [1], [1], [1]]
    result = runOperations(arg0, arg1)
    if isinstance(result, ListNode) or result is None:
        result = list_values(result)
    expected = [2, 3]
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 (['get', 'addAtIndex', 'get'], [[0], [1, 9], [0]])；期望 [-1, -1]。
    arg0 = ['get', 'addAtIndex', 'get']
    arg1 = [[0], [1, 9], [0]]
    result = runOperations(arg0, arg1)
    if isinstance(result, ListNode) or result is None:
        result = list_values(result)
    expected = [-1, -1]
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 (['addAtHead', 'deleteAtIndex', 'get'], [[5], [0], [0]])；期望 [-1]。
    arg0 = ['addAtHead', 'deleteAtIndex', 'get']
    arg1 = [[5], [0], [0]]
    result = runOperations(arg0, arg1)
    if isinstance(result, ListNode) or result is None:
        result = list_values(result)
    expected = [-1]
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected```

## Go

### LeetCode 写法与原有示例

```go
type ListNode struct {
    Val  int
    Next *ListNode
}

type MyLinkedList struct {
    dummy *ListNode // 虚拟头节点
    size  int        // 链表长度
}

// 方法：Constructor；按题目要求处理输入并返回结果，核心算法见方法体。
func Constructor() MyLinkedList {
    return MyLinkedList{dummy: &ListNode{}, size: 0}
}

// 方法：Get；按题目要求处理输入并返回结果，核心算法见方法体。
func (l *MyLinkedList) Get(index int) int {
    if index < 0 || index >= l.size { // 越界返回 -1
        return -1
    }
    cur := l.dummy.Next
    for i := 0; i < index; i++ { // 走到目标节点
        cur = cur.Next
    }
    return cur.Val
}

// 方法：AddAtHead；按题目要求处理输入并返回结果，核心算法见方法体。
func (l *MyLinkedList) AddAtHead(val int) {
    l.AddAtIndex(0, val)
}

// 方法：AddAtTail；按题目要求处理输入并返回结果，核心算法见方法体。
func (l *MyLinkedList) AddAtTail(val int) {
    l.AddAtIndex(l.size, val)
}

// 方法：AddAtIndex；按题目要求处理输入并返回结果，核心算法见方法体。
func (l *MyLinkedList) AddAtIndex(index int, val int) {
    if index > l.size { // 超出长度不插入
        return
    }
    if index < 0 { // 负数视为头部插入
        index = 0
    }
    prev := l.dummy
    for i := 0; i < index; i++ { // 找到插入位置前驱
        prev = prev.Next
    }
    newNode := &ListNode{Val: val, Next: prev.Next} // 新节点接上原后继
    prev.Next = newNode                               // 前驱接上新节点
    l.size++
}

// 方法：DeleteAtIndex；按题目要求处理输入并返回结果，核心算法见方法体。
func (l *MyLinkedList) DeleteAtIndex(index int) {
    if index < 0 || index >= l.size { // 越界不操作
        return
    }
    prev := l.dummy
    for i := 0; i < index; i++ { // 找到待删除节点前驱
        prev = prev.Next
    }
    prev.Next = prev.Next.Next // 跳过待删除节点
    l.size--
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

type ListNode struct { Val int; Next *ListNode }
// 虚拟头节点用于构造链表，测试中的节点由自己创建。
func buildList(values []int) *ListNode {
    dummy:=&ListNode{}; tail:=dummy
    for _,v:=range values { tail.Next=&ListNode{Val:v}; tail=tail.Next }; return dummy.Next
}
func listValues(head *ListNode) []int { result:=[]int{}; for ;head!=nil;head=head.Next {result=append(result,head.Val)};return result }
func buildCycle(values []int,pos int) *ListNode {
    head:=buildList(values); nodes:=[]*ListNode{}
    for n:=head;n!=nil;n=n.Next {nodes=append(nodes,n)}
    if len(nodes)>0 && pos>=0 {nodes[len(nodes)-1].Next=nodes[pos]};return head
}
func attach(prefix []int,shared *ListNode) *ListNode {
    head:=buildList(prefix);if head==nil{return shared};tail:=head;for tail.Next!=nil{tail=tail.Next};tail.Next=shared;return head
}

// 普通函数和题目中的核心算法。
func runOperations(operations []string, arguments [][]int) []int {
    // 使用真实单链表；get 越界返回 -1，插入和删除按题目规则处理。
    dummy:=&ListNode{};size:=0;answers:=[]int{}
    for i,op:=range operations {
        a:=arguments[i]
        if op=="get" {
            index:=a[0];if index<0 || index>=size {answers=append(answers,-1);continue}
            node:=dummy.Next;for j:=0;j<index;j++ {node=node.Next};answers=append(answers,node.Val)
        } else if op=="deleteAtIndex" {
            index:=a[0];if index<0 || index>=size {continue}
            prev:=dummy;for j:=0;j<index;j++ {prev=prev.Next};prev.Next=prev.Next.Next;size--
        } else {
            index:=0;if op=="addAtTail" {index=size} else if op=="addAtIndex" {index=a[0];if index<0 {index=0}}
            if index>size {continue};prev:=dummy;for j:=0;j<index;j++ {prev=prev.Next}
            prev.Next=&ListNode{Val:a[len(a)-1],Next:prev.Next};size++
        }
    }
    return answers
}

func main() {
    // 用例 1：输入 (['addAtHead', 'addAtTail', 'addAtIndex', 'get', 'deleteAtIndex', 'get'], [[1], [3], [1, 2], [1], [1], [1]])；期望 [2, 3]。
    {
        arg0 := []string{"addAtHead", "addAtTail", "addAtIndex", "get", "deleteAtIndex", "get"}
        arg1 := [][]int{[]int{1}, []int{3}, []int{1, 2}, []int{1}, []int{1}, []int{1}}
        result := runOperations(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[2, 3] 实际=", string(data))
    }
    // 用例 2：输入 (['get', 'addAtIndex', 'get'], [[0], [1, 9], [0]])；期望 [-1, -1]。
    {
        arg0 := []string{"get", "addAtIndex", "get"}
        arg1 := [][]int{[]int{0}, []int{1, 9}, []int{0}}
        result := runOperations(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[-1, -1] 实际=", string(data))
    }
    // 用例 3：输入 (['addAtHead', 'deleteAtIndex', 'get'], [[5], [0], [0]])；期望 [-1]。
    {
        arg0 := []string{"addAtHead", "deleteAtIndex", "get"}
        arg1 := [][]int{[]int{5}, []int{0}, []int{0}}
        result := runOperations(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[-1] 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(NULL) {}
};
```

```cpp
class MyLinkedList {
private:
    ListNode* dummy;  // 虚拟头节点，dummy->next 才是真正的第 0 个节点
    int size;          // 维护链表长度，O(1) 判断边界

public:
    MyLinkedList() {
        dummy = new ListNode(0);
        size = 0;
    }

    // 方法：get；按题目要求处理输入并返回结果，核心算法见方法体。
    int get(int index) {
        if (index < 0 || index >= size) return -1;  // 越界直接返回 -1
        ListNode* cur = dummy->next;
        while (index--) cur = cur->next;  // 走 index 步到达目标节点
        return cur->val;
    }

    // 方法：addAtHead；按题目要求处理输入并返回结果，核心算法见方法体。
    void addAtHead(int val) {
        addAtIndex(0, val);  // 复用统一插入逻辑
    }

    // 方法：addAtTail；按题目要求处理输入并返回结果，核心算法见方法体。
    void addAtTail(int val) {
        addAtIndex(size, val);  // index == size 表示插入到末尾
    }

    // 方法：addAtIndex；按题目要求处理输入并返回结果，核心算法见方法体。
    void addAtIndex(int index, int val) {
        if (index > size) return;      // 超出长度，不插入
        if (index < 0) index = 0;      // 负数视为插入头部
        ListNode* prev = dummy;
        while (index--) prev = prev->next;  // 找到插入位置的前驱节点
        ListNode* newNode = new ListNode(val);
        newNode->next = prev->next;  // 新节点指向原本的后继
        prev->next = newNode;         // 前驱指向新节点，完成插入
        size++;
    }

    // 方法：deleteAtIndex；按题目要求处理输入并返回结果，核心算法见方法体。
    void deleteAtIndex(int index) {
        if (index < 0 || index >= size) return;  // 越界不操作
        ListNode* prev = dummy;
        while (index--) prev = prev->next;  // 找到待删除节点的前驱
        ListNode* toDelete = prev->next;
        prev->next = toDelete->next;          // 跳过待删除节点
        delete toDelete;
        size--;
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

struct ListNode {
    int val;
    ListNode* next;
    ListNode(int value = 0, ListNode* n = nullptr) : val(value), next(n) {}
};
// 自己构造链表，不依赖平台提供节点。
ListNode* buildList(const vector<int>& values) {
    ListNode dummy; auto* tail = &dummy;
    for (int v : values) { tail->next = new ListNode(v); tail = tail->next; }
    return dummy.next;
}
vector<int> listValues(ListNode* head) {
    vector<int> result;
    for (; head; head = head->next) result.push_back(head->val);
    return result;
}
ListNode* buildCycle(const vector<int>& values, int pos) {
    auto* head = buildList(values);
    vector<ListNode*> nodes;
    for (auto* n = head; n; n = n->next) nodes.push_back(n);
    if (!nodes.empty() && pos >= 0) nodes.back()->next = nodes[pos];
    return head;
}
ListNode* attach(const vector<int>& prefix, ListNode* shared) {
    auto* head = buildList(prefix);
    if (!head) return shared;
    auto* tail = head;
    while (tail->next) tail = tail->next;
    tail->next = shared;
    return head;
}
void show(ListNode* head) { show(listValues(head)); }

namespace interview {

// 普通函数与辅助函数声明。
vector<int> runOperations(vector<string> operations, vector<vector<int>> arguments);

vector<int> runOperations(vector<string> operations, vector<vector<int>> arguments) {
    // 虚拟头节点统一插入、删除；每组输入都构造新的链表。
    ListNode dummy;int size=0;vector<int> answers;
    for(int i=0;i<operations.size();++i) {
        string op=operations[i];auto a=arguments[i];
        if(op=="get") {
            int index=a[0];if(index<0 || index>=size) {answers.push_back(-1);continue;}
            auto* node=dummy.next;for(int j=0;j<index;++j) node=node->next;answers.push_back(node->val);
        } else if(op=="deleteAtIndex") {
            int index=a[0];if(index<0 || index>=size) continue;
            auto* prev=&dummy;for(int j=0;j<index;++j) prev=prev->next;
            auto* removed=prev->next;prev->next=removed->next;delete removed;--size;
        } else {
            int index=op=="addAtHead"?0:op=="addAtTail"?size:max(0,a[0]);if(index>size) continue;
            auto* prev=&dummy;for(int j=0;j<index;++j) prev=prev->next;
            prev->next=new ListNode(a.back(),prev->next);++size;
        }
    }
    while(dummy.next) {auto* p=dummy.next;dummy.next=p->next;delete p;}
    return answers;
}

}

int main() {
    // 用例 1：输入 (['addAtHead', 'addAtTail', 'addAtIndex', 'get', 'deleteAtIndex', 'get'], [[1], [3], [1, 2], [1], [1], [1]])；期望 [2, 3]。
    {
        vector<string> arg0 = {"addAtHead", "addAtTail", "addAtIndex", "get", "deleteAtIndex", "get"};
        vector<vector<int>> arg1 = {{1}, {3}, {1, 2}, {1}, {1}, {1}};
        auto result = interview::runOperations(arg0, arg1);
        cout << "用例 1: 期望=[2, 3] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 (['get', 'addAtIndex', 'get'], [[0], [1, 9], [0]])；期望 [-1, -1]。
    {
        vector<string> arg0 = {"get", "addAtIndex", "get"};
        vector<vector<int>> arg1 = {{0}, {1, 9}, {0}};
        auto result = interview::runOperations(arg0, arg1);
        cout << "用例 2: 期望=[-1, -1] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 (['addAtHead', 'deleteAtIndex', 'get'], [[5], [0], [0]])；期望 [-1]。
    {
        vector<string> arg0 = {"addAtHead", "deleteAtIndex", "get"};
        vector<vector<int>> arg1 = {{5}, {0}, {0}};
        auto result = interview::runOperations(arg0, arg1);
        cout << "用例 3: 期望=[-1] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 这道题考的不是算法思维，而是工程实现够不够严谨。核心就是所有操作前都得先想清楚 `index` 的合法范围在哪，`get`/`deleteAtIndex` 要求 `0 <= index < size`，`addAtIndex` 却允许 `index == size`（也就是插到尾部）。
- 维护一个 `size` 字段去记长度，这样就不用每次操作都遍历一遍链表去判断边界，这其实是个很常见的工程优化思路，面试的时候可以主动提一下。
- 用虚拟头节点加上"统一定位到前驱节点"这个套路去实现插入删除，好处是能把 `addAtHead`/`addAtTail` 都归约成 `addAtIndex` 的特殊情况，代码量能省不少。
- 这道题跟前面"移除链表元素"其实是一脉相承的，本质上链表的插入删除操作说白了都是"找到前驱节点，改它的 `next` 指针"，虚拟头节点就是让这个操作在头部和中间节点上保持一致，不用写两套逻辑。
