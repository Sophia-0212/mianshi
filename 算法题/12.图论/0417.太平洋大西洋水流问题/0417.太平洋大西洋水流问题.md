# 0417. 太平洋大西洋水流问题

**题目描述**

给定一个 `m x n` 的二维矩阵 `heights`，`heights[r][c]` 表示坐标 `(r, c)` 处的地面高度。规定水流规则：**水只能从高处流向相邻的、高度小于或等于当前高度的地方**（可以流向更低处，也可以流向相同高度，但绝不能流向更高处）。

网格的左边界和上边界被认为与"太平洋"相邻，右边界和下边界被认为与"大西洋"相邻。请找出所有满足下面条件的坐标：从这个坐标出发的水，既能流到太平洋，也能流到大西洋。

```
示例：
输入：
heights = [
  [1,2,2,3,5],
  [3,2,3,4,4],
  [2,4,5,3,1],
  [6,7,1,4,5],
  [5,1,1,2,4]
]
输出：[[0,4],[1,3],[1,4],[2,2],[3,0],[3,1],[4,0]]
```

以坐标 `(0,4)` 为例：你看啊，它在最上面一行，天然就跟太平洋相邻了，同时它高度是 5，往下能流到高度更低（或者相等）的格子，最终到达右边界，从而流到大西洋，所以它是同时满足两个条件的。

## 思路

**先说说正向思路，以及它为什么会超时**

最直接的想法是什么呢？就是对网格里的每一个格子 `(r, c)`，分别做一次搜索，判断"从这个格子出发，水能不能流到太平洋"，以及"能不能流到大西洋"。搜索的方式呢，就是从 `(r, c)` 出发做 DFS（或者 BFS），每一步只能走向高度**小于或等于**当前高度的相邻格子——这是题目给的正向规则，水往低处或者同高度流嘛。那如果搜索过程中走到了左边界或者上边界，就说明能流到太平洋；走到右边界或下边界，就说明能流到大西洋。

这个思路逻辑上是没问题的，但效率非常差。你想啊，网格里一共有 `m*n` 个格子，每个格子都要独立做一次搜索，而每次搜索最坏情况下要遍历整个网格，也就是 O(mn)，那总的时间复杂度就变成了 **O(m²n²)**。当 `m` 和 `n` 达到几百的规模时，这个复杂度根本受不了，肯定会超时。

问题出在哪呢？根源就是不同格子出发的搜索之间，完全没有复用任何信息，哪怕两个格子相邻、结果高度相关，也得各自从零搜索一遍，白白浪费了很多计算。

**那咱们换个方向：从边界出发反向搜索**

既然"从每个格子出发判断能不能到达边界"这个效率太低，那咱们就把问题反过来想：**不再问"我能不能到边界"，而是问"边界能不能反过来找到我"**。

具体怎么做呢？就是从太平洋相邻的边界出发，也就是最上面一行和最左边一列的所有格子，做一次**多源** DFS 或 BFS，去标记"哪些格子的水最终能流到太平洋"。但这里有个关键的细节你得想清楚：**搜索方向要反过来**。

正向规则是什么？水从高处流向"高度小于等于自己"的相邻格子，也就是说，如果 `A` 能流到 `B`，那必须满足 `height[A] >= height[B]`。

那现在咱们要做的是反向搜索——从边界格子出发，去找"谁能流到我这里"。你站在边界格子 `A` 的角度想一想，需要找的就是所有满足 `height[B] >= height[A]` 的相邻格子 `B`，为什么呢？因为 `B` 能流到 `A`，是因为 `B` 的高度不低于 `A`，这符合"高处流向低处或同高度"的规则。所以反向搜索的移动条件就是：**只能走向高度大于或等于当前格子的相邻格子**，这正好跟正向规则的大小关系反了过来。

这其实本质上就是图论里"反向边"或者"求反图"这个思想的具体应用：原图里边的方向是"从高到低（或相等）"，那如果要算"谁能到达某个集合"，其实就等价于在反图上算"从这个集合出发能到达谁"，而反图的边方向正好就是原图的反方向，体现在这道题里，就是不等号的方向反了过来——把"我能流向的方向"换成了"能流向我的方向"。

按同样的方法，从大西洋相邻的边界出发，也就是最下面一行和最右边一列，也做一次多源 DFS/BFS，标记"哪些格子的水能流到大西洋"。

最后呢，把两次标记的结果取**交集**——同时被"能到太平洋"和"能到大西洋"标记过的格子，那就是最终答案了。

**为什么反向搜索能把复杂度降下来**

多源 DFS/BFS 跟普通的单源搜索比起来，起点数量确实增多了，边界上有 O(m+n) 个起点，但整体访问的格子数量并没有增多。为什么呢？只要给每个格子维护一个"是否已标记"的状态，一旦某个格子被标记过，就不会再重复进入搜索了——不管它是被哪个起点标记的，或者被同一个起点从哪条路径标记的。也就是说，从太平洋边界出发的这一次多源搜索，最多访问网格里的每个格子一次，整体复杂度就是 O(mn)；大西洋方向同理也是 O(mn)；最后取交集再遍历一次也是 O(mn)。三步加起来，总复杂度就是 **O(mn)**，比正向思路的 O(m²n²) 快了整整 `mn` 倍，提升非常大。

那直观理解一下就是：正向思路里，每个格子的搜索都是独立的、互不复用的，存在大量重复遍历；而反向思路里，"标记数组"起到了一个全局共享状态的作用，任何一个格子只要被标记过，就一次性确定了结果，不会再被重复处理，这样就天然避免了重复计算。

**复杂度分析**

设网格大小为 `m x n`。两次多源 DFS/BFS，也就是太平洋方向和大西洋方向，各自都是 O(mn) 时间、O(mn) 空间（标记数组加上递归栈或者队列）；最后遍历取交集也是 O(mn)。所以总时间复杂度是 O(mn)，空间复杂度也是 O(mn)。

## 代码

### C++

```cpp
class Solution {
public:
    vector<vector<int>> pacificAtlantic(vector<vector<int>>& heights) {
        int m = heights.size(), n = heights[0].size();
        vector<vector<bool>> canPacific(m, vector<bool>(n, false)); // 能流到太平洋的标记
        vector<vector<bool>> canAtlantic(m, vector<bool>(n, false)); // 能流到大西洋的标记

        // 方向数组：上下左右
        int dirs[4][2] = {{-1, 0}, {1, 0}, {0, -1}, {0, 1}};

        // 反向 DFS：只走向高度 >= 当前格子的相邻格子（因为要模拟"谁能流到我这里"）
        function<void(int, int, vector<vector<bool>>&)> dfs = [&](int row, int col, vector<vector<bool>>& visited) {
            visited[row][col] = true; // 标记当前格子已确定能到达对应大洋
            for (auto& d : dirs) {
                int nr = row + d[0], nc = col + d[1];
                if (nr < 0 || nr >= m || nc < 0 || nc >= n) continue; // 越界跳过
                if (visited[nr][nc]) continue; // 已标记过，跳过，避免重复访问
                if (heights[nr][nc] < heights[row][col]) continue; // 反向条件：邻居高度必须 >= 当前格子
                dfs(nr, nc, visited);
            }
        };

        // 从太平洋边界（最上行、最左列）出发做多源 DFS
        for (int i = 0; i < m; i++) dfs(i, 0, canPacific);
        for (int j = 0; j < n; j++) dfs(0, j, canPacific);

        // 从大西洋边界（最下行、最右列）出发做多源 DFS
        for (int i = 0; i < m; i++) dfs(i, n - 1, canAtlantic);
        for (int j = 0; j < n; j++) dfs(m - 1, j, canAtlantic);

        // 取两个标记数组的交集，即为答案
        vector<vector<int>> result;
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (canPacific[i][j] && canAtlantic[i][j]) {
                    result.push_back({i, j});
                }
            }
        }
        return result;
    }
};
```

### Java

```java
class Solution {
    int m, n;
    int[][] dirs = {{-1, 0}, {1, 0}, {0, -1}, {0, 1}}; // 上下左右

    public List<List<Integer>> pacificAtlantic(int[][] heights) {
        m = heights.length;
        n = heights[0].length;
        boolean[][] canPacific = new boolean[m][n];  // 能流到太平洋的标记
        boolean[][] canAtlantic = new boolean[m][n]; // 能流到大西洋的标记

        // 从太平洋边界（最上行、最左列）出发做多源 DFS
        for (int i = 0; i < m; i++) dfs(heights, i, 0, canPacific);
        for (int j = 0; j < n; j++) dfs(heights, 0, j, canPacific);

        // 从大西洋边界（最下行、最右列）出发做多源 DFS
        for (int i = 0; i < m; i++) dfs(heights, i, n - 1, canAtlantic);
        for (int j = 0; j < n; j++) dfs(heights, m - 1, j, canAtlantic);

        // 取两个标记数组的交集，即为答案
        List<List<Integer>> result = new ArrayList<>();
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (canPacific[i][j] && canAtlantic[i][j]) {
                    result.add(Arrays.asList(i, j));
                }
            }
        }
        return result;
    }

    // 反向 DFS：只走向高度 >= 当前格子的相邻格子
    private void dfs(int[][] heights, int row, int col, boolean[][] visited) {
        visited[row][col] = true; // 标记当前格子已确定能到达对应大洋
        for (int[] d : dirs) {
            int nr = row + d[0], nc = col + d[1];
            if (nr < 0 || nr >= m || nc < 0 || nc >= n) continue; // 越界跳过
            if (visited[nr][nc]) continue; // 已标记过，跳过
            if (heights[nr][nc] < heights[row][col]) continue; // 反向条件：邻居高度必须 >= 当前格子
            dfs(heights, nr, nc, visited);
        }
    }
}
```

### Python

这种图论题其实没有什么标准库函数能一行解决的，所以下面就直接给手写的 DFS 实现：

```python
class Solution:
    def pacificAtlantic(self, heights: list[list[int]]) -> list[list[int]]:
        if not heights or not heights[0]:
            return []
        m, n = len(heights), len(heights[0])
        can_pacific = [[False] * n for _ in range(m)]   # 能流到太平洋的标记
        can_atlantic = [[False] * n for _ in range(m)]   # 能流到大西洋的标记
        dirs = [(-1, 0), (1, 0), (0, -1), (0, 1)]  # 上下左右

        def dfs(row: int, col: int, visited: list[list[bool]]) -> None:
            visited[row][col] = True  # 标记当前格子已确定能到达对应大洋
            for dr, dc in dirs:
                nr, nc = row + dr, col + dc
                if nr < 0 or nr >= m or nc < 0 or nc >= n:
                    continue  # 越界跳过
                if visited[nr][nc]:
                    continue  # 已标记过，跳过
                if heights[nr][nc] < heights[row][col]:
                    continue  # 反向条件：邻居高度必须 >= 当前格子
                dfs(nr, nc, visited)

        # 从太平洋边界（最上行、最左列）出发做多源 DFS
        for i in range(m):
            dfs(i, 0, can_pacific)
        for j in range(n):
            dfs(0, j, can_pacific)

        # 从大西洋边界（最下行、最右列）出发做多源 DFS
        for i in range(m):
            dfs(i, n - 1, can_atlantic)
        for j in range(n):
            dfs(m - 1, j, can_atlantic)

        # 取两个标记数组的交集，即为答案
        result = []
        for i in range(m):
            for j in range(n):
                if can_pacific[i][j] and can_atlantic[i][j]:
                    result.append([i, j])
        return result
```

### Go

```go
func pacificAtlantic(heights [][]int) [][]int {
    m, n := len(heights), len(heights[0])
    canPacific := make([][]bool, m)  // 能流到太平洋的标记
    canAtlantic := make([][]bool, m) // 能流到大西洋的标记
    for i := range canPacific {
        canPacific[i] = make([]bool, n)
        canAtlantic[i] = make([]bool, n)
    }
    dirs := [4][2]int{{-1, 0}, {1, 0}, {0, -1}, {0, 1}} // 上下左右

    // 反向 DFS：只走向高度 >= 当前格子的相邻格子
    var dfs func(row, col int, visited [][]bool)
    dfs = func(row, col int, visited [][]bool) {
        visited[row][col] = true // 标记当前格子已确定能到达对应大洋
        for _, d := range dirs {
            nr, nc := row+d[0], col+d[1]
            if nr < 0 || nr >= m || nc < 0 || nc >= n {
                continue // 越界跳过
            }
            if visited[nr][nc] {
                continue // 已标记过，跳过
            }
            if heights[nr][nc] < heights[row][col] {
                continue // 反向条件：邻居高度必须 >= 当前格子
            }
            dfs(nr, nc, visited)
        }
    }

    // 从太平洋边界（最上行、最左列）出发做多源 DFS
    for i := 0; i < m; i++ {
        dfs(i, 0, canPacific)
    }
    for j := 0; j < n; j++ {
        dfs(0, j, canPacific)
    }

    // 从大西洋边界（最下行、最右列）出发做多源 DFS
    for i := 0; i < m; i++ {
        dfs(i, n-1, canAtlantic)
    }
    for j := 0; j < n; j++ {
        dfs(m-1, j, canAtlantic)
    }

    // 取两个标记数组的交集，即为答案
    result := [][]int{}
    for i := 0; i < m; i++ {
        for j := 0; j < n; j++ {
            if canPacific[i][j] && canAtlantic[i][j] {
                result = append(result, []int{i, j})
            }
        }
    }
    return result
}
```

## 总结

- 这道题最大的坑，就是很容易想到"正向枚举每个格子，分别判断能不能到达边界"，这个思路虽然直观，但复杂度是 O(m²n²)，网格稍微大一点就会超时。解题的关键其实是想到反过来，从边界出发做多源搜索。
- 反向搜索说白了就是图论里"反图"的思想：原图的边方向是"从高处指向不高于自己的相邻格子"，那要算"谁能到达边界集合"，就等价于在反图上算"边界集合能到达谁"，反图的边方向正好是反过来的，体现在代码里，就是把移动条件的大小关系反过来了——原本是"走向小于等于自己"，反向就变成了"走向大于等于自己"。
- 多源 DFS/BFS 效率上的优势，其实来自于"标记去重"这个机制：不管起点有多少个，只要维护一个全局的标记数组，每个格子最多也就被访问一次。所以从 O(m+n) 个边界起点出发的搜索，总代价还是 O(mn)，而不是"起点数乘以单次搜索代价"这么算。
- 最终答案就是两次独立标记结果的交集，这其实提示了一种挺通用的解题模式：当问题要求"同时满足两个独立条件"的时候，你可以把两个条件分别求出各自的可行解集合，最后再取交集，不用非得在一次搜索里同时判断两个条件。
