# 797. 所有可能的路径

**题目描述**

给定一个有 `n` 个节点的有向无环图（DAG），节点编号从 `0` 到 `n-1`，用邻接表 `graph` 表示：`graph[i]` 是一个列表，存放节点 `i` 所有的直接后继节点。请找出所有从节点 `0` 到节点 `n-1` 的路径，并以任意顺序返回这些路径（路径本身内部节点顺序是固定的，不能打乱）。

```
示例：
输入: graph = [[1,2],[3],[3],[]]
输出: [[0,1,3],[0,2,3]]
解释: 图中有两条从 0 到 3（n-1=3）的路径：
     0 -> 1 -> 3
     0 -> 2 -> 3
```

因为题目保证是有向无环图，所以不用担心递归死循环；但也正因为要收集"所有"路径，同一个节点可能出现在多条不同的合法路径里，这就决定了这道题不能用一次性标记的 `visited` 数组。

## 思路

DFS＋回溯：路径从 0 开始，依次加入邻居并递归，返回时撤销；到达 `n-1` 保存路径副本。DAG 无环，无需防环标记；不能用永久 `visited` 阻止不同路径共用节点。

最坏时间 O(n·2ⁿ)，辅助空间 O(n)，不计结果。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。

class Solution:
    # 方法：allPathsSourceTarget；按题目要求处理输入并返回结果，核心算法见方法体。
    def allPathsSourceTarget(self, graph: list[list[int]]) -> list[list[int]]:
        n = len(graph)
        result = []   # 收集所有路径
        path = [0]    # 当前路径，起点 0 先放入

        # 方法：dfs；按题目要求处理输入并返回结果，核心算法见方法体。
        def dfs(node: int) -> None:
            if node == n - 1:          # 终止条件：到达终点 n-1
                result.append(path[:]) # 拷贝当前路径（切片拷贝，防止后续修改影响已收集结果）
                return
            for next_node in graph[node]:  # 遍历当前节点的所有邻居
                path.append(next_node)      # 加入路径
                dfs(next_node)               # 递归深入这个邻居
                path.pop()                   # 回溯：撤销刚才加入的节点

        dfs(0)  # 从节点 0 开始搜索
        return result
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：allPathsSourceTarget；按题目要求处理输入并返回结果，核心算法见方法体。
def allPathsSourceTarget(graph: list[list[int]]) -> list[list[int]]:
    n = len(graph)
    result = []   # 收集所有路径
    path = [0]    # 当前路径，起点 0 先放入

    # 方法：dfs；按题目要求处理输入并返回结果，核心算法见方法体。
    def dfs(node: int) -> None:
        if node == n - 1:          # 终止条件：到达终点 n-1
            result.append(path[:]) # 拷贝当前路径（切片拷贝，防止后续修改影响已收集结果）
            return
        for next_node in graph[node]:  # 遍历当前节点的所有邻居
            path.append(next_node)      # 加入路径
            dfs(next_node)               # 递归深入这个邻居
            path.pop()                   # 回溯：撤销刚才加入的节点

    dfs(0)  # 从节点 0 开始搜索
    return result


if __name__ == "__main__":
    # 用例 1：输入 ([[1, 2], [3], [3], []],)；期望 [[0, 1, 3], [0, 2, 3]]。
    arg0 = [[1, 2], [3], [3], []]
    result = allPathsSourceTarget(arg0)
    expected = [[0, 1, 3], [0, 2, 3]]
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 2：输入 ([[1], []],)；期望 [[0, 1]]。
    arg0 = [[1], []]
    result = allPathsSourceTarget(arg0)
    expected = [[0, 1]]
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 3：输入 ([[], []],)；期望 []。
    arg0 = [[], []]
    result = allPathsSourceTarget(arg0)
    expected = []
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：allPathsSourceTarget；按题目要求处理输入并返回结果，核心算法见方法体。
func allPathsSourceTarget(graph [][]int) [][]int {
    n := len(graph)
    result := make([][]int, 0)
    path := []int{0} // 当前路径，起点 0 先放入

    var dfs func(node int)
    dfs = func(node int) {
        if node == n-1 { // 终止条件：到达终点 n-1
            tmp := make([]int, len(path)) // 拷贝当前路径，避免共享底层数组
            copy(tmp, path)
            result = append(result, tmp)
            return
        }
        for _, next := range graph[node] { // 遍历当前节点的所有邻居
            path = append(path, next) // 加入路径
            dfs(next)                 // 递归深入这个邻居
            path = path[:len(path)-1] // 回溯：撤销刚才加入的节点
        }
    }

    dfs(0) // 从节点 0 开始搜索
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：allPathsSourceTarget；按题目要求处理输入并返回结果，核心算法见方法体。
func allPathsSourceTarget(graph [][]int) [][]int {
    n := len(graph)
    result := make([][]int, 0)
    path := []int{0} // 当前路径，起点 0 先放入

    var dfs func(node int)
    dfs = func(node int) {
        if node == n-1 { // 终止条件：到达终点 n-1
            tmp := make([]int, len(path)) // 拷贝当前路径，避免共享底层数组
            copy(tmp, path)
            result = append(result, tmp)
            return
        }
        for _, next := range graph[node] { // 遍历当前节点的所有邻居
            path = append(path, next) // 加入路径
            dfs(next)                 // 递归深入这个邻居
            path = path[:len(path)-1] // 回溯：撤销刚才加入的节点
        }
    }

    dfs(0) // 从节点 0 开始搜索
    return result
}

func main() {
    // 用例 1：输入 ([[1, 2], [3], [3], []],)；期望 [[0, 1, 3], [0, 2, 3]]。
    {
        arg0 := [][]int{[]int{1, 2}, []int{3}, []int{3}, []int{}}
        result := allPathsSourceTarget(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[[0, 1, 3], [0, 2, 3]] 实际=", string(data))
    }
    // 用例 2：输入 ([[1], []],)；期望 [[0, 1]]。
    {
        arg0 := [][]int{[]int{1}, []int{}}
        result := allPathsSourceTarget(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[[0, 1]] 实际=", string(data))
    }
    // 用例 3：输入 ([[], []],)；期望 []。
    {
        arg0 := [][]int{[]int{}, []int{}}
        result := allPathsSourceTarget(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。

class Solution {
private:
    vector<vector<int>> result;  // 收集所有满足条件的路径
    vector<int> path;            // 当前递归路径，回溯用

    // 方法：dfs；按题目要求处理输入并返回结果，核心算法见方法体。
    void dfs(vector<vector<int>>& graph, int node, int n) {
        if (node == n - 1) {          // 终止条件：到达终点 n-1
            result.push_back(path);   // 拷贝当前路径加入结果集
            return;
        }
        for (int next : graph[node]) {  // 遍历当前节点的所有邻居
            path.push_back(next);       // 把邻居加入路径
            dfs(graph, next, n);        // 递归深入这个邻居
            path.pop_back();            // 回溯：撤销刚才加入的节点
        }
    }

public:
    vector<vector<int>> allPathsSourceTarget(vector<vector<int>>& graph) {
        int n = graph.size();
        path.push_back(0);   // 起点 0 先放入路径
        dfs(graph, 0, n);    // 从节点 0 开始搜索
        return result;
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {
vector<vector<int>> result;  
    vector<int> path;
// 普通函数与辅助函数声明。
void dfs(vector<vector<int>>& graph, int node, int n);
vector<vector<int>> allPathsSourceTargetImpl(vector<vector<int>>& graph);

// 方法：dfs；按题目要求处理输入并返回结果，核心算法见方法体。
    void dfs(vector<vector<int>>& graph, int node, int n) {
        if (node == n - 1) {          // 终止条件：到达终点 n-1
            result.push_back(path);   // 拷贝当前路径加入结果集
            return;
        }
        for (int next : graph[node]) {  // 遍历当前节点的所有邻居
            path.push_back(next);       // 把邻居加入路径
            dfs(graph, next, n);        // 递归深入这个邻居
            path.pop_back();            // 回溯：撤销刚才加入的节点
        }
    }

vector<vector<int>> allPathsSourceTargetImpl(vector<vector<int>>& graph) {
        int n = graph.size();
        path.push_back(0);   // 起点 0 先放入路径
        dfs(graph, 0, n);    // 从节点 0 开始搜索
        return result;
    }
vector<vector<int>> allPathsSourceTarget(vector<vector<int>>& graph) {
    // 清空上次调用的辅助状态，三组输入可以连续独立运行。
    result = {};
    path = {};
    return allPathsSourceTargetImpl(graph);
}
}

int main() {
    // 用例 1：输入 ([[1, 2], [3], [3], []],)；期望 [[0, 1, 3], [0, 2, 3]]。
    {
        vector<vector<int>> arg0 = {{1, 2}, {3}, {3}, {}};
        auto result = interview::allPathsSourceTarget(arg0);
        cout << "用例 1: 期望=[[0, 1, 3], [0, 2, 3]] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([[1], []],)；期望 [[0, 1]]。
    {
        vector<vector<int>> arg0 = {{1}, {}};
        auto result = interview::allPathsSourceTarget(arg0);
        cout << "用例 2: 期望=[[0, 1]] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([[], []],)；期望 []。
    {
        vector<vector<int>> arg0 = {{}, {}};
        auto result = interview::allPathsSourceTarget(arg0);
        cout << "用例 3: 期望=[] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 这道题就是图论 DFS + 回溯模板的直接套用，跟二叉树"收集根到叶子路径"是同一套思路，区别只是邻居来源从"左右孩子"变成了"邻接表"。
- 因为要收集所有路径而不是判断连通性，不能用一次性标记的 `visited`，必须靠 `path` 的"入队-递归-出队"来实现回溯，让每条分支互不干扰。
- 题目保证是有向无环图（DAG），天然无环，所以省去了防死循环的判断，这也是这道题和"钥匙和房间"这种通用图遍历题的关键区别。
- 复杂度不是 O(V+E)，因为路径数本身可能是指数级的，这是"穷举所有解"类图论题的共性，要和"只遍历一次"类题目的复杂度区分开。
