# 685. 冗余连接 II

**题目描述**

给定一个有向图，它原本是一棵"有根树"——除了根节点没有父节点外，其余每个节点都恰好只有一个父节点，且从根节点出发能到达所有其他节点。现在往这棵树上多加了一条有向边（`edges` 数组比合法树多一条边），导致图不再是一棵合法的树。要求找出这条可以删除、使图重新变成合法有根树的多余边。如果存在多个可以删除的候选答案，返回在输入 `edges` 数组中**最后出现**的那一条。

```
示例：
输入: edges = [[1,2],[1,3],[2,3]]
输出: [2,3]
解释: 节点 1 是根，指向 2 和 3；但 3 同时被 1 和 2 指向，形成环，删除 [2,3] 后恢复合法树
```

```
示例：
输入: edges = [[1,2],[2,3],[3,4],[4,1],[1,5]]
输出: [4,1]
解释: 1→2→3→4→1 形成环，且没有节点入度为2，删除环上最后加入的边 [4,1] 即可
```

## 思路

先检查入度：

- 有入度为 2 的节点：优先跳过较晚入边，用并查集检查剩余边。无环则删较晚边，否则删较早边。
- 无入度为 2 的节点：按输入顺序合并端点，首次同根的边即应删除。

当前仅路径压缩：时间 O(n log n) 上界，空间 O(n)；加按秩合并可为 O(nα(n))。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：findRedundantDirectedConnection；按题目要求处理输入并返回结果，核心算法见方法体。
    def findRedundantDirectedConnection(self, edges: list[list[int]]) -> list[int]:
        n = len(edges)
        uf_parent = list(range(n + 1))  # 并查集数组，初始每个节点父节点是自己

        # 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
        def find(x: int) -> int:
            if uf_parent[x] != x:
                uf_parent[x] = find(uf_parent[x])  # 路径压缩
            return uf_parent[x]

        # 方法：build_without_edge；按题目要求处理输入并返回结果，核心算法见方法体。
        def build_without_edge(skip_index: int) -> bool:
            # 尝试把除 skip_index 外的所有边都union，能成功构成树则返回 True
            nonlocal uf_parent
            uf_parent = list(range(n + 1))  # 重新初始化并查集
            for i, (u, v) in enumerate(edges):
                if i == skip_index:
                    continue  # 跳过被假设删除的边
                ru, rv = find(u), find(v)
                if ru == rv:
                    return False  # 成环，假设失败
                uf_parent[ru] = rv
            return True

        parent_of = [0] * (n + 1)  # parent_of[v] 记录 v 当前的父节点
        earlier_idx = later_idx = -1

        for i, (u, v) in enumerate(edges):
            if parent_of[v] != 0:
                for j in range(i):  # 找到较早给 v 赋父节点的边
                    if edges[j][1] == v:
                        earlier_idx = j
                        break
                later_idx = i
                break  # v 入度为2，提前结束扫描
            parent_of[v] = u

        if later_idx != -1:  # 存在入度为2的节点
            if build_without_edge(later_idx):
                return edges[later_idx]  # 删除较晚的边即可成树
            return edges[earlier_idx]  # 否则答案是较早的那条

        # 不存在入度为2的节点，退化为纯环检测
        uf_parent = list(range(n + 1))
        for u, v in edges:
            ru, rv = find(u), find(v)
            if ru == rv:
                return [u, v]  # 第一条成环边即为答案
            uf_parent[ru] = rv

        return []  # 题目保证有解，理论不会到达这里
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；enumerate：Python 内置函数：遍历序列时同时得到下标和值。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：findRedundantDirectedConnection；按题目要求处理输入并返回结果，核心算法见方法体。
def findRedundantDirectedConnection(edges: list[list[int]]) -> list[int]:
    n = len(edges)
    uf_parent = list(range(n + 1))  # 并查集数组，初始每个节点父节点是自己

    # 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
    def find(x: int) -> int:
        if uf_parent[x] != x:
            uf_parent[x] = find(uf_parent[x])  # 路径压缩
        return uf_parent[x]

    # 方法：build_without_edge；按题目要求处理输入并返回结果，核心算法见方法体。
    def build_without_edge(skip_index: int) -> bool:
        # 尝试把除 skip_index 外的所有边都union，能成功构成树则返回 True
        nonlocal uf_parent
        uf_parent = list(range(n + 1))  # 重新初始化并查集
        for i, (u, v) in enumerate(edges):
            if i == skip_index:
                continue  # 跳过被假设删除的边
            ru, rv = find(u), find(v)
            if ru == rv:
                return False  # 成环，假设失败
            uf_parent[ru] = rv
        return True

    parent_of = [0] * (n + 1)  # parent_of[v] 记录 v 当前的父节点
    earlier_idx = later_idx = -1

    for i, (u, v) in enumerate(edges):
        if parent_of[v] != 0:
            for j in range(i):  # 找到较早给 v 赋父节点的边
                if edges[j][1] == v:
                    earlier_idx = j
                    break
            later_idx = i
            break  # v 入度为2，提前结束扫描
        parent_of[v] = u

    if later_idx != -1:  # 存在入度为2的节点
        if build_without_edge(later_idx):
            return edges[later_idx]  # 删除较晚的边即可成树
        return edges[earlier_idx]  # 否则答案是较早的那条

    # 不存在入度为2的节点，退化为纯环检测
    uf_parent = list(range(n + 1))
    for u, v in edges:
        ru, rv = find(u), find(v)
        if ru == rv:
            return [u, v]  # 第一条成环边即为答案
        uf_parent[ru] = rv

    return []  # 题目保证有解，理论不会到达这里


if __name__ == "__main__":
    # 用例 1：输入 ([[1, 2], [1, 3], [2, 3]],)；期望 [2, 3]。
    arg0 = [[1, 2], [1, 3], [2, 3]]
    result = findRedundantDirectedConnection(arg0)
    expected = [2, 3]
    print("用例 1: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 2：输入 ([[1, 2], [2, 3], [3, 4], [4, 1], [1, 5]],)；期望 [4, 1]。
    arg0 = [[1, 2], [2, 3], [3, 4], [4, 1], [1, 5]]
    result = findRedundantDirectedConnection(arg0)
    expected = [4, 1]
    print("用例 2: 期望=", expected, "实际=", result)
    assert result == expected

    # 用例 3：输入 ([[2, 1], [3, 1], [4, 2], [1, 4]],)；期望 [2, 1]。
    arg0 = [[2, 1], [3, 1], [4, 2], [1, 4]]
    result = findRedundantDirectedConnection(arg0)
    expected = [2, 1]
    print("用例 3: 期望=", expected, "实际=", result)
    assert result == expected
```

## Go

### LeetCode 写法与原有示例

```go
var ufParent []int

// 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
func find(x int) int {
    if ufParent[x] != x {
        ufParent[x] = find(ufParent[x]) // 路径压缩
    }
    return ufParent[x]
}

// 尝试把除 skipIndex 外的所有边都union，能成功构成树则返回 true
// 方法：buildWithoutEdge；按题目要求处理输入并返回结果，核心算法见方法体。
func buildWithoutEdge(edges [][]int, n int, skipIndex int) bool {
    ufParent = make([]int, n+1)
    for i := 1; i <= n; i++ {
        ufParent[i] = i // 初始化
    }
    for i, e := range edges {
        if i == skipIndex {
            continue // 跳过被假设删除的边
        }
        u, v := e[0], e[1]
        ru, rv := find(u), find(v)
        if ru == rv {
            return false // 成环，假设失败
        }
        ufParent[ru] = rv
    }
    return true
}

// 方法：findRedundantDirectedConnection；按题目要求处理输入并返回结果，核心算法见方法体。
func findRedundantDirectedConnection(edges [][]int) []int {
    n := len(edges)
    parentOf := make([]int, n+1) // parentOf[v] 记录 v 当前的父节点
    earlierIdx, laterIdx := -1, -1

    for i, e := range edges {
        u, v := e[0], e[1]
        if parentOf[v] != 0 {
            for j := 0; j < i; j++ { // 找到较早给 v 赋父节点的边
                if edges[j][1] == v {
                    earlierIdx = j
                    break
                }
            }
            laterIdx = i
            break // v 入度为2，提前结束扫描
        }
        parentOf[v] = u
    }

    if laterIdx != -1 { // 存在入度为2的节点
        if buildWithoutEdge(edges, n, laterIdx) {
            return edges[laterIdx] // 删除较晚的边即可成树
        }
        return edges[earlierIdx] // 否则答案是较早的那条
    }

    // 不存在入度为2的节点，退化为纯环检测
    ufParent = make([]int, n+1)
    for i := 1; i <= n; i++ {
        ufParent[i] = i
    }
    for _, e := range edges {
        u, v := e[0], e[1]
        ru, rv := find(u), find(v)
        if ru == rv {
            return e // 第一条成环边即为答案
        }
        ufParent[ru] = rv
    }
    return nil // 题目保证有解，理论不会到达这里
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
var ufParent []int

// 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
func find(x int) int {
    if ufParent[x] != x {
        ufParent[x] = find(ufParent[x]) // 路径压缩
    }
    return ufParent[x]
}

// 尝试把除 skipIndex 外的所有边都union，能成功构成树则返回 true
// 方法：buildWithoutEdge；按题目要求处理输入并返回结果，核心算法见方法体。
func buildWithoutEdge(edges [][]int, n int, skipIndex int) bool {
    ufParent = make([]int, n+1)
    for i := 1; i <= n; i++ {
        ufParent[i] = i // 初始化
    }
    for i, e := range edges {
        if i == skipIndex {
            continue // 跳过被假设删除的边
        }
        u, v := e[0], e[1]
        ru, rv := find(u), find(v)
        if ru == rv {
            return false // 成环，假设失败
        }
        ufParent[ru] = rv
    }
    return true
}

// 方法：findRedundantDirectedConnection；按题目要求处理输入并返回结果，核心算法见方法体。
func findRedundantDirectedConnection(edges [][]int) []int {
    n := len(edges)
    parentOf := make([]int, n+1) // parentOf[v] 记录 v 当前的父节点
    earlierIdx, laterIdx := -1, -1

    for i, e := range edges {
        u, v := e[0], e[1]
        if parentOf[v] != 0 {
            for j := 0; j < i; j++ { // 找到较早给 v 赋父节点的边
                if edges[j][1] == v {
                    earlierIdx = j
                    break
                }
            }
            laterIdx = i
            break // v 入度为2，提前结束扫描
        }
        parentOf[v] = u
    }

    if laterIdx != -1 { // 存在入度为2的节点
        if buildWithoutEdge(edges, n, laterIdx) {
            return edges[laterIdx] // 删除较晚的边即可成树
        }
        return edges[earlierIdx] // 否则答案是较早的那条
    }

    // 不存在入度为2的节点，退化为纯环检测
    ufParent = make([]int, n+1)
    for i := 1; i <= n; i++ {
        ufParent[i] = i
    }
    for _, e := range edges {
        u, v := e[0], e[1]
        ru, rv := find(u), find(v)
        if ru == rv {
            return e // 第一条成环边即为答案
        }
        ufParent[ru] = rv
    }
    return nil // 题目保证有解，理论不会到达这里
}

func main() {
    // 用例 1：输入 ([[1, 2], [1, 3], [2, 3]],)；期望 [2, 3]。
    {
        arg0 := [][]int{[]int{1, 2}, []int{1, 3}, []int{2, 3}}
        result := findRedundantDirectedConnection(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[2, 3] 实际=", string(data))
    }
    // 用例 2：输入 ([[1, 2], [2, 3], [3, 4], [4, 1], [1, 5]],)；期望 [4, 1]。
    {
        arg0 := [][]int{[]int{1, 2}, []int{2, 3}, []int{3, 4}, []int{4, 1}, []int{1, 5}}
        result := findRedundantDirectedConnection(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[4, 1] 实际=", string(data))
    }
    // 用例 3：输入 ([[2, 1], [3, 1], [4, 2], [1, 4]],)；期望 [2, 1]。
    {
        arg0 := [][]int{[]int{2, 1}, []int{3, 1}, []int{4, 2}, []int{1, 4}}
        result := findRedundantDirectedConnection(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[2, 1] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。

class Solution {
public:
    vector<int> ufParent; // 并查集数组，与记录树结构的 parent 区分开

    // 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
    int find(int x) {
        if (ufParent[x] != x) {
            ufParent[x] = find(ufParent[x]); // 路径压缩
        }
        return ufParent[x];
    }

    // 尝试把除 skipIndex 外的所有边都union，能成功构成树则返回 true
    // 方法：buildWithoutEdge；按题目要求处理输入并返回结果，核心算法见方法体。
    bool buildWithoutEdge(vector<vector<int>>& edges, int n, int skipIndex) {
        ufParent.resize(n + 1);
        for (int i = 1; i <= n; i++) ufParent[i] = i; // 初始化，每个节点父节点是自己
        for (int i = 0; i < (int)edges.size(); i++) {
            if (i == skipIndex) continue; // 跳过被假设删除的这条边
            int u = edges[i][0], v = edges[i][1];
            int ru = find(u), rv = find(v);
            if (ru == rv) return false; // 两端点已经同根，加入会成环，说明假设失败
            ufParent[ru] = rv; // 合并两个集合
        }
        return true; // 顺利处理完所有边，没有成环
    }

    // 方法：findRedundantDirectedConnection；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> findRedundantDirectedConnection(vector<vector<int>>& edges) {
        int n = edges.size();
        vector<int> parentOf(n + 1, 0); // parentOf[v] 记录 v 当前的父节点
        int earlierIdx = -1, laterIdx = -1; // 记录入度为2冲突的两条边下标

        for (int i = 0; i < n; i++) {
            int u = edges[i][0], v = edges[i][1];
            if (parentOf[v] != 0) {
                earlierIdx = -1; // 先重置，下面重新定位
                for (int j = 0; j < i; j++) { // 找到较早那条给 v 赋父节点的边
                    if (edges[j][1] == v) { earlierIdx = j; break; }
                }
                laterIdx = i; // 当前这条是较晚触发冲突的边
                break; // v 入度为2，无需继续扫描
            }
            parentOf[v] = u;
        }

        if (laterIdx != -1) { // 存在入度为2的节点
            if (buildWithoutEdge(edges, n, laterIdx)) {
                return edges[laterIdx]; // 删除较晚的边即可成树，它就是答案
            }
            return edges[earlierIdx]; // 否则真正的答案是较早的那条
        }

        // 不存在入度为2的节点，退化为纯环检测（与684题相同）
        ufParent.resize(n + 1);
        for (int i = 1; i <= n; i++) ufParent[i] = i;
        for (auto& e : edges) {
            int u = e[0], v = e[1];
            int ru = find(u), rv = find(v);
            if (ru == rv) return e; // 第一条使两端点同根的边即为多余边
            ufParent[ru] = rv;
        }
        return {}; // 理论上不会走到这里（题目保证有解）
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {
vector<int> ufParent;
// 普通函数与辅助函数声明。
int find(int x);
bool buildWithoutEdge(vector<vector<int>>& edges, int n, int skipIndex);
vector<int> findRedundantDirectedConnectionImpl(vector<vector<int>>& edges);

// 方法：find；按题目要求处理输入并返回结果，核心算法见方法体。
    int find(int x) {
        if (ufParent[x] != x) {
            ufParent[x] = find(ufParent[x]); // 路径压缩
        }
        return ufParent[x];
    }

// 尝试把除 skipIndex 外的所有边都union，能成功构成树则返回 true
    // 方法：buildWithoutEdge；按题目要求处理输入并返回结果，核心算法见方法体。
    bool buildWithoutEdge(vector<vector<int>>& edges, int n, int skipIndex) {
        ufParent.resize(n + 1);
        for (int i = 1; i <= n; i++) ufParent[i] = i; // 初始化，每个节点父节点是自己
        for (int i = 0; i < (int)edges.size(); i++) {
            if (i == skipIndex) continue; // 跳过被假设删除的这条边
            int u = edges[i][0], v = edges[i][1];
            int ru = find(u), rv = find(v);
            if (ru == rv) return false; // 两端点已经同根，加入会成环，说明假设失败
            ufParent[ru] = rv; // 合并两个集合
        }
        return true; // 顺利处理完所有边，没有成环
    }

// 方法：findRedundantDirectedConnection；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<int> findRedundantDirectedConnectionImpl(vector<vector<int>>& edges) {
        int n = edges.size();
        vector<int> parentOf(n + 1, 0); // parentOf[v] 记录 v 当前的父节点
        int earlierIdx = -1, laterIdx = -1; // 记录入度为2冲突的两条边下标

        for (int i = 0; i < n; i++) {
            int u = edges[i][0], v = edges[i][1];
            if (parentOf[v] != 0) {
                earlierIdx = -1; // 先重置，下面重新定位
                for (int j = 0; j < i; j++) { // 找到较早那条给 v 赋父节点的边
                    if (edges[j][1] == v) { earlierIdx = j; break; }
                }
                laterIdx = i; // 当前这条是较晚触发冲突的边
                break; // v 入度为2，无需继续扫描
            }
            parentOf[v] = u;
        }

        if (laterIdx != -1) { // 存在入度为2的节点
            if (buildWithoutEdge(edges, n, laterIdx)) {
                return edges[laterIdx]; // 删除较晚的边即可成树，它就是答案
            }
            return edges[earlierIdx]; // 否则真正的答案是较早的那条
        }

        // 不存在入度为2的节点，退化为纯环检测（与684题相同）
        ufParent.resize(n + 1);
        for (int i = 1; i <= n; i++) ufParent[i] = i;
        for (auto& e : edges) {
            int u = e[0], v = e[1];
            int ru = find(u), rv = find(v);
            if (ru == rv) return e; // 第一条使两端点同根的边即为多余边
            ufParent[ru] = rv;
        }
        return {}; // 理论上不会走到这里（题目保证有解）
    }
vector<int> findRedundantDirectedConnection(vector<vector<int>>& edges) {
    // 清空上次调用的辅助状态，三组输入可以连续独立运行。
    ufParent = {};
    return findRedundantDirectedConnectionImpl(edges);
}
}

int main() {
    // 用例 1：输入 ([[1, 2], [1, 3], [2, 3]],)；期望 [2, 3]。
    {
        vector<vector<int>> arg0 = {{1, 2}, {1, 3}, {2, 3}};
        auto result = interview::findRedundantDirectedConnection(arg0);
        cout << "用例 1: 期望=[2, 3] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ([[1, 2], [2, 3], [3, 4], [4, 1], [1, 5]],)；期望 [4, 1]。
    {
        vector<vector<int>> arg0 = {{1, 2}, {2, 3}, {3, 4}, {4, 1}, {1, 5}};
        auto result = interview::findRedundantDirectedConnection(arg0);
        cout << "用例 2: 期望=[4, 1] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ([[2, 1], [3, 1], [4, 2], [1, 4]],)；期望 [2, 1]。
    {
        vector<vector<int>> arg0 = {{2, 1}, {3, 1}, {4, 2}, {1, 4}};
        auto result = interview::findRedundantDirectedConnection(arg0);
        cout << "用例 3: 期望=[2, 1] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 685 题比 684 题多了"方向"这个维度，导致异常模式从单一的"成环"分裂成两种互斥情况：某节点入度为2（多父节点冲突），或所有节点入度都合法但整体构成环（纯环结构）。
- 必须先扫描判断是否存在入度为2的节点，因为这种情况下候选答案是明确的两条边中的一条，不能直接套用684题"逐条union找第一条成环边"的方法，否则可能在同时存在入度冲突和环的场景下选错答案。
- 处理入度为2的节点时，优先假设删除较晚出现的那条边（符合"多个候选取最后出现"的要求），用并查集验证剩余边能否构成合法树；验证失败就说明较晚的边是树结构必需的，答案退化为较早的那条边。
- 排除入度为2的特殊情况后，剩下的纯环场景处理方式和684题完全一致：并查集逐条union，第一条导致两端点同根的边就是答案；整体时间复杂度 O(n)，空间复杂度 O(n)。
