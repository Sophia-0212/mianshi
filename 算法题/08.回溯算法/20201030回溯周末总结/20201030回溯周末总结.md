# 20201030 回溯周末总结

回溯统一执行选择→递归→撤销，区别在候选与终止条件：

- 77 组合：从 `[1,n]` 递增选满 k 个；剩余数量不足可剪枝。剪枝后时间 O(k·C(n,k))，辅助空间 O(k)。
- 216 组合总和 III：从 1～9 选 k 个且和达标，和超标或数量不足即剪枝。时间上界 O(2⁹+k·C(9,k))，辅助空间 O(k)。
- 17 电话号码：每层选当前数字对应的字母，处理完全部数字保存。最坏时间 O(m·4ᵐ)，辅助空间 O(m)。

以上均不计结果空间。
