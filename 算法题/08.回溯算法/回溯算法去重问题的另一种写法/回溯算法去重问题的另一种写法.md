# 回溯算法去重问题的另一种写法

**题目描述**

本篇不针对具体题目，是一篇方法论文档，介绍回溯去重的另一种实现方式——不排序，直接用哈希 set 记录本层已经使用过的值，并对比这种写法和"排序 + 相邻去重"两种方式的适用场景与效率差异。

## 思路

- 排序＋相邻比较：可排序时，同层跳过相同值；额外去重空间 O(1)，排序时间 O(n log n)。

```
if (i > startIndex && nums[i] == nums[i-1]) continue;
```

- 每层集合：不能排序或需保留原序时，记录本层已选值；平均单次判断 O(1)，每层最多 O(n) 空间。

```
unordered_set<int> used;  // 本层去重，进入函数时新建
for (...) {
    if (used.count(nums[i])) continue;
    used.insert(nums[i]);
    ...
}
```

集合用于同层去重；路径中的下标是否可复用，由 `startIndex/used` 单独控制。总复杂度取决于搜索树及结果复制。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

# 写法一：排序 + 相邻去重
# 方法：backtrack_sorted；按题目要求处理输入并返回结果，核心算法见方法体。
def backtrack_sorted(nums, start_index, path, result):
    result.append(path[:])
    for i in range(start_index, len(nums)):
        if i > start_index and nums[i] == nums[i - 1]:   # 相邻位置比较去重
            continue
        path.append(nums[i])
        backtrack_sorted(nums, i + 1, path, result)
        path.pop()

# 写法二：哈希 set 去重
# 方法：backtrack_hashset；按题目要求处理输入并返回结果，核心算法见方法体。
def backtrack_hashset(nums, start_index, path, result):
    result.append(path[:])
    used = set()          # 每层新建，记录本层已处理过的数值
    for i in range(start_index, len(nums)):
        if nums[i] in used:    # 值已出现过，跳过
            continue
        used.add(nums[i])
        path.append(nums[i])
        backtrack_hashset(nums, i + 1, path, result)
        path.pop()```


由于本篇是方法论对比，以下用 0090 子集 II 的双写法对照，展示两种去重方式在同一问题上的实现差异。

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 写法一：排序 + 相邻去重
# 方法：backtrack_sorted；按题目要求处理输入并返回结果，核心算法见方法体。
def backtrack_sorted(nums, start_index, path, result):
    result.append(path[:])
    for i in range(start_index, len(nums)):
        if i > start_index and nums[i] == nums[i - 1]:   # 相邻位置比较去重
            continue
        path.append(nums[i])
        backtrack_sorted(nums, i + 1, path, result)
        path.pop()

# 写法二：哈希 set 去重
# 方法：backtrack_hashset；按题目要求处理输入并返回结果，核心算法见方法体。
def backtrack_hashset(nums, start_index, path, result):
    result.append(path[:])
    used = set()          # 每层新建，记录本层已处理过的数值
    for i in range(start_index, len(nums)):
        if nums[i] in used:    # 值已出现过，跳过
            continue
        used.add(nums[i])
        path.append(nums[i])
        backtrack_hashset(nums, i + 1, path, result)
        path.pop()


if __name__ == "__main__":
    # 用例 1：输入 ([1, 2, 2], 0, [], [])；期望 [[], [1], [1, 2], [1, 2, 2], [2], [2, 2]]。
    arg0 = [1, 2, 2]
    arg1 = 0
    arg2 = []
    arg3 = []
    result = backtrack_sorted(arg0, arg1, arg2, arg3)
    result = arg3  # 原地修改题检查修改后的数据。
    expected = [[], [1], [1, 2], [1, 2, 2], [2], [2, 2]]
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 2：输入 ([1], 0, [], [])；期望 [[], [1]]。
    arg0 = [1]
    arg1 = 0
    arg2 = []
    arg3 = []
    result = backtrack_sorted(arg0, arg1, arg2, arg3)
    result = arg3  # 原地修改题检查修改后的数据。
    expected = [[], [1]]
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 3：输入 ([2, 2], 0, [], [])；期望 [[], [2], [2, 2]]。
    arg0 = [2, 2]
    arg1 = 0
    arg2 = []
    arg3 = []
    result = backtrack_sorted(arg0, arg1, arg2, arg3)
    result = arg3  # 原地修改题检查修改后的数据。
    expected = [[], [2], [2, 2]]
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)```

## Go

### LeetCode 写法与原有示例

```go
// 写法一：排序 + 相邻去重
// 方法：backtrackSorted；按题目要求处理输入并返回结果，核心算法见方法体。
func backtrackSorted(nums []int, startIndex int, path *[]int, result *[][]int) {
    tmp := make([]int, len(*path))
    copy(tmp, *path)
    *result = append(*result, tmp)
    for i := startIndex; i < len(nums); i++ {
        if i > startIndex && nums[i] == nums[i-1] { // 相邻位置比较去重
            continue
        }
        *path = append(*path, nums[i])
        backtrackSorted(nums, i+1, path, result)
        *path = (*path)[:len(*path)-1]
    }
}

// 写法二：哈希 set 去重（Go 用 map[int]bool 模拟 set）
// 方法：backtrackHashSet；按题目要求处理输入并返回结果，核心算法见方法体。
func backtrackHashSet(nums []int, startIndex int, path *[]int, result *[][]int) {
    tmp := make([]int, len(*path))
    copy(tmp, *path)
    *result = append(*result, tmp)
    used := make(map[int]bool)   // 每层新建
    for i := startIndex; i < len(nums); i++ {
        if used[nums[i]] {         // 值已出现过，跳过
            continue
        }
        used[nums[i]] = true
        *path = append(*path, nums[i])
        backtrackHashSet(nums, i+1, path, result)
        *path = (*path)[:len(*path)-1]
    }
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 写法一：排序 + 相邻去重
// 方法：backtrackSorted；按题目要求处理输入并返回结果，核心算法见方法体。
func backtrackSorted(nums []int, startIndex int, path *[]int, result *[][]int) {
    tmp := make([]int, len(*path))
    copy(tmp, *path)
    *result = append(*result, tmp)
    for i := startIndex; i < len(nums); i++ {
        if i > startIndex && nums[i] == nums[i-1] { // 相邻位置比较去重
            continue
        }
        *path = append(*path, nums[i])
        backtrackSorted(nums, i+1, path, result)
        *path = (*path)[:len(*path)-1]
    }
}

// 写法二：哈希 set 去重（Go 用 map[int]bool 模拟 set）
// 方法：backtrackHashSet；按题目要求处理输入并返回结果，核心算法见方法体。
func backtrackHashSet(nums []int, startIndex int, path *[]int, result *[][]int) {
    tmp := make([]int, len(*path))
    copy(tmp, *path)
    *result = append(*result, tmp)
    used := make(map[int]bool)   // 每层新建
    for i := startIndex; i < len(nums); i++ {
        if used[nums[i]] {         // 值已出现过，跳过
            continue
        }
        used[nums[i]] = true
        *path = append(*path, nums[i])
        backtrackHashSet(nums, i+1, path, result)
        *path = (*path)[:len(*path)-1]
    }
}

func main() {
    // 用例 1：输入 ([1, 2, 2], 0, [], [])；期望 [[], [1], [1, 2], [1, 2, 2], [2], [2, 2]]。
    {
        arg0 := []int{1, 2, 2}
        arg1 := 0
        arg2 := []int{}
        arg3 := [][]int{}
        backtrackSorted(arg0, arg1, &arg2, &arg3)
        data, err := json.Marshal(arg3)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[[], [1], [1, 2], [1, 2, 2], [2], [2, 2]] 实际=", string(data))
    }
    // 用例 2：输入 ([1], 0, [], [])；期望 [[], [1]]。
    {
        arg0 := []int{1}
        arg1 := 0
        arg2 := []int{}
        arg3 := [][]int{}
        backtrackSorted(arg0, arg1, &arg2, &arg3)
        data, err := json.Marshal(arg3)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[[], [1]] 实际=", string(data))
    }
    // 用例 3：输入 ([2, 2], 0, [], [])；期望 [[], [2], [2, 2]]。
    {
        arg0 := []int{2, 2}
        arg1 := 0
        arg2 := []int{}
        arg3 := [][]int{}
        backtrackSorted(arg0, arg1, &arg2, &arg3)
        data, err := json.Marshal(arg3)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[[], [2], [2, 2]] 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
// 写法一：排序 + 相邻去重（要求数组可排序）
// 方法：backtrackSorted；按题目要求处理输入并返回结果，核心算法见方法体。
void backtrackSorted(vector<int>& nums, int startIndex, vector<int>& path, vector<vector<int>>& result) {
    result.push_back(path);
    for (int i = startIndex; i < nums.size(); i++) {
        if (i > startIndex && nums[i] == nums[i - 1]) continue;  // 相邻位置比较去重
        path.push_back(nums[i]);
        backtrackSorted(nums, i + 1, path, result);
        path.pop_back();
    }
}

// 写法二：哈希 set 去重（不要求排序，通用写法）
// 方法：backtrackHashSet；按题目要求处理输入并返回结果，核心算法见方法体。
void backtrackHashSet(vector<int>& nums, int startIndex, vector<int>& path, vector<vector<int>>& result) {
    result.push_back(path);
    unordered_set<int> used;   // 每层新建，记录本层已处理过的数值
    for (int i = startIndex; i < nums.size(); i++) {
        if (used.count(nums[i])) continue;  // 值已出现过，跳过
        used.insert(nums[i]);
        path.push_back(nums[i]);
        backtrackHashSet(nums, i + 1, path, result);
        path.pop_back();
    }
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {

// 普通函数与辅助函数声明。
void backtrackSorted(vector<int>& nums, int startIndex, vector<int>& path, vector<vector<int>>& result);
void backtrackHashSet(vector<int>& nums, int startIndex, vector<int>& path, vector<vector<int>>& result);

// 写法一：排序 + 相邻去重（要求数组可排序）
// 方法：backtrackSorted；按题目要求处理输入并返回结果，核心算法见方法体。
void backtrackSorted(vector<int>& nums, int startIndex, vector<int>& path, vector<vector<int>>& result) {
    result.push_back(path);
    for (int i = startIndex; i < nums.size(); i++) {
        if (i > startIndex && nums[i] == nums[i - 1]) continue;  // 相邻位置比较去重
        path.push_back(nums[i]);
        backtrackSorted(nums, i + 1, path, result);
        path.pop_back();
    }
}

// 写法二：哈希 set 去重（不要求排序，通用写法）
// 方法：backtrackHashSet；按题目要求处理输入并返回结果，核心算法见方法体。
void backtrackHashSet(vector<int>& nums, int startIndex, vector<int>& path, vector<vector<int>>& result) {
    result.push_back(path);
    unordered_set<int> used;   // 每层新建，记录本层已处理过的数值
    for (int i = startIndex; i < nums.size(); i++) {
        if (used.count(nums[i])) continue;  // 值已出现过，跳过
        used.insert(nums[i]);
        path.push_back(nums[i]);
        backtrackHashSet(nums, i + 1, path, result);
        path.pop_back();
    }
}

}

int main() {
    // 用例 1：输入 ([1, 2, 2], 0, [], [])；期望 [[], [1], [1, 2], [1, 2, 2], [2], [2, 2]]。
    {
        vector<int> arg0 = {1, 2, 2};
        int arg1 = 0;
        vector<int> arg2 = {};
        vector<vector<int>> arg3 = {};
        interview::backtrackSorted(arg0, arg1, arg2, arg3);
        cout << "用例 1: 期望=[[], [1], [1, 2], [1, 2, 2], [2], [2, 2]] 实际=";
        show(arg3);
        cout << "\n";
    }
    // 用例 2：输入 ([1], 0, [], [])；期望 [[], [1]]。
    {
        vector<int> arg0 = {1};
        int arg1 = 0;
        vector<int> arg2 = {};
        vector<vector<int>> arg3 = {};
        interview::backtrackSorted(arg0, arg1, arg2, arg3);
        cout << "用例 2: 期望=[[], [1]] 实际=";
        show(arg3);
        cout << "\n";
    }
    // 用例 3：输入 ([2, 2], 0, [], [])；期望 [[], [2], [2, 2]]。
    {
        vector<int> arg0 = {2, 2};
        int arg1 = 0;
        vector<int> arg2 = {};
        vector<vector<int>> arg3 = {};
        interview::backtrackSorted(arg0, arg1, arg2, arg3);
        cout << "用例 3: 期望=[[], [2], [2, 2]] 实际=";
        show(arg3);
        cout << "\n";
    }
    return 0;
}```

## 总结

- "排序 + 相邻去重"和"哈希 set 去重"解决的其实是同一个问题（同一层内避免重复选择相同数值），前者依赖排序带来的物理相邻性，后者用额外的数据结构直接记录"本层出现过的值"，不依赖数组的顺序。
- 当题目要求保持原始相对顺序（不能排序）的时候，只能用哈希 set 去重，这就是 0491 题必须采用这种写法的根本原因。
- 当题目允许排序的时候，两种写法都能得到正确结果，"排序 + 相邻去重"通常常数开销更小，是更常见的首选写法；哈希 set 写法更通用，但每层新建 `set` 有额外的空间和时间开销。
- 理解这两种去重方式本质上相通的地方（都是判断"本层是否已经处理过这个数值"），有助于在面对新的去重变形题时灵活选择合适的实现方式，而不是死记某一种固定写法。
