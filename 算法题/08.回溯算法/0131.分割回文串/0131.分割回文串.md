# 0131. 分割回文串

**题目描述**

给定一个字符串 `s`，将 `s` 分割成若干子串，使每个子串都是回文串，返回所有可能的分割方案。

```
示例：
输入: s = "aab"
输出: [["a","a","b"],["aa","b"]]
```

要求：判断一个字符串是否是回文串（正读反读都一样）。

## 思路

回溯枚举当前段终点，用双指针判断回文；合法则加入路径，从下一位置继续，返回时撤销；耗尽字符串后保存分割。

最坏时间 O(n·2ⁿ)，辅助空间 O(n)，不计结果。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：partition；按题目要求处理输入并返回结果，核心算法见方法体。
    def partition(self, s: str) -> list[list[str]]:
        result = []
        path = []

        # 方法：is_palindrome；按题目要求处理输入并返回结果，核心算法见方法体。
        def is_palindrome(left: int, right: int) -> bool:
            while left < right:              # 双指针从两端向中间收缩
                if s[left] != s[right]:
                    return False
                left += 1
                right -= 1
            return True

        # 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
        def backtrack(start_index: int):
            if start_index == len(s):         # 终止条件：切割线到达字符串末尾
                result.append(path[:])
                return
            for i in range(start_index, len(s)):     # 横向遍历：切割线的位置
                if not is_palindrome(start_index, i):  # 剪枝：不是回文串就不递归
                    continue
                path.append(s[start_index:i + 1])      # 【选择】把回文子串加入路径
                backtrack(i + 1)                         # 【递归】处理剩余字符串
                path.pop()                                # 【撤销选择】

        backtrack(0)
        return result```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：partition；按题目要求处理输入并返回结果，核心算法见方法体。
def partition(s: str) -> list[list[str]]:
    result = []
    path = []

    # 方法：is_palindrome；按题目要求处理输入并返回结果，核心算法见方法体。
    def is_palindrome(left: int, right: int) -> bool:
        while left < right:              # 双指针从两端向中间收缩
            if s[left] != s[right]:
                return False
            left += 1
            right -= 1
        return True

    # 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    def backtrack(start_index: int):
        if start_index == len(s):         # 终止条件：切割线到达字符串末尾
            result.append(path[:])
            return
        for i in range(start_index, len(s)):     # 横向遍历：切割线的位置
            if not is_palindrome(start_index, i):  # 剪枝：不是回文串就不递归
                continue
            path.append(s[start_index:i + 1])      # 【选择】把回文子串加入路径
            backtrack(i + 1)                         # 【递归】处理剩余字符串
            path.pop()                                # 【撤销选择】

    backtrack(0)
    return result


if __name__ == "__main__":
    # 用例 1：输入 ('aab',)；期望 [['a', 'a', 'b'], ['aa', 'b']]。
    arg0 = 'aab'
    result = partition(arg0)
    expected = [['a', 'a', 'b'], ['aa', 'b']]
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 2：输入 ('a',)；期望 [['a']]。
    arg0 = 'a'
    result = partition(arg0)
    expected = [['a']]
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 3：输入 ('ab',)；期望 [['a', 'b']]。
    arg0 = 'ab'
    result = partition(arg0)
    expected = [['a', 'b']]
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)```

## Go

### LeetCode 写法与原有示例

```go
// 方法：partition；按题目要求处理输入并返回结果，核心算法见方法体。
func partition(s string) [][]string {
    var result [][]string
    var path []string

    isPalindrome := func(left, right int) bool {
        for left < right {                 // 双指针从两端向中间收缩
            if s[left] != s[right] {
                return false
            }
            left++
            right--
        }
        return true
    }

    var backtrack func(startIndex int)
    backtrack = func(startIndex int) {
        if startIndex == len(s) {          // 终止条件：切割线到达字符串末尾
            tmp := make([]string, len(path))
            copy(tmp, path)
            result = append(result, tmp)
            return
        }
        for i := startIndex; i < len(s); i++ {   // 横向遍历：切割线的位置
            if !isPalindrome(startIndex, i) {      // 剪枝：不是回文串就不递归
                continue
            }
            path = append(path, s[startIndex:i+1]) // 【选择】把回文子串加入路径
            backtrack(i + 1)                        // 【递归】处理剩余字符串
            path = path[:len(path)-1]               // 【撤销选择】
        }
    }
    backtrack(0)
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：partition；按题目要求处理输入并返回结果，核心算法见方法体。
func partition(s string) [][]string {
    var result [][]string
    var path []string

    isPalindrome := func(left, right int) bool {
        for left < right {                 // 双指针从两端向中间收缩
            if s[left] != s[right] {
                return false
            }
            left++
            right--
        }
        return true
    }

    var backtrack func(startIndex int)
    backtrack = func(startIndex int) {
        if startIndex == len(s) {          // 终止条件：切割线到达字符串末尾
            tmp := make([]string, len(path))
            copy(tmp, path)
            result = append(result, tmp)
            return
        }
        for i := startIndex; i < len(s); i++ {   // 横向遍历：切割线的位置
            if !isPalindrome(startIndex, i) {      // 剪枝：不是回文串就不递归
                continue
            }
            path = append(path, s[startIndex:i+1]) // 【选择】把回文子串加入路径
            backtrack(i + 1)                        // 【递归】处理剩余字符串
            path = path[:len(path)-1]               // 【撤销选择】
        }
    }
    backtrack(0)
    return result
}

func main() {
    // 用例 1：输入 ('aab',)；期望 [['a', 'a', 'b'], ['aa', 'b']]。
    {
        arg0 := "aab"
        result := partition(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[[\"a\", \"a\", \"b\"], [\"aa\", \"b\"]] 实际=", string(data))
    }
    // 用例 2：输入 ('a',)；期望 [['a']]。
    {
        arg0 := "a"
        result := partition(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[[\"a\"]] 实际=", string(data))
    }
    // 用例 3：输入 ('ab',)；期望 [['a', 'b']]。
    {
        arg0 := "ab"
        result := partition(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[[\"a\", \"b\"]] 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
private:
    vector<vector<string>> result;
    vector<string> path;

    // 方法：isPalindrome；按题目要求处理输入并返回结果，核心算法见方法体。
    bool isPalindrome(const string& s, int left, int right) {
        while (left < right) {           // 双指针从两端向中间收缩
            if (s[left] != s[right]) return false;
            left++;
            right--;
        }
        return true;
    }

    // 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    void backtrack(const string& s, int startIndex) {
        if (startIndex == s.size()) {        // 终止条件：切割线到达字符串末尾，说明切割完成
            result.push_back(path);
            return;
        }
        for (int i = startIndex; i < s.size(); i++) {  // 横向遍历：本次切割线可以落在哪个位置
            if (!isPalindrome(s, startIndex, i)) {     // 剪枝：不是回文串就不递归
                continue;
            }
            path.push_back(s.substr(startIndex, i - startIndex + 1)); // 【选择】把回文子串加入路径
            backtrack(s, i + 1);                       // 【递归】处理剩余字符串
            path.pop_back();                             // 【撤销选择】回退，尝试下一个切割位置
        }
    }

public:
    vector<vector<string>> partition(string s) {
        backtrack(s, 0);
        return result;
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {
vector<vector<string>> result;
    vector<string> path;
// 普通函数与辅助函数声明。
bool isPalindrome(const string& s, int left, int right);
void backtrack(const string& s, int startIndex);
vector<vector<string>> partitionImpl(string s);

// 方法：isPalindrome；按题目要求处理输入并返回结果，核心算法见方法体。
    bool isPalindrome(const string& s, int left, int right) {
        while (left < right) {           // 双指针从两端向中间收缩
            if (s[left] != s[right]) return false;
            left++;
            right--;
        }
        return true;
    }

// 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    void backtrack(const string& s, int startIndex) {
        if (startIndex == s.size()) {        // 终止条件：切割线到达字符串末尾，说明切割完成
            result.push_back(path);
            return;
        }
        for (int i = startIndex; i < s.size(); i++) {  // 横向遍历：本次切割线可以落在哪个位置
            if (!isPalindrome(s, startIndex, i)) {     // 剪枝：不是回文串就不递归
                continue;
            }
            path.push_back(s.substr(startIndex, i - startIndex + 1)); // 【选择】把回文子串加入路径
            backtrack(s, i + 1);                       // 【递归】处理剩余字符串
            path.pop_back();                             // 【撤销选择】回退，尝试下一个切割位置
        }
    }

vector<vector<string>> partitionImpl(string s) {
        backtrack(s, 0);
        return result;
    }
vector<vector<string>> partition(string s) {
    // 清空上次调用的辅助状态，三组输入可以连续独立运行。
    result = {};
    path = {};
    return partitionImpl(s);
}
}

int main() {
    // 用例 1：输入 ('aab',)；期望 [['a', 'a', 'b'], ['aa', 'b']]。
    {
        string arg0 = "aab";
        auto result = interview::partition(arg0);
        cout << "用例 1: 期望=[[\"a\", \"a\", \"b\"], [\"aa\", \"b\"]] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ('a',)；期望 [['a']]。
    {
        string arg0 = "a";
        auto result = interview::partition(arg0);
        cout << "用例 2: 期望=[[\"a\"]] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ('ab',)；期望 [['a', 'b']]。
    {
        string arg0 = "ab";
        auto result = interview::partition(arg0);
        cout << "用例 3: 期望=[[\"a\", \"b\"]] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 切割问题的本质是组合问题的变种：把"选数字"换成了"选切割位置"，`startIndex` 表示的是"上一次切割结束、本次切割开始"的位置，而不是数组下标的下界。
- 判断当前子串是否合法（这题是判断回文）应该在加入 `path` 之前就完成，不合法就直接跳过这个分支，这本身就是一种针对切割问题的剪枝方式。
- 终止条件是 `startIndex == s.length()`，表示整个字符串被完全消耗、切割完成了，这跟组合问题里"选够 k 个数"的终止条件形式不太一样，但本质都是"任务已完成"。
- 判断回文子串可以用双指针 O(n) 判断，也可以提前用动态规划预处理所有子串是否为回文（空间换时间），后者能优化整体的时间复杂度，是这题常见的进阶优化方向。
