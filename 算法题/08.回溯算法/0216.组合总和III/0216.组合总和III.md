# 0216. 组合总和 III

**题目描述**

找出所有相加之和为 `n` 的 `k` 个数的组合，且满足下列条件：只使用数字 1 到 9，每个数字最多使用一次。返回所有可能的组合列表，解集不能包含重复的组合。

```
示例：
输入: k = 3, n = 7
输出: [[1,2,4]]

输入: k = 3, n = 9
输出: [[1,2,6],[1,3,5],[2,3,4]]
```

## 思路

从 1～9 递增回溯选数，下一层从 `i+1` 开始；选满 `k` 个且和为 `n` 时保存。和过大、剩余数字不足时剪枝，返回时撤销。

时间上界 O(2⁹+k·C(9,k))，辅助空间 O(k)，不计结果。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。；range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：combinationSum3；按题目要求处理输入并返回结果，核心算法见方法体。
    def combinationSum3(self, k: int, n: int) -> list[list[int]]:
        result = []
        path = []
        path_sum = 0  # 用外部变量记录路径和，Python 闭包中修改需要 nonlocal

        # 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
        def backtrack(start_index: int):
            nonlocal path_sum
            if path_sum > n:                      # 剪枝：和已超过 n，不可能凑出结果
                return
            if len(path) == k:                    # 已经选够 k 个数
                if path_sum == n:                  # 且和恰好等于 n
                    result.append(path[:])
                return
            upper_bound = 9 - (k - len(path)) + 1  # 个数剪枝上界
            for i in range(start_index, upper_bound + 1):
                path.append(i)          # 【选择】
                path_sum += i
                backtrack(i + 1)         # 【递归】
                path_sum -= i            # 【撤销选择】
                path.pop()               # 【撤销选择】

        backtrack(1)
        return result```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：combinationSum3；按题目要求处理输入并返回结果，核心算法见方法体。
def combinationSum3(k: int, n: int) -> list[list[int]]:
    result = []
    path = []
    path_sum = 0  # 用外部变量记录路径和，Python 闭包中修改需要 nonlocal

    # 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    def backtrack(start_index: int):
        nonlocal path_sum
        if path_sum > n:                      # 剪枝：和已超过 n，不可能凑出结果
            return
        if len(path) == k:                    # 已经选够 k 个数
            if path_sum == n:                  # 且和恰好等于 n
                result.append(path[:])
            return
        upper_bound = 9 - (k - len(path)) + 1  # 个数剪枝上界
        for i in range(start_index, upper_bound + 1):
            path.append(i)          # 【选择】
            path_sum += i
            backtrack(i + 1)         # 【递归】
            path_sum -= i            # 【撤销选择】
            path.pop()               # 【撤销选择】

    backtrack(1)
    return result


if __name__ == "__main__":
    # 用例 1：输入 (3, 7)；期望 [[1, 2, 4]]。
    arg0 = 3
    arg1 = 7
    result = combinationSum3(arg0, arg1)
    expected = [[1, 2, 4]]
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 2：输入 (3, 9)；期望 [[1, 2, 6], [1, 3, 5], [2, 3, 4]]。
    arg0 = 3
    arg1 = 9
    result = combinationSum3(arg0, arg1)
    expected = [[1, 2, 6], [1, 3, 5], [2, 3, 4]]
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 3：输入 (4, 1)；期望 []。
    arg0 = 4
    arg1 = 1
    result = combinationSum3(arg0, arg1)
    expected = []
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)```

## Go

### LeetCode 写法与原有示例

```go
// 方法：combinationSum3；按题目要求处理输入并返回结果，核心算法见方法体。
func combinationSum3(k int, n int) [][]int {
    var result [][]int
    var path []int
    pathSum := 0

    var backtrack func(startIndex int)
    backtrack = func(startIndex int) {
        if pathSum > n {                          // 剪枝：和已超过 n
            return
        }
        if len(path) == k {                       // 已经选够 k 个数
            if pathSum == n {                      // 且和恰好等于 n
                tmp := make([]int, len(path))
                copy(tmp, path)
                result = append(result, tmp)
            }
            return
        }
        upperBound := 9 - (k - len(path)) + 1      // 个数剪枝上界
        for i := startIndex; i <= upperBound; i++ {
            path = append(path, i)        // 【选择】
            pathSum += i
            backtrack(i + 1)               // 【递归】
            pathSum -= i                   // 【撤销选择】
            path = path[:len(path)-1]      // 【撤销选择】
        }
    }
    backtrack(1)
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：combinationSum3；按题目要求处理输入并返回结果，核心算法见方法体。
func combinationSum3(k int, n int) [][]int {
    var result [][]int
    var path []int
    pathSum := 0

    var backtrack func(startIndex int)
    backtrack = func(startIndex int) {
        if pathSum > n {                          // 剪枝：和已超过 n
            return
        }
        if len(path) == k {                       // 已经选够 k 个数
            if pathSum == n {                      // 且和恰好等于 n
                tmp := make([]int, len(path))
                copy(tmp, path)
                result = append(result, tmp)
            }
            return
        }
        upperBound := 9 - (k - len(path)) + 1      // 个数剪枝上界
        for i := startIndex; i <= upperBound; i++ {
            path = append(path, i)        // 【选择】
            pathSum += i
            backtrack(i + 1)               // 【递归】
            pathSum -= i                   // 【撤销选择】
            path = path[:len(path)-1]      // 【撤销选择】
        }
    }
    backtrack(1)
    return result
}

func main() {
    // 用例 1：输入 (3, 7)；期望 [[1, 2, 4]]。
    {
        arg0 := 3
        arg1 := 7
        result := combinationSum3(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[[1, 2, 4]] 实际=", string(data))
    }
    // 用例 2：输入 (3, 9)；期望 [[1, 2, 6], [1, 3, 5], [2, 3, 4]]。
    {
        arg0 := 3
        arg1 := 9
        result := combinationSum3(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[[1, 2, 6], [1, 3, 5], [2, 3, 4]] 实际=", string(data))
    }
    // 用例 3：输入 (4, 1)；期望 []。
    {
        arg0 := 4
        arg1 := 1
        result := combinationSum3(arg0, arg1)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[] 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
private:
    vector<vector<int>> result;
    vector<int> path;
    int pathSum = 0;  // 记录当前路径的和，避免每次都重新累加

    // 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    void backtrack(int k, int n, int startIndex) {
        if (pathSum > n) {          // 剪枝：和已经超过 n，不可能凑出结果，直接返回
            return;
        }
        if (path.size() == k) {                  // 已经选够 k 个数
            if (pathSum == n) {                   // 且和恰好等于 n，才是合法解
                result.push_back(path);
            }
            return;                               // 无论和是否等于 n，选够 k 个都要返回
        }
        // 剪枝：结合个数剪枝，候选集合固定为 1~9
        for (int i = startIndex; i <= 9 - (k - path.size()) + 1; i++) {
            path.push_back(i);          // 【选择】
            pathSum += i;                // 维护路径和
            backtrack(k, n, i + 1);     // 【递归】
            pathSum -= i;                // 【撤销选择】撤销路径和
            path.pop_back();            // 【撤销选择】撤销路径
        }
    }

public:
    vector<vector<int>> combinationSum3(int k, int n) {
        backtrack(k, n, 1);
        return result;
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {
vector<vector<int>> result;
    vector<int> path;
    int pathSum = 0;
// 普通函数与辅助函数声明。
void backtrack(int k, int n, int startIndex);
vector<vector<int>> combinationSum3Impl(int k, int n);

// 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    void backtrack(int k, int n, int startIndex) {
        if (pathSum > n) {          // 剪枝：和已经超过 n，不可能凑出结果，直接返回
            return;
        }
        if (path.size() == k) {                  // 已经选够 k 个数
            if (pathSum == n) {                   // 且和恰好等于 n，才是合法解
                result.push_back(path);
            }
            return;                               // 无论和是否等于 n，选够 k 个都要返回
        }
        // 剪枝：结合个数剪枝，候选集合固定为 1~9
        for (int i = startIndex; i <= 9 - (k - path.size()) + 1; i++) {
            path.push_back(i);          // 【选择】
            pathSum += i;                // 维护路径和
            backtrack(k, n, i + 1);     // 【递归】
            pathSum -= i;                // 【撤销选择】撤销路径和
            path.pop_back();            // 【撤销选择】撤销路径
        }
    }

vector<vector<int>> combinationSum3Impl(int k, int n) {
        backtrack(k, n, 1);
        return result;
    }
vector<vector<int>> combinationSum3(int k, int n) {
    // 清空上次调用的辅助状态，三组输入可以连续独立运行。
    result = {};
    path = {};
    pathSum = 0;
    return combinationSum3Impl(k, n);
}
}

int main() {
    // 用例 1：输入 (3, 7)；期望 [[1, 2, 4]]。
    {
        int arg0 = 3;
        int arg1 = 7;
        auto result = interview::combinationSum3(arg0, arg1);
        cout << "用例 1: 期望=[[1, 2, 4]] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 (3, 9)；期望 [[1, 2, 6], [1, 3, 5], [2, 3, 4]]。
    {
        int arg0 = 3;
        int arg1 = 9;
        auto result = interview::combinationSum3(arg0, arg1);
        cout << "用例 2: 期望=[[1, 2, 6], [1, 3, 5], [2, 3, 4]] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 (4, 1)；期望 []。
    {
        int arg0 = 4;
        int arg1 = 1;
        auto result = interview::combinationSum3(arg0, arg1);
        cout << "用例 3: 期望=[] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 这题是组合问题叠加"和约束"的典型例子：终止条件从"个数够即收集"变成了"个数够且和等于 n 才收集"，这两个条件缺一不可。
- "和超过 n 就剪枝"是这题最关键的优化点，应该在累加路径和之后就立即判断，而不是等选够 k 个数再判断——不然会有大量注定失败的分支被递归到底才发现无解，白白浪费时间。
- 维护一个 `pathSum` 变量来累加路径和，比每次都对 `path` 求和效率要高，但代价是撤销选择的时候要同步撤销 `pathSum` 的修改，这个地方容易漏写，得格外注意。
- 个数剪枝（剩余候选够不够选够 k 个）和和值剪枝（和是否已超过 n）可以同时用，二者分别作用于 for 循环范围和递归调用前的判断，互不冲突。
