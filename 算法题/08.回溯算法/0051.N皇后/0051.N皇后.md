# 0051. N 皇后

**题目描述**

按照国际象棋的规则，皇后可以攻击与它同一行、同一列或同一斜线上的棋子。`n` 皇后问题研究的是如何将 `n` 个皇后放置在 `n×n` 的棋盘上，并且使皇后彼此之间不能相互攻击。给定整数 `n`，返回所有不同的 `n` 皇后问题的解决方案，每一种解法包含不同的 `n` 皇后问题的棋子放置方案，其中 `Q` 和 `.` 分别代表皇后和空位。

```
示例：
输入: n = 4
输出：
[
  [".Q..","...Q","Q...","..Q."],
  ["..Q.","Q...","...Q",".Q.."]
]
解释：4 皇后问题存在两种不同的解法。
```

## 思路

按行回溯，每行放一个皇后；列、左上及右上对角线无冲突才放置，递归下一行，返回时撤销。放满 `n` 行保存棋盘。

当前逐格检查实现的时间上界 O(n²·n!+R·n²)，辅助空间 O(n²)，结果空间 O(R·n²)，R 为解数。用列和对角线标记可将单次冲突检查降为 O(1)。



## Python

### LeetCode 写法与原有示例

```python
# 依赖说明：range：Python 内置函数：生成按步长遍历的整数序列。

class Solution:
    # 方法：solveNQueens；按题目要求处理输入并返回结果，核心算法见方法体。
    def solveNQueens(self, n: int) -> list[list[str]]:
        result = []
        board = [['.'] * n for _ in range(n)]   # 初始化 n×n 全空棋盘

        # 方法：is_valid；按题目要求处理输入并返回结果，核心算法见方法体。
        def is_valid(row: int, col: int) -> bool:
            for r in range(row):                  # 检查同列
                if board[r][col] == 'Q':
                    return False
            r, c = row - 1, col - 1               # 检查左上方对角线
            while r >= 0 and c >= 0:
                if board[r][c] == 'Q':
                    return False
                r -= 1
                c -= 1
            r, c = row - 1, col + 1               # 检查右上方对角线
            while r >= 0 and c < n:
                if board[r][c] == 'Q':
                    return False
                r -= 1
                c += 1
            return True

        # 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
        def backtrack(row: int):
            if row == n:                          # 终止条件：n 行都放置完毕
                result.append([''.join(r) for r in board])
                return
            for col in range(n):                    # 横向遍历：这一行的候选列
                if not is_valid(row, col):             # 剪枝：冲突则跳过
                    continue
                board[row][col] = 'Q'          # 【选择】
                backtrack(row + 1)              # 【递归】
                board[row][col] = '.'            # 【撤销选择】

        backtrack(0)
        return result
```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。；range：Python 内置函数：生成按步长遍历的整数序列。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：solveNQueens；按题目要求处理输入并返回结果，核心算法见方法体。
def solveNQueens(n: int) -> list[list[str]]:
    result = []
    board = [['.'] * n for _ in range(n)]   # 初始化 n×n 全空棋盘

    # 方法：is_valid；按题目要求处理输入并返回结果，核心算法见方法体。
    def is_valid(row: int, col: int) -> bool:
        for r in range(row):                  # 检查同列
            if board[r][col] == 'Q':
                return False
        r, c = row - 1, col - 1               # 检查左上方对角线
        while r >= 0 and c >= 0:
            if board[r][c] == 'Q':
                return False
            r -= 1
            c -= 1
        r, c = row - 1, col + 1               # 检查右上方对角线
        while r >= 0 and c < n:
            if board[r][c] == 'Q':
                return False
            r -= 1
            c += 1
        return True

    # 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    def backtrack(row: int):
        if row == n:                          # 终止条件：n 行都放置完毕
            result.append([''.join(r) for r in board])
            return
        for col in range(n):                    # 横向遍历：这一行的候选列
            if not is_valid(row, col):             # 剪枝：冲突则跳过
                continue
            board[row][col] = 'Q'          # 【选择】
            backtrack(row + 1)              # 【递归】
            board[row][col] = '.'            # 【撤销选择】

    backtrack(0)
    return result


if __name__ == "__main__":
    # 用例 1：输入 (4,)；期望 [['.Q..', '...Q', 'Q...', '..Q.'], ['..Q.', 'Q...', '...Q', '.Q..']]。
    arg0 = 4
    result = solveNQueens(arg0)
    expected = [['.Q..', '...Q', 'Q...', '..Q.'], ['..Q.', 'Q...', '...Q', '.Q..']]
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 2：输入 (1,)；期望 [['Q']]。
    arg0 = 1
    result = solveNQueens(arg0)
    expected = [['Q']]
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 3：输入 (2,)；期望 []。
    arg0 = 2
    result = solveNQueens(arg0)
    expected = []
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)
```

## Go

### LeetCode 写法与原有示例

```go
// 方法：solveNQueens；按题目要求处理输入并返回结果，核心算法见方法体。
func solveNQueens(n int) [][]string {
    var result [][]string
    board := make([][]byte, n)
    for i := range board {
        board[i] = make([]byte, n)
        for j := range board[i] {
            board[i][j] = '.'          // 初始化全空棋盘
        }
    }

    isValid := func(row, col int) bool {
        for r := 0; r < row; r++ {          // 检查同列
            if board[r][col] == 'Q' {
                return false
            }
        }
        for r, c := row-1, col-1; r >= 0 && c >= 0; r, c = r-1, c-1 {  // 左上对角线
            if board[r][c] == 'Q' {
                return false
            }
        }
        for r, c := row-1, col+1; r >= 0 && c < n; r, c = r-1, c+1 {   // 右上对角线
            if board[r][c] == 'Q' {
                return false
            }
        }
        return true
    }

    var backtrack func(row int)
    backtrack = func(row int) {
        if row == n {                       // 终止条件：n 行都放置完毕
            snapshot := make([]string, n)
            for i, r := range board {
                snapshot[i] = string(r)
            }
            result = append(result, snapshot)
            return
        }
        for col := 0; col < n; col++ {         // 横向遍历：这一行的候选列
            if !isValid(row, col) {              // 剪枝：冲突则跳过
                continue
            }
            board[row][col] = 'Q'          // 【选择】
            backtrack(row + 1)              // 【递归】
            board[row][col] = '.'            // 【撤销选择】
        }
    }
    backtrack(0)
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：solveNQueens；按题目要求处理输入并返回结果，核心算法见方法体。
func solveNQueens(n int) [][]string {
    var result [][]string
    board := make([][]byte, n)
    for i := range board {
        board[i] = make([]byte, n)
        for j := range board[i] {
            board[i][j] = '.'          // 初始化全空棋盘
        }
    }

    isValid := func(row, col int) bool {
        for r := 0; r < row; r++ {          // 检查同列
            if board[r][col] == 'Q' {
                return false
            }
        }
        for r, c := row-1, col-1; r >= 0 && c >= 0; r, c = r-1, c-1 {  // 左上对角线
            if board[r][c] == 'Q' {
                return false
            }
        }
        for r, c := row-1, col+1; r >= 0 && c < n; r, c = r-1, c+1 {   // 右上对角线
            if board[r][c] == 'Q' {
                return false
            }
        }
        return true
    }

    var backtrack func(row int)
    backtrack = func(row int) {
        if row == n {                       // 终止条件：n 行都放置完毕
            snapshot := make([]string, n)
            for i, r := range board {
                snapshot[i] = string(r)
            }
            result = append(result, snapshot)
            return
        }
        for col := 0; col < n; col++ {         // 横向遍历：这一行的候选列
            if !isValid(row, col) {              // 剪枝：冲突则跳过
                continue
            }
            board[row][col] = 'Q'          // 【选择】
            backtrack(row + 1)              // 【递归】
            board[row][col] = '.'            // 【撤销选择】
        }
    }
    backtrack(0)
    return result
}

func main() {
    // 用例 1：输入 (4,)；期望 [['.Q..', '...Q', 'Q...', '..Q.'], ['..Q.', 'Q...', '...Q', '.Q..']]。
    {
        arg0 := 4
        result := solveNQueens(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[[\".Q..\", \"...Q\", \"Q...\", \"..Q.\"], [\"..Q.\", \"Q...\", \"...Q\", \".Q..\"]] 实际=", string(data))
    }
    // 用例 2：输入 (1,)；期望 [['Q']]。
    {
        arg0 := 1
        result := solveNQueens(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[[\"Q\"]] 实际=", string(data))
    }
    // 用例 3：输入 (2,)；期望 []。
    {
        arg0 := 2
        result := solveNQueens(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[] 实际=", string(data))
    }
}
```

## C++

### LeetCode 写法与原有示例

```cpp
// 依赖说明：vector：C++ STL 动态数组。；string：C++ STL 字符串类型。

class Solution {
private:
    vector<vector<string>> result;
    vector<string> board;   // 棋盘，board[row] 是一个长度为 n 的字符串，'.'表示空，'Q'表示皇后

    // 方法：isValid；按题目要求处理输入并返回结果，核心算法见方法体。
    bool isValid(int row, int col, int n) {
        // 检查同列：遍历当前行往上的所有行
        for (int r = 0; r < row; r++) {
            if (board[r][col] == 'Q') return false;      // 同列冲突
        }
        // 检查左上方对角线（主对角线方向，行列差递减）
        for (int r = row - 1, c = col - 1; r >= 0 && c >= 0; r--, c--) {
            if (board[r][c] == 'Q') return false;
        }
        // 检查右上方对角线（副对角线方向）
        for (int r = row - 1, c = col + 1; r >= 0 && c < n; r--, c++) {
            if (board[r][c] == 'Q') return false;
        }
        return true;   // 三种冲突都没有，可以放置
    }

    // 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    void backtrack(int row, int n) {
        if (row == n) {                     // 终止条件：n 行都放置完毕
            result.push_back(board);
            return;
        }
        for (int col = 0; col < n; col++) {    // 横向遍历：这一行皇后可以尝试放在哪一列
            if (!isValid(row, col, n)) continue;  // 剪枝：与已放置的皇后冲突，跳过
            board[row][col] = 'Q';         // 【选择】放置皇后
            backtrack(row + 1, n);         // 【递归】处理下一行
            board[row][col] = '.';           // 【撤销选择】恢复空位，尝试这一行的下一列
        }
    }

public:
    vector<vector<string>> solveNQueens(int n) {
        board.assign(n, string(n, '.'));   // 初始化 n×n 全空棋盘
        backtrack(0, n);
        return result;
    }
};```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {
vector<vector<string>> result;
    vector<string> board;
// 普通函数与辅助函数声明。
bool isValid(int row, int col, int n);
void backtrack(int row, int n);
vector<vector<string>> solveNQueensImpl(int n);

// 方法：isValid；按题目要求处理输入并返回结果，核心算法见方法体。
    bool isValid(int row, int col, int n) {
        // 检查同列：遍历当前行往上的所有行
        for (int r = 0; r < row; r++) {
            if (board[r][col] == 'Q') return false;      // 同列冲突
        }
        // 检查左上方对角线（主对角线方向，行列差递减）
        for (int r = row - 1, c = col - 1; r >= 0 && c >= 0; r--, c--) {
            if (board[r][c] == 'Q') return false;
        }
        // 检查右上方对角线（副对角线方向）
        for (int r = row - 1, c = col + 1; r >= 0 && c < n; r--, c++) {
            if (board[r][c] == 'Q') return false;
        }
        return true;   // 三种冲突都没有，可以放置
    }

// 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    void backtrack(int row, int n) {
        if (row == n) {                     // 终止条件：n 行都放置完毕
            result.push_back(board);
            return;
        }
        for (int col = 0; col < n; col++) {    // 横向遍历：这一行皇后可以尝试放在哪一列
            if (!isValid(row, col, n)) continue;  // 剪枝：与已放置的皇后冲突，跳过
            board[row][col] = 'Q';         // 【选择】放置皇后
            backtrack(row + 1, n);         // 【递归】处理下一行
            board[row][col] = '.';           // 【撤销选择】恢复空位，尝试这一行的下一列
        }
    }

vector<vector<string>> solveNQueensImpl(int n) {
        board.assign(n, string(n, '.'));   // 初始化 n×n 全空棋盘
        backtrack(0, n);
        return result;
    }
vector<vector<string>> solveNQueens(int n) {
    // 清空上次调用的辅助状态，三组输入可以连续独立运行。
    result = {};
    board = {};
    return solveNQueensImpl(n);
}
}

int main() {
    // 用例 1：输入 (4,)；期望 [['.Q..', '...Q', 'Q...', '..Q.'], ['..Q.', 'Q...', '...Q', '.Q..']]。
    {
        int arg0 = 4;
        auto result = interview::solveNQueens(arg0);
        cout << "用例 1: 期望=[[\".Q..\", \"...Q\", \"Q...\", \"..Q.\"], [\"..Q.\", \"Q...\", \"...Q\", \".Q..\"]] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 (1,)；期望 [['Q']]。
    {
        int arg0 = 1;
        auto result = interview::solveNQueens(arg0);
        cout << "用例 2: 期望=[[\"Q\"]] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 (2,)；期望 []。
    {
        int arg0 = 2;
        auto result = interview::solveNQueens(arg0);
        cout << "用例 3: 期望=[] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}
```

## 总结

- 用"行数"作为递归深度是这题最巧妙的设计：它把"每行只能放一个皇后"这个约束直接融入了递归结构，代码里完全不需要专门检查同行冲突。
- 放置皇后前只需要检查"同列"和"两条对角线"这三个方向的冲突，而且因为按行递归的顺序，只需要检查已放置皇后所在的行（当前行的上方），不需要考虑下方尚未处理的行。
- 判断对角线冲突的两个数学关系要记牢：主对角线方向是"行差等于列差"（`row-r == col-c`），副对角线方向是"行列之和相等"（`row+col == r+c`），也可以用两个方向的双指针遍历来实现，效果是等价的。
- N 皇后是棋盘类回溯问题的入门代表，理解了"按维度递归、每层检查冲突、选择-递归-撤销"的模式之后，就可以进一步过渡到更复杂的棋盘问题了，比如数独。
