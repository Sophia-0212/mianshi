# 20201107 回溯周末总结

- 回文切割：每层枚举段末，只递归回文段；字符串耗尽时保存。最坏时间 O(n·2ⁿ)，辅助空间 O(n)。
- IP 切割：四段、每段 1～3 位、0～255、禁止多位前导零；段数与剩余长度不匹配即剪枝。时间、辅助空间 O(1)。
- 子集：每层入口保存路径，包括空集；组合只在满足目标条件时保存。
- 含重复元素的子集：先排序，同层跳过相同值。

子集最坏时间 O(n·2ⁿ)，辅助空间 O(n)，均不计结果。
