# 0017. 电话号码的字母组合

**题目描述**

给定一个仅包含数字 `2-9` 的字符串，返回所有它能表示的字母组合，按九宫格键盘的映射关系（`2:abc`, `3:def`, `4:ghi`, `5:jkl`, `6:mno`, `7:pqrs`, `8:tuv`, `9:wxyz`）。答案可以按任意顺序返回。

```
示例：
输入：digits = "23"
输出：["ad","ae","af","bd","be","bf","cd","ce","cf"]

输入：digits = ""
输出：[]
```

## 思路

回溯：每层处理一位数字，依次选择其对应字母；处理完全部数字时保存字符串，返回时撤销选择。空输入返回空列表。也可用 `itertools.product` 枚举各位字母的笛卡尔积。

最坏时间 O(n·4ⁿ)，辅助空间 O(n)，不计结果，n 为数字位数。



## Python

### LeetCode 写法与原有示例

标准库无直接对应函数可以一行解出九宫格映射结果（`itertools.product` 可以在拿到每位对应的字母表后做笛卡尔积，勉强算"库解"，但本质仍需手动构建字母表，这里只做简要提及）：

```python
from itertools import product

# 依赖说明：Python 标准库 itertools：提供高效的迭代器工具。

class Solution:
    # 方法：letterCombinations；按题目要求处理输入并返回结果，核心算法见方法体。
    def letterCombinations(self, digits: str) -> list[str]:
        if not digits:
            return []
        letter_map = {'2':'abc','3':'def','4':'ghi','5':'jkl','6':'mno','7':'pqrs','8':'tuv','9':'wxyz'}
        pools = [letter_map[d] for d in digits]      # 每一位对应的候选字母表
        return [''.join(p) for p in product(*pools)]  # 笛卡尔积，仅作对比，非回溯写法```


手写回溯实现（还原递归树的逐层选择过程）：

```python
# 依赖说明：len：Python 内置函数：返回容器中元素的数量。

class Solution:
    # 方法：letterCombinations；按题目要求处理输入并返回结果，核心算法见方法体。
    def letterCombinations(self, digits: str) -> list[str]:
        if not digits:
            return []  # 边界：空字符串直接返回空结果

        letter_map = {
            '2': 'abc', '3': 'def', '4': 'ghi', '5': 'jkl',
            '6': 'mno', '7': 'pqrs', '8': 'tuv', '9': 'wxyz'
        }
        result = []
        path = []

        # 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
        def backtrack(index: int):
            if index == len(digits):            # 终止条件：每一位数字都已处理完
                result.append(''.join(path))     # 收集拼出的字母组合
                return
            letters = letter_map[digits[index]]  # 当前数字对应的候选字母表
            for c in letters:                    # 横向遍历本层候选字母
                path.append(c)        # 【选择】
                backtrack(index + 1)  # 【递归】处理下一位数字
                path.pop()             # 【撤销选择】

        backtrack(0)
        return result```

### 面试普通函数写法（可直接运行）

保存为 `main.py`，执行 `python3 main.py`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```python
from __future__ import annotations
from collections import deque, Counter, defaultdict
from typing import Optional, List

# 依赖说明：Python 内置 future 特性：annotations 让类型标注延迟解析。；Python 标准库 collections：deque 是双端队列，Counter 是计数器，defaultdict 是带默认值的字典。；Python 标准库 typing：Optional、List 等只用于类型标注。；len：Python 内置函数：返回容器中元素的数量。；sorted：Python 内置函数：返回排序后的新列表，不修改原列表。

# 普通函数：保留题目的核心算法，不依赖 Solution 或在线判题平台。
# 方法：letterCombinations；按题目要求处理输入并返回结果，核心算法见方法体。
def letterCombinations(digits: str) -> list[str]:
    if not digits:
        return []  # 边界：空字符串直接返回空结果

    letter_map = {
        '2': 'abc', '3': 'def', '4': 'ghi', '5': 'jkl',
        '6': 'mno', '7': 'pqrs', '8': 'tuv', '9': 'wxyz'
    }
    result = []
    path = []

    # 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    def backtrack(index: int):
        if index == len(digits):            # 终止条件：每一位数字都已处理完
            result.append(''.join(path))     # 收集拼出的字母组合
            return
        letters = letter_map[digits[index]]  # 当前数字对应的候选字母表
        for c in letters:                    # 横向遍历本层候选字母
            path.append(c)        # 【选择】
            backtrack(index + 1)  # 【递归】处理下一位数字
            path.pop()             # 【撤销选择】

    backtrack(0)
    return result


if __name__ == "__main__":
    # 用例 1：输入 ('23',)；期望 ['ad', 'ae', 'af', 'bd', 'be', 'bf', 'cd', 'ce', 'cf']。
    arg0 = '23'
    result = letterCombinations(arg0)
    expected = ['ad', 'ae', 'af', 'bd', 'be', 'bf', 'cd', 'ce', 'cf']
    print("用例 1: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 2：输入 ('',)；期望 []。
    arg0 = ''
    result = letterCombinations(arg0)
    expected = []
    print("用例 2: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)

    # 用例 3：输入 ('2',)；期望 ['a', 'b', 'c']。
    arg0 = '2'
    result = letterCombinations(arg0)
    expected = ['a', 'b', 'c']
    print("用例 3: 期望=", expected, "实际=", result)
    assert sorted(result, key=repr) == sorted(expected, key=repr)```

## Go

### LeetCode 写法与原有示例

```go
// 方法：letterCombinations；按题目要求处理输入并返回结果，核心算法见方法体。
func letterCombinations(digits string) []string {
    if len(digits) == 0 {
        return []string{}  // 边界：空字符串直接返回空结果
    }
    letterMap := map[byte]string{
        '2': "abc", '3': "def", '4': "ghi", '5': "jkl",
        '6': "mno", '7': "pqrs", '8': "tuv", '9': "wxyz",
    }
    var result []string
    path := make([]byte, 0, len(digits))

    var backtrack func(index int)
    backtrack = func(index int) {
        if index == len(digits) {              // 终止条件：每一位都已处理完
            result = append(result, string(path))
            return
        }
        letters := letterMap[digits[index]]     // 当前数字对应的候选字母表
        for i := 0; i < len(letters); i++ {     // 横向遍历本层候选字母
            path = append(path, letters[i])     // 【选择】
            backtrack(index + 1)                 // 【递归】
            path = path[:len(path)-1]            // 【撤销选择】
        }
    }
    backtrack(0)
    return result
}
```

### 面试普通函数写法（可直接运行）

保存为 `main.go`，执行 `go run main.go`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```go
// 依赖说明：Go 标准库 encoding/json：JSON 编码和解码。；Go 标准库 fmt：格式化输出和输入。
package main

import (
    "encoding/json"
    "fmt"
)

// 普通函数和题目中的核心算法。
// 方法：letterCombinations；按题目要求处理输入并返回结果，核心算法见方法体。
func letterCombinations(digits string) []string {
    if len(digits) == 0 {
        return []string{}  // 边界：空字符串直接返回空结果
    }
    letterMap := map[byte]string{
        '2': "abc", '3': "def", '4': "ghi", '5': "jkl",
        '6': "mno", '7': "pqrs", '8': "tuv", '9': "wxyz",
    }
    var result []string
    path := make([]byte, 0, len(digits))

    var backtrack func(index int)
    backtrack = func(index int) {
        if index == len(digits) {              // 终止条件：每一位都已处理完
            result = append(result, string(path))
            return
        }
        letters := letterMap[digits[index]]     // 当前数字对应的候选字母表
        for i := 0; i < len(letters); i++ {     // 横向遍历本层候选字母
            path = append(path, letters[i])     // 【选择】
            backtrack(index + 1)                 // 【递归】
            path = path[:len(path)-1]            // 【撤销选择】
        }
    }
    backtrack(0)
    return result
}

func main() {
    // 用例 1：输入 ('23',)；期望 ['ad', 'ae', 'af', 'bd', 'be', 'bf', 'cd', 'ce', 'cf']。
    {
        arg0 := "23"
        result := letterCombinations(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 1: 期望=[\"ad\", \"ae\", \"af\", \"bd\", \"be\", \"bf\", \"cd\", \"ce\", \"cf\"] 实际=", string(data))
    }
    // 用例 2：输入 ('',)；期望 []。
    {
        arg0 := ""
        result := letterCombinations(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 2: 期望=[] 实际=", string(data))
    }
    // 用例 3：输入 ('2',)；期望 ['a', 'b', 'c']。
    {
        arg0 := "2"
        result := letterCombinations(arg0)
        data, err := json.Marshal(result)
        if err != nil { panic(err) }
        fmt.Println("用例 3: 期望=[\"a\", \"b\", \"c\"] 实际=", string(data))
    }
}```

## C++

### LeetCode 写法与原有示例

```cpp
class Solution {
private:
    // 数字到字母的映射表，下标 0-1 不使用（题目保证不出现 0、1）
    vector<string> letterMap = {
        "", "", "abc", "def", "ghi", "jkl", "mno", "pqrs", "tuv", "wxyz"
    };
    vector<string> result;
    string path;

    // 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    void backtrack(const string& digits, int index) {
        if (index == digits.size()) {          // 终止条件：每一位数字都已处理完
            result.push_back(path);            // 收集当前拼出的字母组合
            return;
        }
        int digit = digits[index] - '0';        // 取出当前处理的数字
        const string& letters = letterMap[digit]; // 该数字对应的字母表（本层候选集合）
        for (char c : letters) {                 // 横向遍历：本层的每个候选字母
            path.push_back(c);                   // 【选择】把字母加入路径
            backtrack(digits, index + 1);        // 【递归】处理下一位数字
            path.pop_back();                      // 【撤销选择】回退，尝试下一个字母
        }
    }

public:
    // 方法：letterCombinations；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<string> letterCombinations(string digits) {
        if (digits.empty()) return result;   // 边界：空字符串直接返回空结果
        backtrack(digits, 0);
        return result;
    }
};
```

### 面试普通函数写法（可直接运行）

保存为 `main.cpp`，执行 `c++ -std=c++17 main.cpp -o main && ./main`。下面是完整独立程序，包含 3 组真实输入，并打印期望结果和实际结果。

```cpp
#include <algorithm>
#include <array>
#include <climits>
#include <cmath>
#include <deque>
#include <functional>
#include <iostream>
#include <map>
#include <numeric>
#include <optional>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// 依赖说明：C++ STL algorithm：sort、max、min 等通用算法。；C++ STL array：定长数组。；C++ 标准库 climits：整数边界常量。；C++ 标准库 cmath：数学函数。；C++ STL deque：双端队列。；C++ STL functional：function 等函数对象工具。；C++ 标准库 iostream：控制台输入输出。；C++ STL map：有序键值映射。；C++ STL numeric：数值算法。；C++17 STL optional：表示可能为空的值。；C++ STL queue：队列和 priority_queue。；C++ STL set：有序集合。；C++ STL stack：栈。；C++ STL string：字符串。；C++ STL unordered_map：哈希映射。；C++ STL unordered_set：哈希集合。；C++ STL utility：pair 等通用工具。；C++ STL vector：动态数组。

using namespace std;

// 打印标量和数组，便于直接比较实际结果与期望结果。
template<class T> void show(const T& value) { cout << boolalpha << value; }
void show(const string& value) { cout << '"' << value << '"'; }
void show(char value) { cout << '"' << value << '"'; }
template<class T> void show(const optional<T>& value) {
    if (value) show(*value); else cout << "null";
}
template<class T> void show(const vector<T>& values) {
    cout << "[";
    for (size_t i = 0; i < values.size(); ++i) {
        if (i) cout << ", ";
        show(values[i]);
    }
    cout << "]";
}

namespace interview {
vector<string> letterMap = {
        "", "", "abc", "def", "ghi", "jkl", "mno", "pqrs", "tuv", "wxyz"
    };
    vector<string> result;
    string path;
// 普通函数与辅助函数声明。
void backtrack(const string& digits, int index);
vector<string> letterCombinationsImpl(string digits);

// 方法：backtrack；按题目要求处理输入并返回结果，核心算法见方法体。
    void backtrack(const string& digits, int index) {
        if (index == digits.size()) {          // 终止条件：每一位数字都已处理完
            result.push_back(path);            // 收集当前拼出的字母组合
            return;
        }
        int digit = digits[index] - '0';        // 取出当前处理的数字
        const string& letters = letterMap[digit]; // 该数字对应的字母表（本层候选集合）
        for (char c : letters) {                 // 横向遍历：本层的每个候选字母
            path.push_back(c);                   // 【选择】把字母加入路径
            backtrack(digits, index + 1);        // 【递归】处理下一位数字
            path.pop_back();                      // 【撤销选择】回退，尝试下一个字母
        }
    }

// 方法：letterCombinations；按题目要求处理输入并返回结果，核心算法见方法体。
    vector<string> letterCombinationsImpl(string digits) {
        if (digits.empty()) return result;   // 边界：空字符串直接返回空结果
        backtrack(digits, 0);
        return result;
    }
vector<string> letterCombinations(string digits) {
    // 清空上次调用的辅助状态，三组输入可以连续独立运行。
    letterMap = {
        "", "", "abc", "def", "ghi", "jkl", "mno", "pqrs", "tuv", "wxyz"
    };
    result = {};
    path = {};
    return letterCombinationsImpl(digits);
}
}

int main() {
    // 用例 1：输入 ('23',)；期望 ['ad', 'ae', 'af', 'bd', 'be', 'bf', 'cd', 'ce', 'cf']。
    {
        string arg0 = "23";
        auto result = interview::letterCombinations(arg0);
        cout << "用例 1: 期望=[\"ad\", \"ae\", \"af\", \"bd\", \"be\", \"bf\", \"cd\", \"ce\", \"cf\"] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 2：输入 ('',)；期望 []。
    {
        string arg0 = "";
        auto result = interview::letterCombinations(arg0);
        cout << "用例 2: 期望=[] 实际=";
        show(result);
        cout << "\n";
    }
    // 用例 3：输入 ('2',)；期望 ['a', 'b', 'c']。
    {
        string arg0 = "2";
        auto result = interview::letterCombinations(arg0);
        cout << "用例 3: 期望=[\"a\", \"b\", \"c\"] 实际=";
        show(result);
        cout << "\n";
    }
    return 0;
}```

## 总结

- 这题的关键区别在于"树的宽度不固定"：每一层的候选集合是由当前数字对应的字母表决定的，而不是像组合问题一样有一个固定的候选区间。
- 不需要 `startIndex` 去重，为什么呢？因为每一位处理的是不同位置的数字，候选字母表天然就不重叠，参数 `index` 只起到"往下推进一层"的作用。
- 递归深度等于 `digits` 的长度，每层的分支数等于该位数字对应的字母个数，这种"多叉树"结构在字符串拼接类回溯题目里很常见。
- 空字符串是一个明确的边界条件，必须在进入递归前单独处理，不然会返回一个包含空字符串的错误结果。
